# Legacy root Next.js shell (archived)

Early v0.dev exporter-dashboard prototype that lived at the repository root before apps were split under `apps/*`.

- **Do not deploy** — no Vercel project should point at the repo root or this folder.
- **Production apps:** `apps/dashboard-product`, `apps/marketing`, `apps/offline-product`, `tracebud-backend`.
- Kept for historical reference only (contacts add flow stub).

To run locally (not supported in CI):

```bash
cd legacy/root-v0-next-shell
# Requires a local Next install; prefer working in apps/dashboard-product instead.
```
