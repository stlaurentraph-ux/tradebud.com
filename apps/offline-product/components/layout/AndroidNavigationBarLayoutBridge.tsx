/**
 * TypeScript entry — Metro resolves `.android.tsx` / `.ios.tsx` at bundle time.
 * iOS stub satisfies typecheck when the platform suffix is not selected.
 */
export { AndroidNavigationBarLayoutBridge } from './AndroidNavigationBarLayoutBridge.ios';
