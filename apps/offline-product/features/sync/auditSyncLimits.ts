/** Max declaration audit rows processed per queue pass (remaining rows stay for next Sync now). */
export const MAX_AUDIT_SYNC_ACTIONS_PER_PASS = 5;

/** Backend POST /v1/audit/batch hard limit. */
export const MAX_AUDIT_BATCH_API_SIZE = 20;
