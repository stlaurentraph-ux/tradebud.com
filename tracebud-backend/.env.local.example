# Local backend secrets — copy to tracebud-backend/.env.local (never commit real values).
# Repo root .env.local can hold the same keys; backend scripts read both.

# Tracebud production Supabase (uzsktajlnofosxeqwdwl) — migrations, verify scripts, prod tooling
DATABASE_URL=postgresql://postgres.uzsktajlnofosxeqwdwl:PASSWORD@aws-1-eu-central-1.pooler.supabase.com:6543/postgres?pgbouncer=true

# Supabase Test project (atisrfxsjjvjekwqcbjk) — npm run test:integration only
TEST_DATABASE_URL=postgresql://postgres.atisrfxsjjvjekwqcbjk:PASSWORD@aws-1-eu-west-2.pooler.supabase.com:6543/postgres?pgbouncer=true

# Match Railway production (local API dev optional)
PG_POOL_MAX=5

SUPABASE_URL=https://uzsktajlnofosxeqwdwl.supabase.co
SUPABASE_ANON_KEY=your_anon_key

# Quick checks:
# npm run db:sync:test-env      — copy TEST_DATABASE_URL from repo root if missing
# npm run check:db-connection   — validate prod vs test split + probe both DBs
# npm run test:scripts          — regression tests for URL resolution guards
