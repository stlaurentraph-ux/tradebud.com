# Done Log

Track completed milestones/features/docs updates.

## Initial entries

- FEAT-009 post-closeout hardening slice delivered: DB-backed integration spec `src/integrations/coolfarm-sai-v2.controller.int.spec.ts` now verifies scheduler wrapper token auth fail-closed behavior and persisted sweeper rollup payload (`integration_v2_stale_sweeper_executed`) including scheduler token-version lineage.
- Release hygiene pass delivered: backend full matrix is green (`npm test`: 16/16 suites, 187 tests; `npm run test:integration`: 18/18 suites, 77 tests), reducing latent flaky-failure risk before next lane work.
- FEAT-005 governance cleanup delivered: benchmark import/create/update now canonicalize coffee commodity + ISO-2 geography keys, with backfill migration `tb_v16_017_yield_benchmarks_canonicalize_dimensions.sql` for existing rows.
- FEAT-005 ops automation delivered: yearly FAOSTAT dry-run workflow added (`.github/workflows/faostat-yearly-sync-dry-run.yml`) and operational runbook published (`product-os/04-quality/faostat-yearly-sync-runbook.md`).
- Fixture strategy delivered: scripted cleanup path added (`npm run fixtures:yield-cap:cleanup`) to remove fake farmer/plot/harvest fixture rows after validation runs.
- Milestone prep delivered: next-lane decision pack published (`product-os/01-roadmap/next-milestone-decision-pack.md`) with explicit FEAT-009 vs FEAT-004 execution fork.
- FEAT-009 ops-hardening slice delivered: scheduler token rotation checklist is now documented in `product-os/04-quality/p1-integration-target-decision-template.md` (trigger header/env contract, 90-day cadence, one-pass rotation runbook, and required rotation evidence fields).
- FEAT-009 post-closeout hardening slice delivered: V2 scheduler wrapper now requires explicit token auth (`x-tracebud-scheduler-token`) validated against backend env `COOLFARM_SAI_V2_SCHEDULER_TOKEN`, with deterministic rejection for missing/invalid scheduler credentials.
- FEAT-005 post-closeout hardening slice delivered: harvest benchmark resolver now maps commodity aliases (`coffee` <-> `656` / `coffee, green`), closing runtime mismatch between harvest commodity key and FAOSTAT-imported benchmark commodity values.
- FEAT-005 post-closeout hardening slice delivered: plot creation now persists `plot.geography` (`v.geom::geography`) during insert, resolving `plot_geography_present_check` failures and restoring live fixture plot creation for end-to-end enforcement tests.
- FEAT-005 live enforcement verification delivered: below/near/above cap harvest path is now validated end-to-end against active FAOSTAT benchmark rows with audit evidence (`yield_cap_resolved`, `yield_cap_violation_detected`, non-null `benchmarkId`, `sourceType=FAOSTAT`).
- FEAT-005 operational decision delivered: FAOSTAT benchmark sync/import cadence is now explicitly yearly (aligned to FAOSTAT annual assessments/publications), while dual-control activation remains mandatory.
- FEAT-009 post-closeout hardening slice delivered: V2 stale sweeper now has a scheduler trigger wrapper endpoint (`POST /v1/integrations/coolfarm-sai/v2/runs/release-stale/trigger`) and summary rollup fields (`lastSweeperReleasedCount`, `lastSweeperTriggerSource`) to simplify operations reporting.
- FEAT-009 post-closeout hardening slice delivered: V2 stale-claim sweeper contract now supports scheduled triggers (`triggerSource=scheduled`) and emits execution rollup telemetry (`integration_v2_stale_sweeper_executed`); run summary now includes `lastSweeperRun` metadata for operator diagnostics.
- FEAT-009 post-closeout hardening slice delivered: V2 stale-claim sweeper (`POST /v1/integrations/coolfarm-sai/v2/runs/release-stale`) is now implemented with threshold+limit controls and per-run stale-release telemetry, and V2 run summary now reports `staleClaimCount` for operator visibility.
- FEAT-009 post-closeout hardening slice delivered: V2 run-claim release endpoint (`POST /v1/integrations/coolfarm-sai/v2/runs/{runId}/release`) is now implemented with ownership-aware release rules, forced stale-claim override support, and immutable release telemetry (`integration_v2_run_released`).
- FEAT-009 post-closeout hardening slice delivered: V2 run-claim locking is now implemented with claim metadata migration (`tb_v16_016_coolfarm_sai_v2_run_claim_lock.sql`), due-failed-unclaimed claim endpoint (`POST /v1/integrations/coolfarm-sai/v2/runs/{runId}/claim`), claimed-row queue exclusion, and immutable claim audit event telemetry (`integration_v2_run_claimed`).
- FEAT-009 post-closeout hardening slice delivered: V2 retry queue polling surface is now available at `GET /v1/integrations/coolfarm-sai/v2/runs/retry-queue`, returning tenant-scoped failed runs due for retry (`next_retry_at<=now`) with limit guardrails for worker orchestration.
- FEAT-009 post-closeout hardening slice delivered: V2 retry flow now enforces exponential backoff scheduling (`next_retry_at`) and max-attempt cap (`5`) with deterministic exhaustion telemetry (`integration_v2_run_retry_exhausted`) and schema support from migration `tb_v16_015_coolfarm_sai_v2_retry_backoff.sql`.
- FEAT-009 post-closeout hardening slice delivered: V2 run retry lifecycle is now implemented at `POST /v1/integrations/coolfarm-sai/v2/runs/{runId}/retry` with failed-only eligibility checks, `attempt_count` progression, queue refresh semantics, and immutable retry audit events for outcome lineage.
- FEAT-009 post-closeout hardening slice delivered: V2 run queue metadata (`queued_at`, `attempt_count`, `error_code`) is now persisted via `tb_v16_014_coolfarm_sai_v2_run_queue_metadata.sql`, and tenant-scoped aggregate run-health diagnostics are now available at `GET /v1/integrations/coolfarm-sai/v2/runs/summary`.
- FEAT-009 post-closeout hardening slice delivered: V2 run execution now supports deterministic `completed|failed` outcomes via `POST /v1/integrations/coolfarm-sai/v2/questionnaire-drafts/{id}/runs`, and tenant-safe run diagnostics reads are now exposed at `GET /v1/integrations/coolfarm-sai/v2/questionnaire-drafts/{id}/runs` with immutable run outcome audit telemetry.
- FEAT-009 post-closeout hardening slice delivered: V2 questionnaire submit transition endpoint (`POST /v1/integrations/coolfarm-sai/v2/questionnaire-drafts/{id}/submit`) now enforces canonical `draft->submitted` state progression and records validation run lifecycle (`started/completed`) plus immutable submit audit evidence in isolated V2 tables.
- FEAT-009 post-closeout hardening slice delivered: V2 questionnaire persistence baseline is now in place via SQL migration `tb_v16_013_coolfarm_sai_v2_questionnaire.sql` and `POST /v1/integrations/coolfarm-sai/v2/questionnaire-drafts` save API with tenant-safe idempotent draft upsert + V2 audit event capture (`integration_v2_questionnaire_draft_saved`).
- FEAT-009 post-closeout hardening slice delivered: V2 Cool Farm + SAI schema surface now includes pathway-scoped mapping registry output (`farmQuestionnaireMappingV1`) with fail-fast required-field mapping coverage validation to prevent questionnaire/mapping drift.
- FEAT-009 post-closeout hardening slice delivered: V2 Cool Farm + SAI schema bootstrap endpoint is now implemented at `GET /v1/integrations/coolfarm-sai/v2/questionnaire-schema` with tenant fail-closed + role-scoped access control, pathway-aware (`annuals|rice`) `farmQuestionnaireV1` schema output, and explicit shadow rollout metadata (`coolfarm_sai_v2_enabled`, default `off`).
- FEAT-009 V2 parallel-planning slice delivered: Product OS execution pack is now published at `product-os/01-roadmap/coolfarm-sai-v2-parallel-execution-pack.md` to run Cool Farm + SAI implementation in parallel without V1 integration (feature-flag off by default, isolated persistence, adapter stubs/shadow mode, and readiness gates for later enablement).
- FEAT-005 post-closeout hardening slice delivered: `FAOSTAT` source sync now supports direct authenticated fetch fallback (`x-api-key` via `YIELD_BENCHMARKS_FAOSTAT_API_KEY`) against default `faostatservices` endpoint when adapter URL is not configured, with normalized hg/ha -> kg/ha import mapping and unit coverage.
- FEAT-005 post-closeout hardening slice delivered: source-sync adapter boundary is now implemented via `POST /v1/yield-benchmarks/import/sync-source` (USDA/FAOSTAT URL-configured pull + dry-run) with import-run diagnostics endpoint (`GET /v1/yield-benchmarks/import-runs`) and run-persistence SQL baseline `tb_v16_012_yield_benchmark_import_runs.sql`.
- FEAT-005 post-closeout hardening slice delivered: internal benchmark import scaffold is now implemented (`POST /v1/yield-benchmarks/import`) with deterministic row validation and inactive-draft upsert behavior plus import lifecycle telemetry (`yield_benchmark_import_started`, `yield_benchmark_import_completed`).
- FEAT-005 post-closeout hardening slice delivered: harvest runtime now uses active `yield_benchmarks` (commodity/geography keyed) for yield-cap enforcement with deterministic `GLOBAL` + default fallback behavior, and now emits explicit cap-resolution telemetry (`yield_cap_resolved`, `yield_cap_fallback_used`, `yield_cap_violation_detected`).
- FEAT-009 post-closeout hardening slice delivered: admin interaction coverage now asserts success-path status-read completion toast (`EUDR DDS status read completed (status 200).`) alongside the existing `Download JSON` visibility gating flow.
- FEAT-005 CI gate slice delivered: backend workflow now fail-closes on benchmark-admin claim configuration via dedicated preflight script (`auth:benchmark-admin:claims:check`) requiring `BENCHMARK_ADMIN_ROLE_CLAIMS` with canonical claims (`ADMIN`, `COMPLIANCE_MANAGER`).
- FEAT-009 post-closeout hardening slice delivered: admin interaction coverage now verifies main status `Download JSON` control visibility gating (hidden before successful status read, visible after success).
- FEAT-009 post-closeout hardening slice delivered: admin interaction coverage now validates main status-payload `Download JSON` semantics (expected filename, payload content, URL lifecycle cleanup, success toast).
- FEAT-009 post-closeout hardening slice delivered: admin test harness now centralizes download-path URL/Blob/anchor mocks in reusable helper (`app/admin/test-helpers.tsx`), reducing repeated setup in interaction specs.
- FEAT-009 post-closeout hardening slice delivered: admin interaction coverage now validates downloaded `error JSON` payload content keys (`message`, `referenceNumber`, `occurredAt`) using Blob-stub assertions in jsdom.
- FEAT-009 post-closeout hardening slice delivered: admin interaction coverage now validates `Download error JSON` semantics (expected filename pattern, object URL lifecycle cleanup, and success toast feedback).
- FEAT-009 post-closeout hardening slice delivered: admin interaction coverage now asserts `Copy error context` success branch writes normalized payload (`message`, `referenceNumber`, `occurredAt`) and emits success toast.
- FEAT-009 post-closeout hardening slice delivered: admin interaction coverage now includes `Copy error context` clipboard-failure branch with deterministic error-toast assertion (`Failed to copy EUDR status error context.`).
- FEAT-009 post-closeout hardening slice delivered: admin interaction coverage now includes `Copy filename` clipboard-failure branch with deterministic error-toast assertion (`Failed to copy EUDR status error filename.`).
- FEAT-009 post-closeout hardening slice delivered: admin interaction coverage now asserts `Copy filename` action writes expected export name to clipboard and triggers success toast feedback.
- FEAT-009 post-closeout hardening slice delivered: reusable admin test helper now encapsulates DDS status failure-path setup (`app/admin/test-helpers.tsx`) to reduce duplication and keep assertions focused.
- FEAT-009 post-closeout hardening slice delivered: admin DDS status interaction coverage is now split into focused tests (control labels vs helper note) with shared failure-path setup for narrower failure isolation.
- FEAT-005 operational-safety slice delivered: `/health` now reports benchmark-admin claim diagnostics (`claimEnforced`, `configured`, `requiredClaims`) and warns when claim configuration is empty, with dedicated health controller unit coverage.
- FEAT-009 post-closeout hardening slice delivered: admin interaction coverage now verifies timestamp helper-note rendering (`is replaced at download time.`) in DDS status last-error export panel.
- FEAT-005 auth-hardening closure delivered: yield benchmark admin routes now enforce claim-only authorization (`ADMIN` / `COMPLIANCE_MANAGER`), and legacy exporter-email fallback has been removed with regression coverage for deny behavior.
- FEAT-009 post-closeout hardening slice delivered: admin interaction test now validates accessible names for DDS status last-error export controls (`Copy DDS status error context JSON`, `Download DDS status error context JSON`, `Copy DDS status error export filename`).
- FEAT-005 claim-mapping hardening slice delivered: role derivation now supports canonical auth claims (`ADMIN`, `COMPLIANCE_MANAGER`) and yield-benchmark admin routes enforce claim-first policy with explicit `policyMode` audit attribution plus tested compatibility fallback.
- FEAT-005 runtime governance slice delivered: yield benchmark admin contract is now implemented (`POST/GET /v1/yield-benchmarks`, `PATCH /v1/yield-benchmarks/{id}`, `POST /v1/yield-benchmarks/{id}/activate`) with citable source-reference validation, draft-only mutability, dual-control activation, lifecycle audit events, and SQL baseline `tb_v16_011_yield_benchmarks.sql`.
- P2 spec hardening slice delivered: `TRACEBUD_V1_2_EUDR_SPEC.md` Section 37 bootstrap benchmark table now uses source-verified seed guidance with explicit derivation rule and source-reference baseline field (FAOSTAT primary + USDA FAS cross-check lane).
- FEAT-009 post-closeout hardening slice delivered: DDS status error-export helper controls now include explicit `aria-label` metadata for filename/context copy and JSON download actions.
- FEAT-009 post-closeout hardening slice delivered: DDS status timestamp placeholder token is now defined once in shared utility and reused in filename preview + helper note copy to prevent drift.
- TB-V16-009 quick-dispatch variant delivered: remaining-lanes packet now includes a concise fill-before-send support template (`B7`) with explicit placeholders for environment window/requester metadata while retaining deterministic runbook/verifier PASS criteria.
- TB-V16-009 support handoff template delivered: remaining-lanes packet now includes a copy-paste Supabase support/operator comment with blocker context, exact runbook/verifier requests, expected PASS criteria, and required verifier artifact return.
- TB-V16-009 owner-window operator block delivered: remaining-lanes packet now includes copy-paste `psql` commands (runbook + verifier + output capture) and expected PASS summary row format for deterministic privileged execution evidence.
- TB-V16-009 owner-remediation evidence automation delivered: new verifier script `tracebud-backend/sql/tb_v16_009_postgis_owner_remediation_verify.sql` now emits deterministic `PASS/FAIL` summary and supporting extension/RLS/policy/grant snapshots, and the remaining-lanes execution packet is wired to use it as primary post-run validation.
- FEAT-009 post-closeout hardening slice delivered: DDS status error-context helpers are now extracted into shared utility (`lib/eudr-dds-status-error-context.ts`) with dedicated unit tests, reducing admin-page drift risk.
- FEAT-009 post-closeout hardening slice delivered: DDS status filename generation is now centralized in one builder reused by preview and download, eliminating naming drift risk across escalation artifacts.
- FEAT-009 post-closeout hardening slice delivered: DDS status filename preview now includes explicit `<timestamp>` resolution guidance so operators know the placeholder is replaced at download time.
- FEAT-009 contract lock slice delivered: Tracebud OpenAPI now publishes implemented EUDR integration endpoints (`GET /v1/integrations/eudr/echo`, `POST /v1/integrations/eudr/dds`, `GET /v1/integrations/eudr/dds/status`) under a dedicated `Integrations` tag, and governance checks (`openapi:lint`, `openapi:governance:policy:validate`, `openapi:governance:check`) are green.
- BLK-003 closure delivered: initial integration targets are now formally locked and evidence-linked in `product-os/04-quality/p1-integration-target-decision-template.md` with FEAT-009 OpenAPI publication evidence, so blocker status is closed.
- FEAT-009 post-closeout hardening slice delivered: DDS status last-error panel now supports one-click `Copy filename` from suggested export name with deterministic clipboard toast feedback.
- FEAT-009 post-closeout hardening slice delivered: DDS status last-error panel now includes suggested export filename preview so operators can pre-reference attachment naming before download.
- FEAT-009 post-closeout hardening slice delivered: DDS status last-error panel now shows inline destination guidance so operators know exported JSON artifacts are saved in the browser's default Downloads folder.
- FEAT-009 post-closeout hardening slice delivered: admin DDS status panel now provides one-click `Download error JSON` export (message/timestamp/reference) so escalation artifacts can be attached without manual formatting.
- FEAT-009 post-closeout hardening slice delivered: admin DDS status panel now provides one-click `Copy error context` export (message/timestamp/reference) with deterministic clipboard toast feedback for support escalation handoffs.
- FEAT-009 post-closeout hardening slice delivered: admin DDS status panel now retains and renders compact last-error context (mapped message, timestamp, reference number) after failed reads, and clears it on successful checks.
- FEAT-009 post-closeout hardening slice delivered: admin DDS status-read flow now renders a compact retry/escalation hint block for malformed provider payload failures, with deterministic predicate-driven branching and helper test coverage.
- P1 legal checklist scheduling slice delivered: managed-vendor tracker now includes suggested owner-role assignments, staged due dates, and starter evidence artifact targets to convert governance checklist into an actionable execution plan.
- P1 managed-vendor legal checklist tracker delivered: decision template now includes explicit owner/due/status/evidence columns for each legal/procurement work item to support deterministic production gate closeout.
- P1 integration strategy decision delivered: `product-os/04-quality/p1-integration-target-decision-template.md` now records managed EUDR REST API as primary path with open-source fallback lane, adapter-boundary/feature-flag constraints, and a required managed-vendor legal checklist (MSA/DPA/security/accountability/procurement + AGPL fallback review gate).
- FEAT-009 post-closeout hardening slice delivered: admin DDS status-read errors now map malformed provider payload failures to explicit operator guidance via shared status-feedback helper, with dedicated utility tests and status-route malformed-payload pass-through coverage.
- FEAT-009 post-closeout hardening slice delivered: backend DDS status read now maps malformed provider payloads to deterministic `BadGatewayException` (`EUDR DDS status response was not valid JSON`) with dedicated controller unit coverage.
- FEAT-009 post-closeout hardening slice delivered: replay/non-replay DDS submit success messaging is now centralized in shared helper (`lib/eudr-dds-submit-feedback.ts`) with dedicated regression tests for replay wording and default status fallback.
- FEAT-009 post-closeout hardening slice delivered: dashboard DDS submit flow now surfaces replay-aware success messaging for idempotent duplicate outcomes (`replayed=true`), and proxy route tests lock replay payload pass-through semantics.
- FEAT-009 post-closeout hardening slice delivered: backend DDS submit now treats provider `409` duplicate responses as deterministic idempotency replay success, returns `replayed=true`, and records replay-aware telemetry phase in `integration_eudr_dds_submitted`.
- FEAT-009 post-closeout hardening slice delivered: payload redaction logic is now extracted to shared utility (`apps/dashboard-product/lib/payload-redaction.ts`) with dedicated recursive-masking tests, reducing drift risk across diagnostics surfaces.
- FEAT-009 post-closeout hardening slice delivered: admin DDS status diagnostics now redact likely sensitive payload fields before rendering/copying/downloading status JSON, reducing accidental operator-side PII/secret exposure.
- FEAT-009 post-closeout hardening slice delivered: admin DDS status diagnostics panel now includes `Copy payload` and `Download JSON` actions for quick operator evidence extraction and handoff.
- FEAT-009 post-closeout hardening slice delivered: admin DDS status workflow now includes a last-result diagnostics panel showing checked timestamp, reference number, HTTP status, and formatted provider payload JSON for faster operator triage.
- FEAT-009 post-closeout hardening slice delivered: dashboard DDS status read path is now live with proxy route `GET /api/integrations/eudr/dds/status`, auth pass-through/fail-closed semantics, dedicated route tests, and an admin operator status-check trigger UI.
- FEAT-009 post-closeout hardening slice delivered: backend DDS status read operation is now implemented at `GET /v1/integrations/eudr/dds/status` with `referenceNumber` query requirement, exporter/agent + tenant-claim enforcement, upstream timeout/non-2xx mapping, immutable audit event `integration_eudr_dds_status_checked`, and unit tests for deny/validation/success/failure paths.
- FEAT-009 post-closeout hardening slice delivered: shared DDS schema validator module (`apps/dashboard-product/lib/eudr-dds-validation.ts`) now powers both admin submit preflight and dashboard DDS proxy route validation, with dedicated utility tests and updated proxy-route assertions.
- FEAT-009 post-closeout hardening slice delivered: admin DDS pre-submit checks are now schema-driven via an in-file JSON schema validator (nested required/type validation with path-specific operator error messages), replacing hard-coded field-by-field checks.
- FEAT-009 post-closeout hardening slice delivered: admin DDS operator UI now enforces lightweight pre-submit shape checks (`declarationType`, `referenceNumber`, `operator.name`, `product.commodity`) with focused validation toasts, reducing malformed payload submits before proxy/backend validation.
- FEAT-009 post-closeout hardening slice delivered: admin DDS submit card now includes a `Copy JSON` helper action with clipboard success/failure toasts, enabling quick payload extraction for external verification/debug workflows.
- FEAT-009 post-closeout hardening slice delivered: admin DDS sample loader now includes a scenario preset selector (`import`, `export`, `domestic`) so operators can prefill context-specific payload scaffolds before submit.
- FEAT-009 post-closeout hardening slice delivered: admin EUDR DDS operator UI now includes `Load sample JSON` one-click scaffold action that pre-fills baseline statement structure and rotates idempotency key for faster/manual-safe testing runs.
- FEAT-009 post-closeout hardening slice delivered: admin diagnostics now exposes an operator DDS submit trigger UI wired to `/api/integrations/eudr/dds` with statement JSON + idempotency controls, client-side validation guardrails, loading-state duplicate-submit prevention, success/error toasts, and filing diagnostics refresh on success.
- FEAT-009 post-closeout hardening slice delivered: dashboard proxy route `POST /api/integrations/eudr/dds` is now live and forwards to backend DDS submit path with request validation (`statement`, `idempotencyKey`), fail-closed `TRACEBUD_BACKEND_URL` enforcement, Authorization pass-through, and dedicated route tests.
- FEAT-009 post-closeout hardening slice delivered: first real DDS write operation is now implemented at `POST /v1/integrations/eudr/dds` with exporter-only + tenant-claim policy gates, required payload/idempotency validation, server-side EUDR credential forwarding, timeout and non-2xx failure mapping, immutable audit event `integration_eudr_dds_submitted`, and unit tests for deny/validation/success/failure paths.
- FEAT-009 post-closeout hardening slice delivered: backend EUDR connectivity endpoint (`GET /v1/integrations/eudr/echo`) is now live with tenant-claim + exporter/agent policy enforcement, server-side `EUDR_API_KEY`/`EUDR_API_VERSION`/`EUDR_BASE_URL` runtime contract, bounded timeout and provider-failure mapping, immutable audit event `integration_eudr_echo_checked`, and dedicated unit test coverage.
- P1 integration secret-placement guidance delivered: decision template now defines authoritative EUDR key placement (`tracebud-backend/.env` or `.env.local` only), dashboard boundary (`TRACEBUD_BACKEND_URL` only), production secret-manager requirement, required variable names (`EUDR_API_KEY`, `EUDR_API_VERSION`, `EUDR_BASE_URL`), and explicit `NEXT_PUBLIC_*` prohibition for secrets.
- P1 integration target lock endpoint-seeding delivered: decision template now includes a provisional TRACES DDS lifecycle mapping (`POST /eudr/dds`, `PUT /eudr/dds`, `DELETE /eudr/dds` with `/api` base prefix) plus a mandatory Swagger verification gate before endpoint lock and implementation kickoff.
- P1 integration target lock source-hardening delivered: `product-os/04-quality/p1-integration-target-decision-template.md` now embeds source-backed TRACES/EUDR REST quick-start contract details (required `x-api-key`, optional `x-api-eudr-version` defaulting to v2, base URL `https://www.eudr-api.eu`, echo endpoint, and response-code baseline including `403` credential-setup and `408` timestamp-window semantics).
- P1 external API integration target lock prefill delivered: `product-os/04-quality/p1-integration-target-decision-template.md` now contains a concrete proposed pair (`TRACES NT filing adapter` + `ERP outbound adapter`) with draft contract baselines (auth, endpoint/error envelope, idempotency/retry taxonomy, tenant/role guardrails, analytics evidence, and v1.6 constraint checks) for immediate Product/Engineering decision review.
- P1 external API integration target decision template delivered: `product-os/04-quality/p1-integration-target-decision-template.md` now provides a one-pass lock workflow for selecting the first 1-2 external targets with explicit provider/auth contract, endpoint schema, idempotency/retry, permission/state/exception/event gates, v1.6 architecture checks, and sign-off fields to drive BLK-003 closure.
- FEAT-002 post-closeout hardening slice delivered: queue i18n CI lane now emits a single schema-versioned combined report artifact (`queue-i18n-report.json`) with summary, comparison delta, and baseline provenance, enforced by dedicated generation + schema assertion gates.
- FEAT-002 post-closeout hardening slice delivered: queue i18n artifact bundle now includes the combined report schema (`docs/openapi/queue-i18n-report.schema.json`) alongside payload JSON files, with explicit schema-file presence gate before publish.
- FEAT-002 post-closeout hardening slice delivered: queue i18n combined report now carries schema integrity fingerprint (`schemaSha256`) and assertion logic verifies digest parity against `docs/openapi/queue-i18n-report.schema.json`.
- FEAT-002 post-closeout hardening slice delivered: queue i18n combined-report assertion now rejects non-canonical digest strings by enforcing lowercase 64-hex `schemaSha256` format before parity verification.
- FEAT-002 post-closeout hardening slice delivered: queue i18n combined report now includes explicit digest algorithm metadata (`schemaDigestAlgorithm=sha256`) with schema const-lock and assertion enforcement.
- FEAT-002 post-closeout hardening slice delivered: queue i18n combined report now emits compact digest token (`schemaDigestRef=sha256:<digest>`) with schema pattern + assertion parity enforcement for simpler downstream comparisons.
- FEAT-002 post-closeout hardening slice delivered: queue i18n report contract documentation + CI summary now establish `schemaDigestRef` as primary provenance key and `schemaSha256` as compatibility field, with schema descriptions and assertion order aligned.
- FEAT-002 post-closeout hardening slice delivered: queue i18n combined report v2 now compares current `schemaDigestRef` to the previous artifact’s report when present, exposing drift fields in machine JSON and CI summary.
- FEAT-002 post-closeout hardening slice delivered: queue i18n baseline artifact unzip now resolves previous `queue-i18n-smoke-summary.json` with apps/offline-product-first path fallback for reliable smoke delta extraction.
- FEAT-002 post-closeout hardening slice delivered: queue i18n baseline artifact step now supports optional `ACTIONS_STEP_DEBUG` zip listing when smoke/report extraction misses for operator diagnosis.
- FEAT-002 post-closeout hardening slice delivered: queue i18n baseline fetch step now wires `ACTIONS_STEP_DEBUG` from secrets into step environment so zip-listing diagnostics can actually trigger from bash.
- FEAT-002 post-closeout hardening slice delivered: offline app `package.json` now includes `i18n:queue:verify` one-shot script for local queue i18n governance parity with CI artifact assertions (minus baseline zip fetch).
- FEAT-002 post-closeout hardening slice delivered: offline app now ships `i18n:queue:clean` + `i18n:queue:verify:clean` with Node clean script plus `i18n-queue-verify.mjs` orchestrator replacing shell-chained verify for stability.
- FEAT-002 post-closeout hardening slice delivered: queue i18n clean script now supports `--all` plus `i18n:queue:clean:all` / `i18n:queue:verify:clean:all` for optional baseline/previous/zip artifact cleanup.
- FEAT-002 post-closeout hardening slice delivered: offline app `.gitignore` now lists queue i18n local artifact filenames to prevent accidental commits of verify/replay outputs.
- FEAT-002 post-closeout hardening slice delivered: root `package.json` now includes npm script aliases for queue i18n smoke/verify/clean flows targeting `apps/offline-product`.
- FEAT-005 S1 code slice 1 delivered: deterministic exporter-only package risk score endpoint + OpenAPI contract + unit coverage shipped as baseline risk-scoring execution slice.
- FEAT-005 S1 code slice 2 delivered: risk-score audit lifecycle events (`requested/evaluated/band`) now persist with unit + DB-backed controller-scope integration evidence.
- FEAT-005 S1 code slice 3 delivered: tenant-scoped risk-score activity read endpoint + dashboard admin diagnostics integration shipped with backend/frontend test coverage.
- FEAT-005 S1 code slice 4 delivered: risk-score CSV diagnostics export path + OpenAPI contract publication shipped with API/route test coverage.
- FEAT-005 closeout delivered: architecture-constraint checklist completed, provider/protocol question resolved (`internal_v1` retained), and feature marked Done (`TB-V16-005 / FEAT-005`).
- FEAT-006 S1 code slice 1 delivered: filing middleware execution matrix bootstrap documented and implementation-ready scope gates established.
- FEAT-006 S1 code slice 2 delivered: exporter-only tenant-safe filing pre-flight endpoint (`GET /v1/harvest/packages/{id}/filing-preflight`) now combines readiness+risk, persists `dds_package_filing_preflight_*` lifecycle telemetry, and is covered by unit + DB-backed integration + OpenAPI contract updates.
- FEAT-006 S1 code slice 3 delivered: filing package generation endpoint and idempotent submission contract (`idempotencyKey`) now enforce exporter+tenant scope, persist generation/submission lifecycle telemetry, and publish lint-validated OpenAPI request/response schemas.
- FEAT-006 S1 code slice 4 delivered: filing lifecycle diagnostics (`/v1/audit/gated-entry/filing-activity` list/export) now expose tenant-scoped generation/submission telemetry in backend + dashboard admin with CSV handoff support and typed OpenAPI event contracts.
- FEAT-006 closeout delivered: architecture-constraint checklist and provider/protocol open question are resolved for S1 scope, and feature status is now `Done (TB-V16-006 / FEAT-006)`.
- FEAT-007 S1 code slice 1 delivered: chat threads execution matrix bootstrap documented with permissions, state transitions, exception/recovery semantics, analytics baseline, acceptance mapping, and v1.6 architecture-constraint applicability.
- FEAT-007 S1 code slice 2 delivered: tenant-scoped chat-thread contract endpoints (`list/create/list-messages/post-message`) now enforce signed tenant claims and idempotent message replay semantics, with unit test and OpenAPI contract publication.
- FEAT-007 S1 code slice 3 delivered: chat-thread lifecycle telemetry now persists (`chat_thread_created`, `chat_thread_message_posted`, `chat_thread_message_replayed`), tenant-scoped diagnostics read endpoint (`/v1/audit/gated-entry/chat-threads`) is live, and admin diagnostics now shows chat-thread operator activity with filter/pagination support.
- FEAT-007 S1 code slice 4 delivered: chat-thread lifecycle transition endpoints (`resolve`, `reopen`, `archive`) now enforce deterministic state transitions, persist parity telemetry (`chat_thread_resolved|reopened|archived`), and extend diagnostics/OpenAPI phase coverage for operator investigations.
- FEAT-007 S1 code slice 5 delivered: closeout acceptance pass now includes transition-phase diagnostics assertions (`resolved`) in unit + DB-backed integration coverage, open question resolved for S1 scope, and FEAT-007 marked Done (`TB-V16-007 / FEAT-007`).
- FEAT-008 S1 code slice 1 delivered: dashboards execution matrix bootstrap documented with permissions, state transitions, exception/recovery semantics, analytics event baseline, acceptance mapping, and v1.6 architecture-constraint applicability.
- FEAT-008 S1 code slice 2 delivered: tenant-scoped dashboard diagnostics summary contract is now available (`/v1/audit/gated-entry/dashboard-summary`) with proxy forwarding, admin summary surface, unit + DB-backed integration checks, and OpenAPI schema coverage.
- FEAT-008 S1 code slice 3 delivered: dashboard diagnostics summary now includes deterministic phase/status drill-down counters plus readiness/export guardrails (`hasAnyDiagnostics`, `canExportDetailed`, `latestEventAt`) with backend/frontend test and OpenAPI contract parity.
- FEAT-008 S1 code slice 4 delivered: admin summary widgets now support click-through drill actions into diagnostics filters, export-all actions are readiness-gated, and acceptance assertions now include empty-tenant non-export-ready summary behavior.
- FEAT-008 S1 code slice 5 delivered: acceptance reconciliation and closeout are complete, the open question is resolved to internal API/audit protocol for S1 scope, and FEAT-008 is now marked Done (`TB-V16-008 / FEAT-008`).
- FEAT-009 S1 code slice 1 delivered: integrations execution matrix bootstrap documented with permissions, state transitions, exception/recovery semantics, analytics baseline, acceptance mapping, and v1.6 architecture-constraint applicability.
- FEAT-009 S1 code slice 2 delivered: tenant-safe webhook contract baseline (`POST/GET /v1/webhooks`, `GET /v1/webhooks/{id}/deliveries`) now enforces role/tenant policy and appends immutable integration delivery telemetry evidence with unit + DB-backed integration coverage.
- FEAT-009 S1 code slice 3 delivered: dashboard proxy/admin diagnostics now surface tenant-scoped webhook registrations and selected-webhook delivery evidence timelines with forwarding/fail-closed route coverage.
- FEAT-009 S1 code slice 4 delivered: acceptance reconciliation and closeout are complete, open question resolved to internal API/audit-log webhook evidence protocol for S1 scope, and FEAT-009 is now marked Done (`TB-V16-009 / FEAT-009`).
- FEAT-010 S1 code slice 1 delivered: workflow templates execution matrix bootstrap documented with permissions, state transitions, exception/recovery semantics, analytics baseline, acceptance mapping, and v1.6 architecture-constraint applicability.
- FEAT-010 S1 code slice 2 delivered: tenant-safe workflow template create/list/stage-transition contracts are now live with deterministic transition guards, immutable workflow audit telemetry, backend unit + DB-backed integration tests, and OpenAPI publication.
- FEAT-010 S1 code slice 3 delivered: workflow diagnostics read surface (`/v1/audit/gated-entry/workflow-activity`) is now wired through backend/proxy/hook/admin evidence UI with phase/SLA filters plus unit/integration/OpenAPI coverage.
- FEAT-010 S1 code slice 4 delivered: workflow SLA warning/breach/escalation write-path is now live with deterministic SLA transition/recovery guardrails, role-scoped enforcement, and immutable SLA telemetry parity (`workflow_stage_sla_*` + `workflow_stage_sla_recovered`) across backend tests/OpenAPI contracts.
- FEAT-010 S1 code slice 5 delivered: closeout reconciliation is complete with acceptance mapping confirmed, open question resolved to internal API/audit-log workflow telemetry protocol, and feature marked Done (`TB-V16-010 / FEAT-010`).
- P0-02 legal memo preparation slice delivered: counsel commissioning brief + output validation checklist is now documented at `product-os/04-quality/p0-02-counsel-memo-commissioning-brief.md` (signature still pending; gate not closed).
- P0-02 legal memo intake slice delivered: standardized memo intake template is now documented at `product-os/04-quality/p0-02-counsel-memo-intake-template.md` to capture signed memo metadata, legal decisions, spec deltas, and closure checklist evidence.
- P0-02 legal spec-delta scaffold slice delivered: memo-to-spec patch scaffold is now documented at `product-os/04-quality/p0-02-legal-spec-delta-patch-scaffold.md` with pre-labeled legal-sensitive sections and acceptance checklist for fast gate closeout once memo arrives.
- P0-02 closeout runbook slice delivered: single-shot closeout checklist is now documented at `product-os/04-quality/p0-02-closeout-checklist.md` with exact file update order, touchpoint index, and final validation criteria.
- P0-02 counsel handoff slice delivered: email-ready legal request draft is now documented at `product-os/04-quality/p0-02-counsel-handoff-message-draft.md` with required decision scope, source pack, and signed memo output contract.
- P0-02 counsel dispatch slice delivered: send-ready dispatch checklist is now documented at `product-os/04-quality/p0-02-counsel-dispatch-checklist.md` with pre-send verification, attachment manifest, and post-send status update workflow.
- P0-02 counsel response-tracking slice delivered: response tracker is now documented at `product-os/04-quality/p0-02-counsel-response-tracker.md` with SLA follow-up log and memo-receipt bridge into intake/patch/closeout flow.
- P0-02 response-tracker initialization slice delivered: tracker now includes dispatch-ready placeholder fields and baseline follow-up row to streamline first-send execution.
- P0-02 post-send status snippet slice delivered: reusable status text template is now documented at `product-os/04-quality/p0-02-post-send-status-snippet.md` for immediate consistent logging after counsel dispatch.
- P0-02 dispatch example slice delivered: copy-edit/send-ready counsel request example is now documented at `product-os/04-quality/p0-02-counsel-dispatch-example.md` with paired tracker metadata snippet.
- P0-02 readiness-index slice delivered: one-page gate-state and artifact-chain index is now documented at `product-os/04-quality/p0-02-readiness-index.md` for rapid operator orientation and execution continuity.
- P0-02 final-send-packet slice delivered: single-file dispatch packet is now documented at `product-os/04-quality/p0-02-send-packet.md` with fill-once request fields and post-send logging payload.
- P0-02 assumed-dispatch logging slice delivered: tracker state advanced to `sent_assumed` with explicit placeholder dispatch metadata to keep workflow moving while awaiting confirmed send details.
- P0-02 assumed-dispatch overwrite slice delivered: overwrite checklist is now documented at `product-os/04-quality/p0-02-assumed-dispatch-overwrite-checklist.md` to convert provisional metadata to confirmed values in one pass.
- P0-02 dispatch reset slice delivered: tracker state reverted from `sent_assumed` to `not_sent` after confirming no external counsel is appointed yet.
- FEAT-003 post-closeout hardening slice delivered: historical deforestation decision endpoint (`POST /v1/plots/{id}/deforestation-decision`) is now implemented with cutoff-date verdict contract, immutable audit evidence, tests, and OpenAPI coverage.
- FEAT-003 post-closeout hardening slice delivered: deforestation decision history endpoint (`GET /v1/plots/{id}/deforestation-decision-history`) is now implemented with immutable event-read contract, tests, and OpenAPI coverage.
- FEAT-003 post-closeout hardening slice delivered: plot-detail decision history visibility is now implemented with dashboard hook/panel/proxy and DB-backed integration evidence for persisted decision-history retrieval.
- FEAT-008 post-closeout hardening slice delivered: request campaign bulk contact import UX is now live with CSV upload/paste parsing, validation+dedup, target preview, and draft payload wiring.
- FEAT-008 post-closeout hardening slice delivered: request campaign creation is now API-wired through dashboard proxy with idempotency forwarding and operator-facing async success/failure feedback.
- FEAT-008 post-closeout hardening slice delivered: requests page test suite now covers post-submit success and failure banners for bulk-target draft creation flow.
- FEAT-008 post-closeout hardening slice delivered: requests submit test contract now asserts exact bulk-target payload/body/header mapping for campaign creation.
- FEAT-008 post-closeout hardening slice delivered: malformed CSV row exclusion is now test-locked, proving only valid contacts reach outbound `targets[]`.
- FEAT-008 post-closeout hardening slice delivered: requests parser UI now surfaces `valid / invalid excluded` summary (and duplicate-skip note) during bulk import review.
- FEAT-008 post-closeout hardening slice delivered: requests bulk import now includes downloadable CSV template helper with integration test coverage.
- FEAT-008 post-closeout hardening slice delivered: requests bulk import now includes `Paste sample CSV` quick-fill action with integration test coverage.
- FEAT-008 post-closeout hardening slice delivered: requests CSV parser now supports `name` header alias and surfaces alias-usage warnings in bulk import UI.
- FEAT-008 post-closeout hardening slice delivered: extended CSV alias map now supports `email_address`, `farmerid`, and `plotid` header variants with tested warning visibility.
- FEAT-008 post-closeout hardening slice delivered: spaced/hyphenated CSV header variants are now parsed into canonical fields with tested alias-warning coverage.
- FEAT-003 post-closeout hardening slice delivered: plot-detail deforestation decision run action now triggers POST write path and auto-refreshes decision history after success.
- Product OS scaffold created.
- Canonical read-order + reference rules established.
- v1.6 operating model hardening completed for Cursor rules and command playbooks.
- Product OS status/docs updated to prioritize spatial, HLC, lineage, TRACES chunking, and GDPR shredding lanes.
- All `product-os/02-features` docs aligned to reference v1.6 canonical spec and architecture checklist.
- All `product-os/04-quality` artifacts upgraded with v1.6 acceptance, release, exception, event, and QA coverage.
- v1.6 per-feature gap matrix created with P0/P1/P2 prioritization and rollout waves.
- Wave 1 sprint-ready ticket pack created (`TB-V16-001` to `TB-V16-006`) with dependencies and acceptance tests.
- TB-V16-001 client-side implementation shipped: HLC queue envelope, ordering, and malformed-HLC fallback audit path.
- TB-V16-001 backend slice shipped: malformed HLC rejection at DTO layer and HLC/client event metadata persisted in sync/harvest audit payloads.
- TB-V16-002 core ingestion logic shipped: polygon `ST_MakeValid` normalization and >5% correction variance rejection in plot create flow.
- TB-V16-003 planning artifacts shipped: SQL migration scaffold and ADR for additive GEOGRAPHY cutover strategy.
- Backend Jest harness added and TB-V16-002 automated unit tests shipped (polygon correction variance accept/reject paths).
- PostGIS integration test lane added with env-gated execution via `TEST_DATABASE_URL`.
- CI gate wired: backend workflow now enforces `TEST_DATABASE_URL` and runs `npm run test:integration`.
- Canonical spec cleaned: historical v1.5 redline critique moved to archive and v1.6 spec kept normative and execution-focused.
- Spec execution board created (`product-os/01-roadmap/v1-6-spec-execution-board.md`) with P1-P6 ownership, dependencies, and acceptance criteria.
- Non-blocking P2 spec hardening shipped in canonical spec: controlled consent purpose vocabulary, parse_result canonical schemas, shipment JSONB canonical keys, simplified declaration geolocation payload rules, audit event partitioning strategy, risk intersection scoring, notification delivery model, portability export format, Stripe integration section, and API error taxonomy.
- Canonical spec expanded with sections 51-55: MVP deadline phasing, commodity-specific due diligence, data reality constraints, data escrow continuity, and internal consistency rules.
- Open questions register extended with OQ-09 and OQ-10; execution board now includes Priority 0 pre-engineering human gates and a contradiction-log artifact template.
- Machine-readable API artifact added: `docs/openapi/tracebud-v1-draft.yaml` generated from Section 32.8 endpoint contract catalog.
- Dashboard resume hardening shipped for cooperative inbox and demo readiness flows: permission-gated actions now include async recovery UX and audit-event emission; auth login loading state now recovers correctly on credential errors.
- Cooperative inbox data is now tenant-scoped via `request-service` + `useInboxRequests` (replacing page-local mock list), and demo bootstrap seed/reset now includes inbox request state.
- Dashboard inbox now exposes API endpoints for list/respond/bootstrap flows, and `useInboxRequests` is API-first with fallback; admin/demo seed actions are async-synced to API bootstrap state.
- `tracebud-backend` now includes an inbox module with persistent snapshot events in `audit_log` and `/api/v1/inbox-requests` endpoints; dashboard API routes proxy to backend via `TRACEBUD_BACKEND_URL` with local fallback.
- Backend inbox endpoints now enforce authenticated tenant context (derived from auth claims/profile) and dashboard proxy routes forward bearer auth headers to backend.
- User-facing role naming is now aligned to product language (Producer/Supplier/Buyer/Sponsor/Reviewer) across badges, login personas, sidebar labels, and admin role displays, while internal role keys remain unchanged.
- Secondary terminology cleanup pass completed across active demo/readiness/UI copy (runbook, readiness checklist, inbox, settings, harvest/package helper text, and legacy sidebar component).
- Historical dashboard markdown artifacts were normalized to the same terminology set (Producer/Supplier/Buyer/Sponsor/Reviewer) for consistency across non-functional docs.
- Readability polish applied to `DESIGN_AUDIT.md` and `MVP_GAP_ANALYSIS.md` to improve flow after terminology migration.
- Final markdown style pass completed for `V1_REMEDIATION_BLUEPRINT.md`, `DASHBOARD_MVP_PLAN.md`, and `GRADING_ANALYSIS.md` for consistent tone and readability.
- Strategic handoff artifacts created for next execution slice: `dashboard-auth-tenant-hardening-week-plan.md` and `FEAT-001` planning status updated with TB-DBX-001..004 sequence.
- TB-DBX-001 delivered: backend inbox tenant resolution is now signed-claim-only (no email/domain fallback), tenant claim is mandatory for list/respond/bootstrap, and dashboard inbox proxy preserves backend auth semantics (`401`/`403`) while still forwarding bearer tokens.
- TB-DBX-002 delivered: backend inbox persistence now uses dedicated tenant-scoped tables (`inbox_requests`, `inbox_request_events`) with indexes and idempotent respond transitions; state-changing operations continue to emit audit events for traceability.
- TB-DBX-003 test slice delivered: added inbox tenant/state integration tests and controller claim-enforcement tests for signed tenant claims and exporter-only bootstrap authorization.
- TB-DBX-004 delivered: deferred dashboard routes are now feature-gated at both nav and route-entry levels (`/requests`, `/reports`) with default MVP-safe flag values and a centralized gate registry/document.
- TB-DBX-004 assertions delivered: dashboard package now includes automated gate tests (Vitest) validating default MVP blocking and flag-enabled access behavior for deferred routes.
- TB-DBX-004 CI enforcement delivered: root CI workflow now runs dashboard `npm test` in a dedicated required `dashboard` job.
- Founder OS foundation delivered: additive Supabase migrations/seeds/functions for outreach CRM and content cadence, plus non-breaking website lead mirror into `prospects` and `outreach_activity`.
- Founder OS automation specs delivered: n8n workflow spec artifacts and setup runbook for daily outreach intelligence, weekly content planning, accountability checks, monthly review, and missed schedule alerts.
- Founder OS dashboard phase 4 delivered: internal CRM/content pages and Supabase-backed API routes are now scaffolded in `apps/dashboard-product` with role-gated navigation entries and typed client hooks.
- Founder OS Lite loop delivered: `Today` dashboard now tracks hard founder cadence goals (3 outreach actions/weekday, 2 posts/week), supports one-click queue/post plan bootstrapping, and captures outreach exchanges directly per person.
- Backend report endpoint hardening delivered: `v1/reports` now requires exporter role, with controller-level regression tests to prevent unauthorized farmer/agent report access regressions.
- Backend harvest/plots scope hardening delivered: farmer-role requests are now ownership-checked against `farmer_profile.user_id` for farmerId/plotId paths, and harvest package listing/detail/TRACES export endpoints now enforce exporter-only access; controller regression tests added.
- Backend ownership integration coverage delivered: env-gated `ownership-scope.int.spec.ts` now validates farmer ownership and plot ownership join behavior against real Postgres fixtures using isolated schema setup.
- Backend controller ownership integration coverage delivered: env-gated `controller-scope.int.spec.ts` now validates controller-level farmer scope deny/allow behavior for harvest vouchers and plot list/update paths with DB-backed ownership checks.
- Ownership CI gate delivered: backend CI now runs `test:integration:ownership` as a dedicated required step, making ownership-scope integration execution explicit and auditable.
- Signed tenant-claim enforcement expanded: backend `harvest`, `plots`, and `reports` controllers now reject requests missing `tenant_id` in signed token metadata; regression tests updated to cover missing-claim denial paths.
- Signed tenant-claim enforcement closure delivered: `audit` controller now also enforces signed `tenant_id` claim with dedicated regression tests, completing coverage for all current `SupabaseAuthGuard` controllers.
- Package/report export API integration coverage delivered: new DB-backed integration test validates combined tenant-claim + exporter-role policy deny/allow for `getPackageTracesJson` and `plotsReport`, and is included in required ownership integration CI script.
- Package/report export API integration coverage expanded: same DB-backed test now also validates `harvestsReport` deny/allow under combined tenant-claim + exporter-role policy.
- Ownership CI evidence publishing delivered: backend CI now writes ownership integration output log, adds a check-summary snapshot in job summary, and uploads raw run output as artifact (`backend-ownership-integration-log`).
- Package access integration coverage expanded: DB-backed policy test now also validates `HarvestController.listPackages` and `HarvestController.getPackage` deny/allow under tenant-claim + exporter-role requirements.
- Ownership CI enforcement tightened: backend CI now fails if ownership integration output reports non-zero skipped suites/tests, preventing silent bypass of DB-backed access-policy coverage.
- Release QA evidence scaffold delivered: added `product-os/04-quality/release-qa-evidence.md` with required CI run/artifact fields for ownership/access signoff.
- Ownership/access local validation delivered: DB-backed `test:integration:ownership` now confirmed non-skipped in local execution (`3/3 suites passed`, `7/7 tests passed`) with `TEST_DATABASE_URL` inline injection.
- Ownership/access CI validation delivered: backend CI run `24401646754` executed required ownership lane non-skipped (`3/3 suites`, `7/7 tests`) and published evidence artifact `backend-ownership-integration-log` (`6428758408`) with QA template updated.
- CI deprecation follow-up ticket created: issue #22 tracks Node 20 action-runtime modernization in GitHub Actions (`https://github.com/stlaurentraph-ux/tradebud.com/issues/22`).
- CI runtime modernization implemented: workflow now opts GitHub actions into Node 24 runtime (`FORCE_JAVASCRIPT_ACTIONS_TO_NODE24=true`) and uses Node 20 for backend/app jobs to align with dependency engine requirements.
- Founder OS Lite analytics/streak slice delivered: planning and completion actions now emit founder analytics via audit-event helper, `Today` shows outreach/publishing cadence streaks, and CRM daily-actions API now supports history-mode retrieval for streak computation.
- Dashboard inbox backend-only hardening delivered: proxy routes for list/respond/bootstrap now fail closed when backend is unavailable, and `useInboxRequests` no longer falls back to local in-memory request state.
- Dashboard inbox proxy regression tests delivered: route tests now verify fail-closed `503` behavior when backend URL is missing and Authorization header pass-through for list/respond/bootstrap backend calls.
- Dashboard inbox proxy denial propagation delivered: route tests now verify backend `401/403` statuses and error payloads are forwarded unchanged for list/respond/bootstrap operations.
- Dashboard role-path regression coverage expanded: RBAC tests now verify importer and country-reviewer nav visibility under mixed request/report feature-gate toggles.
- Dashboard middleware gated-route diagnostics improved: redirects now include explicit gate markers (`request_campaigns` / `annual_reporting`) with regression tests covering both deferred route families.
- Dashboard gated-entry analytics instrumentation delivered: overview landing now parses deferred-route redirect markers and emits a session-deduped audit analytics event for gated-entry attempts, backed by helper unit tests.
- Dashboard gated-entry telemetry endpoint delivered: new `/api/analytics/gated-entry` route validates telemetry payloads and records dedicated `DASHBOARD_GATED_ENTRY_ATTEMPT` events; dashboard landing now posts to this endpoint with session dedupe and has route/page regression coverage.
- Dashboard telemetry persistence proxy delivered: `/api/analytics/gated-entry` now forwards validated telemetry to backend `/v1/audit` with Authorization pass-through and route/page tests covering backend proxy + session token propagation.
- Dashboard telemetry read proxy delivered: `GET /api/analytics/gated-entry` now forwards tenant-scoped analytics reads to backend `/v1/audit/gated-entry` with fail-closed behavior when backend URL is unset and auth pass-through coverage in route tests.
- Dashboard admin diagnostics panel delivered: admin UI now consumes `/api/analytics/gated-entry` via `useGatedEntryEvents` hook and renders tenant-scoped deferred-route gate attempts with loading/error/empty and refresh behavior.
- Dashboard admin diagnostics UX hardening delivered: telemetry card now supports gate/time-window filters, pagination controls, and auth-token health hinting for backend-read failures.
- Telemetry server-side query controls delivered: backend `GET /v1/audit/gated-entry` and dashboard proxy now support validated `gate`/`fromHours`/`limit`/`offset` parameters with paginated envelope responses consumed by admin diagnostics.
- Telemetry sort-order hardening delivered: backend `GET /v1/audit/gated-entry` now validates `sort=desc|asc`, dashboard analytics proxy/hook forwards sort params, and admin diagnostics exposes newest/oldest ordering with server-side pagination.
- Telemetry diagnostics operator tooling delivered: admin telemetry card now includes saved presets (latest/weekly/campaign/reporting) and CSV export for currently loaded rows using a tested shared diagnostics helper.
- Telemetry full-range export delivered: admin telemetry card now supports `Export All CSV`, traversing paginated tenant-scoped telemetry reads with current gate/time/sort filters and exporting a complete filtered dataset.
- Telemetry server-generated export delivered: backend `GET /v1/audit/gated-entry/export` now returns tenant-scoped CSV under validated gate/time/sort filters (with row cap), dashboard analytics proxy supports `format=csv`, and admin full-range export now uses backend-generated CSV directly.
- Telemetry export cap observability delivered: CSV export responses now include row-count/limit/truncated headers end-to-end and admin export flow surfaces truncation warnings when cap-limited results are returned.
- Telemetry export traceability delivered: backend now emits `dashboard_gated_entry_exported` audit events with tenant/filter/row-count/truncation metadata whenever CSV export is executed.
- Telemetry export-activity diagnostics delivered: backend now exposes paginated tenant-scoped `dashboard_gated_entry_exported` reads, dashboard proxy supports `eventKind=exports`, and admin diagnostics surfaces recent export activity rows for operator visibility.
- Telemetry export actor attribution delivered: export audit events now carry `exportedBy` identity metadata and admin export-activity rows now display actor context for each CSV export.
- Telemetry export actor fallback hardening delivered: export events now fallback to `user:<id>` identity when email is unavailable, and admin actor display now falls back to `user_id` before `unknown`.
- Telemetry export actor directory enrichment delivered: admin export-activity actor column now resolves known user IDs to `name (email)` using loaded tenant user records, reducing opaque fallback identifiers.
- Telemetry actor enrichment decoupling delivered: export-activity hook now attaches pre-resolved `actorLabel` values from directory fetch, reducing reliance on page-local user snapshot state.
- Telemetry actor-directory cache delivered: export-activity hook now caches directory lookups with TTL and invalidates on manual reload, reducing repeated user-list fetch churn during diagnostics refresh loops.
- Telemetry actor cache invalidation delivered: export-activity hook now listens to admin-data mutations and refreshes actor labels immediately after user/admin updates.
- Telemetry mutation reload coalescing delivered: export hook now debounces mutation-triggered refreshes, reducing redundant fetch churn during rapid admin update bursts.
- Telemetry debounce debug observability delivered: export hook now supports opt-in session-scoped debug counters/logs for mutation/debounce/fetch behavior analysis.
- Telemetry debug UX delivered: admin diagnostics now includes a `Debug: On/Off` session toggle to control export-hook diagnostics without console commands.
- Telemetry debug statusline delivered: admin diagnostics now shows live debug counters for export-hook mutation/debounce/fetch activity when debug mode is enabled.
- Telemetry backend-authoritative actor resolution delivered: backend now exposes tenant-scoped actor lookup for export events, dashboard proxy supports `eventKind=actors`, and export hook now prioritizes backend actor labels before frontend fallback maps.
- Telemetry debug statusline event-delivery delivered: admin debug counters now update via hook subscription events instead of interval polling.
- Telemetry actor lookup integration hardening delivered: DB-backed audit integration now validates tenant-scoped `/v1/audit/gated-entry/actors` behavior with cross-tenant fallback safety.
- Telemetry canonical fallback enrichment delivered: actor resolver now uses `user_account.name` as backend canonical fallback label when `exportedBy` is absent, with integration coverage updated for shared-schema FK-safe fixture seeding.
- Telemetry actor lookup input hardening delivered: actor resolver now validates and deduplicates requested `ids` and rejects malformed UUID inputs with explicit `400` errors before DB execution.
- Telemetry actor contract coverage delivered: backend now has explicit missing-ids rejection test, dashboard proxy now verifies invalid-ids `400` pass-through, and feature docs include concrete valid/invalid lookup examples.
- Telemetry actor OpenAPI contract delivered: draft OpenAPI now includes `/v1/audit/gated-entry/actors` path, response schema, and invalid/missing-ids `400` examples.
- Telemetry OpenAPI section completed: draft now also documents `/v1/audit/gated-entry`, `/v1/audit/gated-entry/export`, and `/v1/audit/gated-entry/exports` plus shared list response schema and CSV metadata headers.
- Telemetry index artifact + assertion delivered: SQL file `tb_v16_004_audit_gated_entry_index.sql` now defines composite audit_log lookup index for gated-entry reads, and audit DB integration suite asserts expected index signature.
- Telemetry index rollout automation delivered: backend command `npm run db:apply:audit-index` now applies + verifies `idx_audit_log_gated_entry_tenant_gate_ts`, with local execution evidence recorded in release QA notes.
- Telemetry rollout verify-mode delivered: `db:verify:audit-index` now supports explicit verification-only runs, and index apply script now accepts `AUDIT_INDEX_DATABASE_URL` / `DATABASE_URL` for staging/prod-ready execution.
- Backend gated-entry telemetry query endpoint delivered: `GET /v1/audit/gated-entry` now returns tenant-scoped `dashboard_gated_entry_attempt` audit events and is covered by audit controller unit tests.
- Backend gated-entry telemetry integration coverage delivered: new DB-backed spec `src/audit/audit.gated-entry.int.spec.ts` validates tenant-claim denial and tenant-filtered event listing; required ownership lane now includes this audit telemetry suite.
- Backend inbox controller policy assertions expanded: tests now cover respond-path missing-tenant denial message, bootstrap non-exporter denial message, and exporter allow-path bootstrap execution.
- Backend controller policy regressions fixed: `AuditController` now enforces signed tenant claim on create/list, `HarvestController.create` again enforces farmer ownership scope, and backend unit test suite is green.
- Inbox DB-backed controller integration coverage delivered: new env-gated `inbox.controller.int.spec.ts` validates tenant-claim deny/allow and exporter bootstrap policy over real Postgres-backed inbox state.
- Ownership CI lane expanded: `test:integration:ownership` now includes `src/inbox/inbox.controller.int.spec.ts`, making inbox tenant-claim/role policy integration part of the required non-skipped CI evidence set.
- Controller-scope integration hardening delivered: schema reset on setup (`DROP SCHEMA ... CASCADE`) now prevents stale FK metadata from causing intermittent ownership controller integration failures.
- Backend integration runner automation delivered: `test:integration` and `test:integration:ownership` now auto-load `TEST_DATABASE_URL` from repo-root `.env.local` when missing in shell via `scripts/run-with-root-test-db.mjs`.
- Ownership/access QA evidence refreshed to 4-suite CI baseline: run `24407562162` and artifact `6431368794` now recorded with non-skipped `4/4 suites` and `10/10 tests` snapshot.
- Ownership/access QA evidence refreshed to expanded 5-suite CI baseline: run `24445875783` and artifact `6446796613` now recorded with non-skipped `5/5 suites` and `13/13 tests` including audit gated-entry integration.
- Inbox integration stability hardening delivered: `InboxService` now serializes schema verification and retries once on transient undefined-table states (`42P01`) in seed/bootstrap paths.
- Offline-product CI lint hardening delivered: remaining app lint blockers were resolved (`index`, `documents`, `modal`, `plot/[id]`), restoring green Expo lint lane.
- Supabase advisor-driven tenant-isolation baseline delivered: migration `tracebud-backend/sql/tb_v16_005_rls_baseline.sql` was added and applied to enable RLS + own-data policies for `user_account`, `farmer_profile`, `plot`, `harvest_transaction`, `voucher`, `dds_package`, `dds_package_voucher`, and `audit_log`.
- Supabase advisor phase-2 hardening delivered: migration `tracebud-backend/sql/tb_v16_006_rls_phase2_and_function_hardening.sql` was added and applied to enable RLS across remaining app-domain public tables and set explicit `search_path` on flagged public functions, eliminating app-table `rls_disabled_in_public` and mutable-function warnings.
- Supabase lead-intake policy hardening delivered: migration `tracebud-backend/sql/tb_v16_008_leads_insert_policy_hardening.sql` replaced permissive public lead insert policy (`WITH CHECK (true)`) with guarded validation predicates, clearing the permissive-RLS advisor warning while preserving public form submission.
- Supabase owner-remediation runbook delivered: `tracebud-backend/sql/tb_v16_009_postgis_owner_remediation_runbook.sql` now documents privileged PostGIS schema + `spatial_ref_sys` RLS steps needed to clear final extension-owned advisor findings.
- Supabase advisor exception policy documented: `public.spatial_ref_sys` is now explicitly recorded as accepted low-risk PostGIS metadata exception for current release scope, with mitigation and follow-up controls logged in release QA evidence.
- Telemetry OpenAPI example/schema hardening delivered: telemetry list/export-activity endpoints now include concrete `200` examples, and list items are typed via `GatedEntryTelemetryEvent` for stronger client contract onboarding.
- Telemetry contract discriminator hardening delivered: `GatedEntryTelemetryEvent` now uses explicit `oneOf` event variants (`attempt` vs `exported`) with typed payload schemas, and backend unit coverage now asserts required export-audit payload fields to guard docs/runtime drift.
- OpenAPI CI lint gate delivered: root CI now runs dedicated contracts job with `npm run openapi:lint` (`@redocly/cli`) and OpenAPI draft schema conditionals were made OAS 3.0-compatible to keep the gate passing.
- OpenAPI policy + ID baseline delivered: repo now includes `.redocly.yaml` and explicit `operationId` coverage for telemetry/audit export endpoints, with lint script pinned to config for deterministic CI contract checks.
- OpenAPI requests-cluster debt reduction delivered: `/v1/requests/campaigns*` operations now include explicit operation IDs and baseline `4xx` responses, reducing contract-lint warning count (`109` -> `103`) in the progressive hardening lane.
- OpenAPI plots-cluster debt reduction delivered: `/v1/plots` and `/v1/plots/{id}/geometry-versions` now include explicit operation IDs and baseline `4xx` responses, further reducing contract-lint warning count (`103` -> `99`).
- OpenAPI evidence-cluster debt reduction delivered: `/v1/evidence-documents*` operations now include explicit operation IDs and baseline `4xx` responses, further reducing contract-lint warning count (`99` -> `95`).
- OpenAPI consent + compliance-exports debt reduction delivered: `/v1/consent-grants*` and `/v1/compliance-exports*` now include explicit operation IDs and baseline `4xx` responses, reducing contract-lint warning count further (`95` -> `85`).
- OpenAPI batches-cluster debt reduction delivered: `/v1/batches*` endpoints now include explicit operation IDs and baseline `4xx` responses, reducing contract-lint warning count further (`85` -> `79`).
- OpenAPI reviews-cluster debt reduction delivered: yield-exception and dedup-review endpoints now include explicit operation IDs and baseline `4xx` responses, reducing contract-lint warning count further (`79` -> `69`).
- OpenAPI shipments-cluster debt reduction delivered: `/v1/shipment-headers*` endpoints now include explicit operation IDs and baseline `4xx` responses (while retaining `409` seal conflict contract), reducing contract-lint warning count further (`69` -> `64`).
- OpenAPI DDS-cluster debt reduction delivered: `/v1/dds-records/*` endpoints now include explicit operation IDs and baseline `4xx` responses while retaining `409`/`422` submit semantics, reducing contract-lint warning count further (`64` -> `59`).
- OpenAPI sync-cluster debt reduction delivered: `/v1/sync/flush` and `/v1/sync-conflicts/{id}/resolve` now include explicit operation IDs and baseline `4xx` responses, reducing contract-lint warning count further (`59` -> `55`).
- OpenAPI portability-cluster debt reduction delivered: `/v1/portability-requests*` endpoints now include explicit operation IDs and baseline `4xx` responses, reducing contract-lint warning count further (`55` -> `51`).
- OpenAPI annual-reporting-cluster debt reduction delivered: `/v1/annual-reporting-snapshots*` endpoints now include explicit operation IDs and baseline `4xx` responses, reducing contract-lint warning count further (`51` -> `47`).
- OpenAPI compliance-issues-cluster debt reduction delivered: `/v1/compliance-issues*` endpoints now include explicit operation IDs and baseline `4xx` responses, reducing contract-lint warning count further (`47` -> `37`).
- OpenAPI billing + webhooks debt reduction delivered: `/v1/billing*` and `/v1/webhooks*` endpoints now include explicit operation IDs and baseline `4xx` responses, reducing contract-lint warning count further (`37` -> `27`).
- OpenAPI operation-gate enforcement delivered: `.redocly.yaml` now enforces operation ID + 4xx rules as errors; residual strict violations were fixed (organisations/producers/member-invites + audit/access exports), and lint now passes under strict mode with warning count reduced further (`27` -> `21`).
- OpenAPI metadata warning closure delivered: added license metadata, replaced placeholder server URLs, and documented all tag descriptions; strict OpenAPI lint now passes with zero warnings.
- OpenAPI CI trend-note visibility delivered: contracts workflow now captures Redocly output and publishes OpenAPI lint status + warning count in GitHub job summary for per-run governance tracking.
- OpenAPI baseline-delta CI metrics delivered: contracts workflow now compares errors/warnings to repo baseline (`docs/openapi/lint-baseline.json`) and uploads `contracts-openapi-lint-metrics` artifact with per-run governance metrics.
- OpenAPI baseline refresh guard delivered: `openapi:baseline:refresh` now requires explicit approval + reason env flags, re-runs strict lint, and records baseline refresh metadata to reduce accidental governance-baseline drift.
- OpenAPI manual-refresh workflow delivered: `openapi-baseline-refresh.yml` adds a `workflow_dispatch` lane that requires explicit approval input + reason, runs guarded baseline refresh in CI, and uploads refreshed baseline artifact for controlled adoption.
- OpenAPI baseline PR automation delivered: manual refresh workflow now auto-opens a review PR for `docs/openapi/lint-baseline.json` updates (with dispatch metadata), keeping baseline adoption protected-branch reviewed.
- OpenAPI baseline ownership enforcement delivered: `.github/CODEOWNERS` now requires designated reviewer approval for `docs/openapi/lint-baseline.json` and baseline refresh workflow changes.
- OpenAPI governance ownership parity delivered: `.github/CODEOWNERS` now also protects `.redocly.yaml` and `docs/openapi/tracebud-v1-draft.yaml`, extending designated-review enforcement to contract policy and spec edits.
- OpenAPI branch-protection verification guidance delivered: release QA evidence now includes a checklist tying CODEOWNERS-protected paths to required code-owner review settings and PR-based enforcement proof.
- OpenAPI branch-protection evidence template delivered: release QA evidence now includes a fill-in “Completed evidence” subsection (PR URL, reviewer/timestamps, mergeability outcomes) for fast, standardized governance signoff capture.
- OpenAPI governance drift-check delivered: contracts CI now runs `openapi:governance:check` to assert required OpenAPI CODEOWNERS protections remain present and fail fast on accidental removal.
- OpenAPI governance owner-pin check delivered: `openapi:governance:check` now enforces expected CODEOWNERS owner handles for protected OpenAPI paths, preventing reviewer-route drift from passing CI.
- OpenAPI governance policy externalization delivered: CODEOWNERS path/owner requirements now live in `docs/openapi/governance-codeowners-policy.json`, consumed by CI governance checks to avoid dual-edit drift during ownership updates.
- OpenAPI governance policy schema gate delivered: contracts CI now runs `openapi:governance:policy:validate` using `governance-codeowners-policy.schema.json` before CODEOWNERS checks to fail fast on malformed policy edits.
- OpenAPI governance reporting consolidation delivered: contracts CI now publishes unified governance summary output and uploads `contracts-openapi-governance-metrics` artifact for combined policy/CODEOWNERS evidence tracking.
- OpenAPI governance summary hardening delivered: governance scripts now emit report JSON (`--report`) and CI summary status is derived from those reports instead of log-string parsing.
- OpenAPI governance raw-report evidence delivered: contracts CI now uploads `contracts-openapi-governance-reports` with policy/codeowners report JSON files that underpin governance summary outcomes.
- OpenAPI governance summary link-hints delivered: governance summary now includes direct run-link references beside governance artifact names for faster reviewer navigation.
- OpenAPI governance artifact inventory metadata delivered: governance metrics JSON now includes expected artifact names/file lists to support machine-checkable evidence completeness.
- OpenAPI governance artifact inventory assertion delivered: contracts CI now fails fast if expected governance evidence files are missing before artifact upload.
- OpenAPI governance artifact JSON-shape assertion delivered: contracts CI now validates governance metrics/report JSON structure and cross-file status consistency before artifact upload.
- OpenAPI governance assertion script extraction delivered: inventory + JSON-shape checks are now centralized in `openapi:governance:artifacts:assert`, reducing inline CI workflow complexity and drift risk.
- OpenAPI governance script namespace cleanup delivered: governance scripts are now organized under `scripts/openapi-governance/` with npm/CI references updated to the new canonical paths.
- OpenAPI governance runbook docs delivered: `scripts/openapi-governance/README.md` now provides command purpose, report flags, CI order, and source-of-truth file references.
- OpenAPI governance README command gate delivered: contracts CI now runs `openapi:governance:readme:check` to ensure README command references stay synchronized with `package.json`.
- OpenAPI governance README path gate delivered: README CI check now also validates referenced file paths exist, preventing stale documentation links.
- OpenAPI governance markdown QA utility delivered: shared `markdown-reference-check` module now centralizes command/path reference validation logic for reusable documentation governance checks.
- OpenAPI governance markdown QA CLI delivered: `openapi:governance:markdown:check` now supports argument-driven checks (`--markdown`, `--package-json`, optional command/path toggles) so new docs can reuse the same governance guardrail with no new wrapper logic.
- OpenAPI governance markdown QA CI reuse delivered: contracts lane now runs generic checker against `product-os/04-quality/release-qa-evidence.md` with path-only mode (`--no-commands`) to prove multi-doc production applicability.
- OpenAPI governance markdown-reference metrics delivered: checker now emits JSON reports via `--report`, and contracts lane publishes `contracts-markdown-reference-metrics` with summary status/counts for doc-governance trend tracking.
- OpenAPI governance markdown report schema assertion delivered: contracts lane now runs `openapi:governance:markdown:report:assert` to validate markdown report structure and status/error invariants before artifact upload.
- OpenAPI governance markdown report versioned schema delivered: `docs/openapi/markdown-reference-report.schema.json` now defines v1 contract and report payloads include `schemaVersion: 1` with compatibility checks in assertion script.
- OpenAPI governance markdown summary version visibility delivered: CI now prints `Schema version` in `Markdown Reference Checks` summary for faster reviewer verification.
- OpenAPI governance schema-bump runbook gate delivered: README check now enforces `Markdown Schema Bump Procedure` heading and required 5-step structure to catch procedural-doc regressions early.
- OpenAPI governance README structure policy delivered: required runbook anchors are now defined in `docs/openapi/governance-readme-policy.json` with schema-backed validation in README check.
- OpenAPI governance README metrics artifact delivered: README check now supports report output and contracts lane uploads `contracts-openapi-readme-governance-metrics` for artifact-level auditability.
- OpenAPI governance README report schema assertion delivered: contracts lane now runs `openapi:governance:readme:report:assert` against `docs/openapi/governance-readme-report.schema.json` before README metrics artifact upload.
- OpenAPI governance metrics unification delivered: `openapi-governance-metrics.json` now embeds README validation/report status and expected-artifact inventory includes README metrics artifact; artifacts assertion enforces this parity.
- OpenAPI governance metrics generator delivered: CI now uses `scripts/openapi-governance/metrics-generate.mjs` (`openapi:governance:metrics:generate`) for consolidated summary + metrics creation instead of inline workflow JS.
- OpenAPI governance metrics smoke test delivered: `scripts/openapi-governance/metrics-generate.smoke.mjs` now validates fixture-based PASS/FAIL folding and artifact-inventory shape, and CI runs it before artifacts assertion.
- OpenAPI governance summary-line smoke assertions delivered: metrics smoke suite now verifies generated README/policy/CODEOWNERS/overall status lines to catch summary-text regressions.
- OpenAPI governance summary template deduplication delivered: shared `scripts/openapi-governance/metrics-summary-lines.mjs` now centralizes summary line templates consumed by both metrics generator and smoke checks.
- OpenAPI governance smoke strictness balancing delivered: smoke suite now keeps strict structured metrics checks while using ordered summary-line assertions to avoid benign formatting churn failures.
- OpenAPI governance presentation warning lane delivered: contracts workflow now runs non-blocking `metrics-presentation-check` to flag summary-format drift without failing blocking governance gates.
- OpenAPI governance presentation warning UX delivered: contracts summary now auto-appends `Governance Presentation Drift Warning` guidance when non-blocking presentation checks fail.
- OpenAPI governance presentation telemetry metrics delivered: governance metrics JSON now includes per-run `presentationValidationStatus`, `presentationDriftWarningCount`, and `presentationValidationRecordedAt` for artifact-level drift trend analysis.
- OpenAPI governance drift trend helper delivered: `scripts/openapi-governance/metrics-trend-summary.mjs` (`openapi:governance:metrics:trend`) now aggregates presentation statuses and warning totals across metrics artifacts.
- OpenAPI governance trend publishing delivered: contracts lane now appends `Governance Presentation Drift Trend` summary output and uploads `contracts-openapi-governance-presentation-trend` artifact each run.
- OpenAPI governance trend delta lane delivered: contracts lane now attempts prior trend artifact retrieval and publishes FAIL/drift-warning deltas versus previous run when artifact history is available.
- OpenAPI governance delta-baseline hardening delivered: previous trend lookup now prefers same-branch artifacts and skips non-success source runs before delta calculations.
- OpenAPI governance workflow-source filtering delivered: previous trend lookup now requires matching workflow path (`GITHUB_WORKFLOW_REF`) with workflow-name fallback, reducing cross-workflow baseline contamination.
- OpenAPI governance delta baseline traceability delivered: prior-run metadata (`runId`, `workflowPath`, `workflowName`) now flows into delta JSON and summary output.
- OpenAPI governance trend/delta schema assertions delivered: CI now validates both presentation trend artifacts against explicit schemas and uploads paired trend + trend-delta artifacts.
- OpenAPI governance assertion smoke coverage delivered: fixture-based smoke script now exercises trend/delta assert scripts across valid and invalid scenarios in CI.
- FEAT-001 (multi-tenant admin) closure delivered: feature checklist is now fully checked, open question closed for FEAT-001 scope, and feature status moved to Done with linked quality/governance evidence.
- FEAT-003 execution bootstrap delivered: first geospatial mapping slice now has explicit v1.6 gate matrix covering permissions, canonical states, exception recovery, analytics events, and acceptance mapping.
- FEAT-003 S1 first implementation delivered: plots create endpoint now enforces tenant-claim fail-closed behavior and standardized `GEO-101`/`GEO-102` coded geometry normalization failures with unit test coverage.
- FEAT-003 S1 second implementation delivered: `PATCH /v1/plots/:id/geometry` now supports geometry revisions with farmer-scope enforcement, immutable supersession audit events, and canonical `GEO-101`/`GEO-102` failure signaling.
- FEAT-003 S1 read-surface extension delivered: `GET /v1/plots/:id/geometry-history` now exposes immutable geometry audit-chain events (`plot_created`, `plot_geometry_superseded`) with tenant/scope guardrails and OpenAPI contract coverage.
- FEAT-003 S1 response-contract hardening delivered: geometry history now returns a typed `PlotGeometryHistoryEvent` shape (backend DTO + OpenAPI schema), with controller tests covering scope-violation denial and positive-path forwarding.
- FEAT-003 S1 mapping-regression hardening delivered: plots service now has targeted tests guaranteeing geometry-history timestamp normalization, unknown-event coercion fallback, and payload/plotId fallback semantics.
- FEAT-003 S1 integration-proof hardening delivered: DB-backed controller-scope integration now validates geometry-history tenant-claim denial and farmer ownership allow/deny with persisted audit events.
- FEAT-003 S1 contract-clarity hardening delivered: OpenAPI `GET /v1/plots/{id}/geometry-history` now publishes concrete `200` examples for both `plot_created` and `plot_geometry_superseded` event shapes.
- FEAT-003 S1 serialization-guard hardening delivered: controller unit tests now assert geometry-history response uses stable camelCase envelope fields and does not leak snake_case database-style keys.
- FEAT-003 S1 dashboard-proxy hardening delivered: `/api/plots/[id]/geometry-history` now fail-closes when backend URL is missing, forwards auth to backend `GET /v1/plots/:id/geometry-history`, and preserves camelCase response envelope via route-level tests.
- FEAT-003 S1 frontend-usability hardening delivered: dashboard plot detail now renders a geometry-history panel backed by `usePlotGeometryHistory` with explicit loading/error/empty/data states and component-level regression tests.
- FEAT-003 S1 panel-scalability hardening delivered: geometry-history panel now supports event-type filtering and shows API result-cap context (`max 100`) with updated UI tests.
- FEAT-003 S1 server-pagination hardening delivered: backend `GET /v1/plots/:id/geometry-history` now supports `limit`/`offset` and returns `{items,total,limit,offset}`; dashboard proxy/hook/panel now consume this contract with page controls and updated OpenAPI examples/tests.
- FEAT-003 S1 chronology-control hardening delivered: backend/proxy/hook/panel now support geometry-history `sort` direction (`desc|asc`) with OpenAPI contract updates and route/UI regression tests.
- FEAT-003 S1 integration-ordering hardening delivered: ownership integration lane now includes DB-backed assertion that persisted geometry-history events return correct order for both `sort=desc` and `sort=asc`.
- FEAT-003 S1 sort-pagination UX hardening delivered: geometry-history panel now resets to page 1 when switching between `Newest` and `Oldest`, with component-level regression test assertions.
- FEAT-003 S1 hook-query hardening delivered: `usePlotGeometryHistory` now has dedicated hook-level test coverage proving correct `limit`/`offset`/`sort` query construction and auth-header forwarding across pagination/sort state changes.
- FEAT-003 S1 page-flow hardening delivered: plot detail route now has integration-style test coverage proving user interaction sequence (`Oldest` then `Next`) triggers the expected API query progression.
- FEAT-003 S1 visual-regression hardening delivered: geometry-history panel test suite now includes lightweight snapshots for loading/empty/error rendering states to catch unintended UI drift.
- FEAT-003 S1 snapshot-review governance delivered: feature doc now includes a concise reviewer checklist that distinguishes expected snapshot changes from suspicious regressions and standardizes local verification steps.
- Snapshot governance template delivered at quality layer: `release-qa-evidence.md` now includes reusable snapshot-diff review checklist and FEAT-003 example commands to standardize PR review across future snapshot-backed slices.
- FEAT-003 S1 snapshot-doc dedup delivered: FEAT-003 now references shared quality template and keeps feature-local command pointers only, reducing duplicated checklist maintenance.
- Snapshot governance registry delivered: release QA evidence now tracks snapshot-backed features and their required snapshot/behavior commands in a single table, seeded with FEAT-003.
- Snapshot governance registry-operability delivered: release QA evidence now includes a lightweight procedure for adding/updating registry rows and required status-log updates.
- Snapshot governance CI enforcement delivered: new contracts check (`openapi:governance:snapshot:registry:check`) now validates snapshot-registry rows for required command/template fields in CI.
- Snapshot governance trend telemetry delivered: snapshot-registry check now emits `openapi-snapshot-registry-report.json` (`schemaVersion: 1`), contracts CI asserts schema/status invariants, publishes summary counters, and uploads artifact `contracts-openapi-snapshot-registry-metrics`.
- Governance trend unification delivered: `metrics-trend-summary` and `metrics-trend-delta` now include snapshot-registry PASS/FAIL and row-volume telemetry (plus deltas), and CI summary/assert/smoke checks now enforce these multi-signal trend contracts.
- FEAT-003 S2 operational UX delivered: geometry-history panel now persists per-user investigation presets, highlights anomaly flags for large revision jumps and rapid supersessions, groups timeline rows by day for analyst scanability, and adds tighter hook/component regression coverage with refreshed snapshots.
- FEAT-003 S2 anomaly contract delivered: backend `GET /v1/plots/:id/geometry-history` now returns typed `anomalies[]` triage metadata and dashboard consumers now prefer server-provided anomaly signals (with fallback), keeping investigator flags consistent across surfaces.
- FEAT-003 S2 anomaly sensitivity delivered: geometry-history now accepts `anomalyProfile` (`strict`/`balanced`/`lenient`) with profile-aware backend thresholds, proxy forwarding, persisted hook state, analyst-facing panel controls, and refreshed regression/snapshot coverage.
- FEAT-003 S2 investigation ergonomics delivered: geometry-history panel now includes `Signals only` filtering for anomaly-focused triage and resets to page 1 when sensitivity profile changes, reducing stale-pagination confusion.
- FEAT-003 S2 anomaly-only pagination delivered: geometry-history now supports `signalsOnly=true` end-to-end (backend, proxy, hook persistence, UI), enabling exhaustive paging through flagged revisions across long histories.
- FEAT-003 S2 anomaly-summary delivered: geometry-history now returns `anomalySummary` severity/type counters and dashboard panel surfaces this condensed signal mix (`high/medium`, `jump/frequent`) for faster investigation triage.
- FEAT-003 S2 anomaly-summary-scope delivered: geometry-history now returns `anomalySummaryScope` (`current_page` or `full_filtered_set`) and panel displays scope labels to prevent misreading summary counters during investigation handoffs.
- FEAT-003 S2 signals-only reset delivered: geometry-history panel now resets to page 1 when toggling `Signals only`, keeping anomaly-only and mixed timeline transitions pagination-safe.
- FEAT-003 S2 mode-memory delivered: geometry-history panel now remembers last page per mode (`mixed` vs `signalsOnly`) and restores prior position when investigators toggle views.
- FEAT-003 S2 mode-memory persistence delivered: geometry-history presets now store mode-specific page bookmarks per user+plot with positive-integer sanitization so mixed/signals-only context survives reloads safely.
- FEAT-003 S2 mode-memory bounds delivered: geometry-history panel now clamps saved/restored mode bookmarks against current max-page limits so toggles remain pagination-valid when totals shrink.
- FEAT-003 S2 mode-memory clamp centralization delivered: canonical page clamping now lives in `usePlotGeometryHistory` (`clampPage`) and panel toggle flows consume that shared helper to avoid duplicated bounds logic.
- FEAT-003 S2 bounded pagination setter delivered: `usePlotGeometryHistory.setPage` now enforces min/max page bounds (using current totals when available), so all panel pagination actions remain offset-valid.
- FEAT-003 S2 filter pagination reset delivered: event-type filter toggles now reset geometry-history panel to page 1, keeping pagination context valid when narrowing or widening timeline scope.
- FEAT-003 S2 filter page-memory delivered: geometry-history presets now persist per-filter page bookmarks and panel filter toggles restore remembered page positions for `all`/`created`/`revised` views.
- FEAT-003 S2 hybrid-view page-memory delivered: geometry-history now persists combined view bookmarks (`filter + mixed/signals`) and restores them during filter/signal toggles for tighter operator continuity.
- FEAT-003 S2 hybrid-memory cleanup delivered: panel toggles now consume only combined view-key memory while hook initialization migrates legacy mode/filter bookmarks into hybrid keys for backward-compatible continuity.
- FEAT-003 S2 legacy-memory retirement delivered: deprecated mode/filter memory fields were removed from the active hook contract and test mocks, formalizing hybrid view memory as the only supported bookmark API.
- FEAT-003 S2 migration observability delivered: hook legacy-fallback parse path now increments local counter key `tb:geometry-history:legacy-view-fallback-count` for evidence-based deprecation follow-up.
- FEAT-003 S2 migration visibility delivered: panel now renders `Legacy preset fallback migrations: <count>` when non-zero, using hook-exposed `legacyViewFallbackCount` for operator-visible migration diagnostics.
- FEAT-003 S2 migration counter reset delivered: hook now exposes `resetLegacyViewFallbackCount` and panel adds `Reset migration counter` control to clear local migration telemetry baseline.
- FEAT-003 S2 migration reset safety delivered: panel now stages reset behind `Confirm reset` / `Cancel` controls, reducing accidental counter-clearing during diagnostics review.
- FEAT-003 S2 migration reset high-signal safety delivered: panel now requires typed `RESET` confirmation for elevated fallback counts before allowing telemetry reset.
- FEAT-003 post-closeout run-action UX hardening delivered: deforestation decision panel now surfaces a last-run metadata chip (cutoff/verdict/provider mode) and exposes a retry action after failed runs for faster operator recovery.
- P0-02 dispatch operability hardening delivered: final send packet now enforces preflight checks, ships explicit source-pack manifest references, and includes a copy-ready post-send status snippet with channel/thread metadata capture.
- P0-02 follow-up cadence hardening delivered: standardized D+3, D+6, and escalation follow-up templates now exist with tracker action-label conventions for consistent legal-gate SLA execution.
- P0-02 one-pass update hardening delivered: dispatch metadata can now be filled once and pasted across tracker + status logs via `p0-02-dispatch-one-pass-update-block.md`, reducing post-send update drift.
- P0-02 memo-receipt closeout hardening delivered: signed-memo receipt metadata can now be filled once and pasted across tracker/intake/scaffold/status logs via `p0-02-memo-received-one-pass-closeout-block.md`.
- P0-02 operator runbook hardening delivered: a single phased runbook now governs dispatch, SLA follow-ups, memo-receipt validation, and closeout branching for deterministic legal-gate execution.
- P0-03 pilot gate bootstrap delivered: kickoff checklist, evidence log, and readiness index are now in place to run the required 3-month pilot with explicit acceptance evidence tracking.
- P0-03 execution ergonomics hardening delivered: kickoff metadata can now be filled once and pasted into pilot artifacts/status logs, and a phased operator runbook now governs weekly cadence, escalation conditions, and closeout checks.
- P0-03 weekly checkpoint standardization delivered: a 12-week template pack now defines consistent cadence rows, gate-progress scoreboard, and midpoint escalation thresholds for pilot evidence quality.
- P0-03 midpoint decision governance delivered: week-6 review now uses a standardized packet with explicit `continue/recover/escalate` paths and required corrective/escalation ownership fields.
- P0-03 closeout one-pass hardening delivered: final pilot evidence, execution-board closure note, and status-log updates can now be populated from a single closeout block to reduce end-of-pilot drift.
- FEAT-003 closeout delivered: feature checklist is fully checked, open question closed for feature scope, and feature status updated to Done (`TB-V16-002 / FEAT-003`).
- FEAT-004 S1 execution-matrix bootstrap delivered: rules-engine feature doc now defines explicit permissions/state transitions/exception handling/analytics/acceptance/v1.6 constraints baseline for implementation kickoff.
- FEAT-004 S1 deterministic readiness contract slice delivered: backend now provides tenant-safe/exporter-scoped DDS package readiness evaluation with explicit blocker/warning outputs and OpenAPI response schema coverage.
- FEAT-004 S1 readiness analytics lifecycle slice delivered: backend now emits `dds_package_readiness_*` audit events with status and blocker/warning counters for readiness diagnostics traceability.
- FEAT-004 S1 readiness audit integration slice delivered: DB-backed controller-scope integration now verifies readiness lifecycle event persistence and payload counters end-to-end on exporter-triggered checks.
- FEAT-004 closeout delivered: feature checklist and scope-level open question are now resolved in feature spec, and FEAT-004 status moved from `In progress` to `Done`.
- Tenant-hardening controller coverage delivered: `src/harvest/controller-scope.int.spec.ts` now verifies tenant-claim + exporter-role enforcement for package list/detail/TRACES export endpoints, with ownership lane green (`5 suites`, `18 tests`).
- Tenant-hardening package-submit parity delivered: `src/harvest/controller-scope.int.spec.ts` now also enforces tenant-claim + exporter-role policy on package submit path, with ownership lane green at `5 suites`, `19 tests`.
- Ownership/access evidence refresh delivered: `product-os/04-quality/release-qa-evidence.md` now includes latest local ownership-lane snapshot (`5 suites`, `19 tests`) and notes pending CI artifact baseline refresh.
- FEAT-002 S1 execution-matrix bootstrap delivered: mobile field capture feature doc now includes explicit permissions/state transitions/exception recovery/analytics coverage/acceptance mapping and v1.6 constraint applicability for implementation-ready kickoff.
- FEAT-002 S1 sync-envelope enforcement slice delivered: `plots` mobile sync metadata endpoints (`photos-sync`, `legal-sync`, `evidence-sync`) now fail closed on missing tenant claims, with DB-backed controller integration assertions and DTO-level malformed/valid HLC envelope validation coverage.
- FEAT-002 S1 sync-envelope API-proof slice delivered: new DB-backed Nest HTTP integration test (`src/plots/sync-envelope.api.int.spec.ts`) now validates missing-tenant denial (`403`), malformed HLC rejection (`400`), and valid envelope audit persistence (including `hlcTimestamp` + `clientEventId`) end-to-end; required ownership integration lane expanded to include this path.
- FEAT-002 S1 sync ownership-scope slice delivered: sync metadata endpoints now enforce farmer-owned plot scope while allowing agent role paths; controller tests, DB-backed controller-scope integration, and API-level integration now assert foreign-farmer denial (`403`) and valid owner/agent allow behavior.
- FEAT-002 S1 assignment-aware sync scope slice delivered: sync DTOs now carry optional `assignmentId`, and agent sync writes now require active assignment scope (`agent_user_id + plot_id`, optional assignmentId match) with fail-closed fallback when assignment relation is unavailable; ownership/access integration lane remains green (`6 suites`, `24 tests`).
- FEAT-002 S1 assignment contract-publication slice delivered: canonical migration `tb_v16_010_agent_plot_assignment_scope.sql` now defines assignment scope relation + indexes, and OpenAPI draft now documents `photos-sync`/`legal-sync`/`evidence-sync` endpoints with sync envelope + `assignmentId` request semantics (`openapi:lint` passing).
- FEAT-002 S1 assignment lifecycle slice delivered: assignment create/complete/cancel APIs now enforce tenant + role boundaries and canonical state transitions with typed `ASN-001..004` error codes, backed by service/controller tests, DB-backed controller integration, and OpenAPI path/schema updates.
- FEAT-002 S1 assignment-operator UX slice delivered: dashboard plot detail now includes assignment lifecycle controls with backend proxy routes and mapped ASN recovery guidance, with route/component test coverage for fail-closed behavior and success/error operator states.
- FEAT-002 S1 assignment-history UX slice delivered: backend `GET /v1/plots/{id}/assignments` + dashboard proxy history reads now drive plot detail assignment table rendering with click-to-fill ID selection, improving operator completion/cancellation flow ergonomics.
- FEAT-002 S1 assignment-history scaling slice delivered: backend assignment list now supports status/agent/fromDays filters and paginated responses, with dashboard panel controls and pagination navigation to keep assignment triage usable at higher record volumes.
- FEAT-002 S1 assignment-label + filter-consistency slice delivered: assignment list responses now include optional `agentName` enrichment from `user_account`, UI surfaces name+id labels, and additional unit/integration assertions confirm filtered totals remain consistent with returned paged rows.
- FEAT-002 S1 readability-polish slice delivered: assignment rows now include optional `agentEmail` enrichment and status badge rendering in dashboard panel, improving operator scanability without changing authorization or transition semantics.
- FEAT-002 S1 handoff-ergonomics slice delivered: assignment panel now ships a status legend with tooltip definitions and filtered CSV export to support downstream ops review without manual copy/paste.
- FEAT-002 S1 backend-export slice delivered: assignments endpoint now supports tenant-safe CSV export mode and dashboard proxy pass-through, reducing client-side export assembly and improving large filtered handoff reliability.
- FEAT-002 S1 export-telemetry slice delivered: assignment CSV export now emits `plot_assignment_export_requested/succeeded/failed` audit events with tenant/user/filter context and success row-count metadata for evidence traceability.
- FEAT-002 S1 export-diagnostics slice delivered: admin telemetry now includes assignment-export activity (`requested/succeeded/failed`) with actor, filter context, row counts, and error visibility from tenant-scoped backend audit reads.
- FEAT-002 S1 export-diagnostics filtering slice delivered: assignment export diagnostics now support phase/status server filters and filtered CSV export for focused operational/compliance handoff.
- FEAT-002 S1 export-diagnostics full-range slice delivered: assignment export telemetry now supports dedicated backend CSV export endpoint and admin `Export All CSV` for full filtered historical handoff.
- FEAT-002 S1 export-diagnostics pagination slice delivered: admin assignment export diagnostics now show total matching counts and page navigation controls, and OpenAPI now documents assignment-export list/export endpoint contracts.
- FEAT-002 S1 export-diagnostics contract-typing slice delivered: OpenAPI now uses dedicated assignment-export discriminator/event/payload schemas for `plot_assignment_export_*` list responses.
- FEAT-002 S1 export-diagnostics contract-parity slice delivered: backend service tests now assert assignment export audit payload field parity and explicit null normalization, reducing OpenAPI/runtime drift risk.
- FEAT-002 S1 export-diagnostics query-surface parity slice delivered: DB-backed audit integration now verifies tenant-scoped assignment-export list reads preserve expected `plot_assignment_export_*` payload contract keys end-to-end.
- FEAT-002 S1 export-diagnostics combined-filter parity slice delivered: DB-backed audit integration now locks `phase + status` filter behavior (`failed` + `active`) for tenant-scoped assignment-export diagnostics reads.
- FEAT-002 S1 export-diagnostics HTTP-pipeline parity slice delivered: API integration now verifies `/v1/audit/gated-entry/assignment-exports` enforces missing-tenant denial and preserves combined phase/status filter behavior end-to-end.
- FEAT-002 closeout delivered: feature checklist and scope-level open question are now resolved in feature spec, and FEAT-002 status moved from `In progress` to `Done`.
- FEAT-002 post-closeout CSV API-lane hardening delivered: assignment-export API integration now verifies `/v1/audit/gated-entry/assignment-exports/export` filtered CSV response and export metadata headers end-to-end.
- FEAT-002 post-closeout offline queue observability hardening delivered: settings now shows pending-sync retry health and latest queue error context, and pending queue processor now writes explicit local audit telemetry for queue success/failure/invalid-drop outcomes.
- FEAT-002 post-closeout selective retry hardening delivered: settings sync card now lets operators choose queue action types (`harvest`, `photos_sync`, `evidence_sync`) for targeted retry runs, and queue draining now honors the selected action scope.
- FEAT-002 post-closeout selective retry planning hardening delivered: settings queue filter chips now show per-action pending counts so operators can prioritize high-impact queue classes before retry.
- FEAT-002 post-closeout retry-sweep preset hardening delivered: settings now offers attempt-scope presets (`all`, `retrying only`, `first-attempt only`) and queue drain logic applies the selected scope before HLC-ordered processing.
- FEAT-002 post-closeout smart-sweep hardening delivered: settings now supports a two-pass queue sweep (`retrying` then `first-attempt`) with pass-level feedback so operators can clear unstable backlog classes more predictably in one run.
- FEAT-002 post-closeout smart-sweep guardrail hardening delivered: smart sweep now applies configurable per-run caps (`25/50/100/200`) and carries remaining budget from pass 1 to pass 2 to avoid long single-run retries on large queues.
- FEAT-002 post-closeout smart-retry preference persistence hardening delivered: settings now stores and re-hydrates queue attempt scope, smart-sweep toggle state, and cap selection between sessions for faster operator continuity.
- FEAT-002 post-closeout retry-preference reset hardening delivered: settings now includes one-tap reset that restores default retry profile and persisted keys (`all`, smart sweep off, cap `100`).
- FEAT-002 post-closeout reset-feedback hardening delivered: queue preference reset now publishes immediate operator confirmation in sync status, improving trust in local settings actions.
- FEAT-002 post-closeout reset-feedback i18n hardening delivered: reset confirmation now uses localized EN/ES language keys instead of hard-coded English copy.
- FEAT-002 post-closeout queue-panel i18n parity hardening delivered: retry filter/attempt scope summaries, smart-sweep pass labels, queue-health line, and queue control labels are now localized for EN/ES consistency.
- FEAT-002 post-closeout queue-i18n smoke hardening delivered: offline app now includes `i18n:queue:smoke` script to assert required queue-panel translation keys are present in both EN and ES maps.
- FEAT-002 post-closeout queue-i18n CI gate hardening delivered: Expo app CI lane now runs `npm run i18n:queue:smoke` after lint so queue localization parity is enforced automatically.
- FEAT-002 post-closeout queue-i18n CI summary hardening delivered: Expo app CI lane now appends `Offline App Queue i18n Smoke` summary block with command/status visibility for review ergonomics.
- FEAT-002 post-closeout queue-i18n smoke metadata hardening delivered: smoke script now supports `--summary-json` and CI summary now reports smoke version + required key count for fast localization-scope drift checks.
- FEAT-002 post-closeout queue-i18n summary artifact hardening delivered: Expo app CI lane now uploads `offline-app-queue-i18n-smoke-summary` artifact containing per-run queue i18n metadata JSON.
- FEAT-002 post-closeout queue-i18n previous-run delta hardening delivered: Expo app CI lane now attempts previous artifact retrieval and publishes previous-availability, key-count delta, and previous smoke-version context in summary.
- FEAT-002 post-closeout queue-i18n successful-baseline hardening delivered: previous-run delta selection now requires source workflow run `conclusion=success` to avoid noisy baseline comparisons.
- FEAT-002 post-closeout queue-i18n workflow-source baseline hardening delivered: previous-run delta selection now also requires matching workflow path (with workflow-name fallback), reducing cross-workflow baseline contamination.
- FEAT-002 post-closeout queue-i18n baseline-provenance hardening delivered: CI summary now includes selected baseline run id/workflow path/workflow name fields for explicit delta provenance.
- FEAT-002 post-closeout queue-i18n baseline-artifact packaging hardening delivered: uploaded queue i18n artifact now co-packages baseline metadata JSON with current summary JSON.
- FEAT-002 post-closeout queue-i18n artifact-contract hardening delivered: Expo app CI lane now runs `i18n:queue:summary:assert` to enforce summary/baseline JSON shape expectations before summary publishing.
- FEAT-002 post-closeout queue-i18n baseline-schema hardening delivered: baseline metadata now carries `schemaVersion`, CI summary surfaces it, and assertion gate enforces version field presence for baseline payload contracts.
- FEAT-002 post-closeout queue-i18n baseline-schema governance hardening delivered: dedicated baseline metadata schema file and standalone schema assertion command now enforce baseline payload contract in CI (`i18n:queue:baseline:schema:assert`).
- Supabase PostGIS owner-remediation hardening delivered: `tb_v16_009_postgis_owner_remediation_runbook.sql` now revokes broad `anon`/`authenticated` DML grants on `spatial_ref_sys`, enables RLS with explicit read policy, and resolves `spatial_ref_sys` path handling across `public`/`extensions` schemas for safer owner-run execution.
- P1 external decision-gate operational hardening delivered: one-pass decision-cycle block and readiness index now standardize evidence capture and cross-file status propagation for `P1-01..P1-05`.
- P1 external decision evidence hardening delivered: a dedicated evidence register now tracks per-gate source links, freshness windows, owner attribution, and explicit verification status before any gate is marked `decided`.
- P1 external decision runbook hardening delivered: a unified operator playbook now defines phase transitions from preflight through evidence verification, publication, and escalation for repeatable `P1` cycle execution.
- P1 pending-gate tracker hardening delivered: unresolved decision gates now have a dedicated board with status class, owner, blocker taxonomy, SLA due date, and escalation level for tighter follow-up control.
- P1 cycle-snapshot reporting hardening delivered: a reusable cycle template now consolidates gate status, risk posture, escalation rollup, and 3-line leadership summary for fast cross-cycle visibility.
- P1 decision-history ledger hardening delivered: append-only cycle rows now record status deltas, reopened gates, and blocker-theme trends to make recurrence visible across cycles.
- P1 weekly summary-template hardening delivered: standardized wording now composes weekly status/trend updates directly from snapshot + ledger fields to reduce reporting drift.
- P1 cycle-close checklist hardening delivered: cycle closure now requires explicit reconciliation across one-pass, evidence, pending tracker, snapshot, ledger, and weekly summary artifacts before marking pass.
- FEAT-004 compliance-doc remediation hardening delivered: deterministic document reason-code checks now drive evidence-card diagnostics and operator remediation guidance for missing, pending, rejected, stale, and source-missing evidence states.
- FEAT-004 backend reason-code parity hardening delivered: DDS package readiness API now emits deterministic document reason codes matching dashboard diagnostics, with unit coverage for rejected/stale/source-missing paths.
- FEAT-004 readiness-consumption hardening delivered: dashboard compliance page now reads backend readiness reason codes via proxy/hook and renders API-aligned remediation guidance with route/page test coverage.
- FEAT-004 compliance-section data parity hardening delivered: static compliance checks/evidence rows are now replaced by backend package detail + readiness-derived rows in the compliance page execution path.
- FEAT-004 typed evidence-document parity hardening delivered: backend package evidence-documents read endpoint, dashboard proxy/hook, and compliance evidence-card consumption now run on first-class typed evidence-document rows with role-boundary + route/page regression coverage.
- FEAT-004 evidence-document OpenAPI hardening delivered: contract draft now publishes `GET /v1/harvest/packages/{id}/evidence-documents` plus `DdsPackageEvidenceDocument` schema and concrete response example, with strict OpenAPI lint passing.
- FEAT-004 evidence-document Swagger parity hardening delivered: backend `HarvestController` now declares explicit `@ApiOkResponse` typed schema/example for `GET /v1/harvest/packages/{id}/evidence-documents`, keeping decorator-generated API metadata aligned with published OpenAPI contract.
- FEAT-004 evidence-document DTO centralization hardening delivered: backend now defines `DdsPackageEvidenceDocumentDto` + enum-backed metadata and `HarvestController` now references that DTO in `@ApiOkResponse`, eliminating duplicated inline Swagger schema literals.
- FEAT-004 evidence-document service-typing parity hardening delivered: `HarvestService` now aliases evidence-document return type to `DdsPackageEvidenceDocumentDto` and uses shared enum constants for mapped `type`/`reviewStatus` values, extending contract single-sourcing beyond controller decorators.
- FEAT-004 evidence-document enum-parity governance hardening delivered: new script `openapi:governance:evidence-doc:parity:check` now fail-closes when OpenAPI `DdsPackageEvidenceDocument` enum values drift from backend DTO enums, with strict OpenAPI lint remaining green.
- FEAT-004 evidence-document enum-parity CI enforcement hardening delivered: contracts workflow now runs blocking step `Enforce evidence-document OpenAPI/DTO enum parity` (`npm run openapi:governance:evidence-doc:parity:check`) and governance README run-order docs now include this gate.
- FEAT-004 evidence-document schema-parity hardening delivered: parity checker now validates required field set + field-level `type/format/nullable/enum` semantics and OpenAPI default example-row key/enum validity, expanding drift protection beyond enum sets.
- FEAT-004 evidence-document parser-hardening delivered: parity checker now reads DTO/OpenAPI contracts via TypeScript AST and YAML parsing (instead of text-shape regexes), reducing formatting-coupled false positives while preserving fail-closed checks.
- FEAT-004 evidence-document parity smoke-governance hardening delivered: fixture-based smoke harness (`openapi:governance:evidence-doc:parity:smoke`) now validates checker pass/fail behavior via `--dto/--openapi` fixture inputs, and contracts CI executes this smoke gate after parity check.
- FEAT-004 evidence-document smoke-matrix hardening delivered: smoke fixtures now cover enum, required-field, nullable, and example-key drift classes, with mismatch-specific assertion checks to ensure checker diagnostics fail for each targeted regression mode.
- FEAT-004 evidence-document smoke-matrix completion delivered: smoke fixtures now also cover field-format drift and non-nullable example-`null` drift, locking all currently enforced parity dimensions under explicit fixture-based failure assertions.
- FEAT-004 evidence-document error-code stability hardening delivered: parity checker failure paths now emit stable machine-readable error codes and smoke harness assertions now key off codes rather than human message wording.
- FEAT-004 evidence-document JSON-output hardening delivered: parity checker now supports optional `--json` output with structured PASS/FAIL payloads (`status`, `code`, `message`, `details`), and smoke suite now validates JSON-mode pass/fail behavior.
- FEAT-004 evidence-document JSON-contract governance hardening delivered: versioned schema (`evidence-doc-parity-report.schema.json`) plus assertion script now gate `--json` output shape for both PASS and FAIL payloads, and contracts CI executes this assertion after parity smoke checks.
- FEAT-004 evidence-document JSON-contract artifact hardening delivered: contracts CI now captures and uploads `contracts-openapi-evidence-doc-parity-contract` artifact containing parity PASS/FAIL JSON snapshots and schema for run-level governance evidence.
- FEAT-004 evidence-document JSON-contract summary hardening delivered: contracts CI now appends a dedicated summary block (`Evidence-Doc Parity JSON Contract`) with PASS/FAIL status, schema version, and fixture fail-code highlights to speed reviewer triage.
- FEAT-004 evidence-document JSON-contract metrics hardening delivered: contracts CI now generates `openapi-evidence-doc-parity-metrics.json` and uploads `contracts-openapi-evidence-doc-parity-metrics` artifact with compact schema-version/fail-code/check-count/run metadata for trend ingestion.
- Demo enablement standalone atlas delivered: marketing app now includes isolated route `/demo-ecosystem` (noindex, no main-site nav coupling) with diagram-first walkthrough sections for ecosystem map, field-to-filing data flow, canonical lifecycle states, and exception/recovery controls for live demos.
- FEAT-009 external partner data contract slice delivered: integrations feature now defines an MVP third-party reporting/analytics consumption model (tenant-scoped pull API, webhook push, scheduled bulk exports) with explicit scope/permission boundaries, canonical sync transitions, deterministic retry and replay semantics, immutable analytics evidence events, and v1.6 architecture-gate applicability.
- FEAT-009 backend partner-data skeleton delivered: integrations module now exposes tenant-safe external partner endpoints (`GET /v1/partner-data/datasets`, `POST /v1/partner-data/exports`) with explicit role/scope/idempotency guardrails, immutable partner audit events, focused controller tests, and published OpenAPI contracts.
- FEAT-009 partner export persistence/replay slice delivered: partner export starts now use migration-backed idempotency persistence (`TB-V16-018`) with deterministic replay response semantics (`replayed=true`), and new tenant-safe retrieval endpoints (`GET /v1/partner-data/exports/{id}`, `GET /v1/partner-data/exports/{id}/download`) plus OpenAPI/test coverage are in place.
- FEAT-009 export lifecycle slice delivered: partner export flow now includes API-driven finalize transition (`POST /v1/partner-data/exports/{id}/finalize`) with immutable delivery telemetry (`partner_webhook_delivered` / `partner_webhook_terminal_failed`) and DB-backed integration tests for replay, finalize, and completed-download behavior.
- FEAT-009 retry-queue operability slice delivered: partner export persistence now includes retry metadata (`TB-V16-019`), queue read (`GET /v1/partner-data/exports/retry-queue`) and failed-export retry (`POST /v1/partner-data/exports/{id}/retry`) endpoints with attempt-count progression, re-queue transitions, telemetry, and OpenAPI/test updates.
- FEAT-009 retry-policy hardening slice delivered: partner exports now enforce capped exponential retry behavior (`maxAttempts=5`) with exhaustion persistence (`TB-V16-020`), retry-cap denial semantics, and compact retry diagnostics endpoint (`GET /v1/partner-data/exports/retry-summary`) for operator visibility.
- FEAT-009 scheduler sweep slice delivered: partner exports now include scheduler-triggered due-retry sweep execution (`POST /v1/partner-data/exports/retry-sweep/trigger`) guarded by scheduler token contract, with immutable sweep telemetry (`partner_retry_sweep_executed`) and DB-backed verification of sweep re-queue behavior.
- FEAT-009 sweep-lifecycle observability slice delivered: retry sweeps now publish lifecycle events (`partner_retry_sweep_started/completed/failed`) with deterministic execution IDs, and retry summary now includes latest sweep rollup (`lastSweepRun`) for operator diagnostics.
- FEAT-009 scheduler-contract hardening slice delivered: retry sweep auth now uses required header token (`x-tracebud-scheduler-token`) and sweep telemetry/summary now includes scheduler token-version rollups for cross-environment diagnostics.
- FEAT-009 assessment workflow backbone slice delivered: backend now supports tenant-scoped dashboard-to-farmer assessment request creation/listing and lifecycle status transitions for execution/review handoff, backed by `TB-V16-021` persistence and controller regression coverage.
- FEAT-009 assessment workflow UI wiring slice delivered: dashboard `Requests` now integrates with live assessment request API routes (send + status review actions), and offline app home now surfaces assigned assessment tasks with farmer status progression actions (`opened/in_progress/submitted`).
