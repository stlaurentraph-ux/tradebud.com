# Current Focus

## Work now

- FEAT-009 post-closeout hardening slice complete: DB-backed integration coverage now verifies scheduler wrapper auth semantics and sweeper rollup payload persistence (`src/integrations/coolfarm-sai-v2.controller.int.spec.ts`), including scheduler token-version lineage assertions in `integration_audit_v2`.
- Release hygiene pass complete for backend baseline:
  - `npm test` -> `16 suites / 187 tests` pass
  - `npm run test:integration` -> `18 suites / 77 tests` pass
- FEAT-005 governance cleanup complete:
  - benchmark commodity/geography canonicalization now uses `coffee` + ISO-2 geography keys
  - yearly FAOSTAT dry-run workflow added (`.github/workflows/faostat-yearly-sync-dry-run.yml`)
  - yearly ops runbook added (`product-os/04-quality/faostat-yearly-sync-runbook.md`)
- Fixture strategy decision: cleanup by default after QA runs using `npm run fixtures:yield-cap:cleanup`.
- Next milestone decision pack published: `product-os/01-roadmap/next-milestone-decision-pack.md` (Option A FEAT-009 integrations-first vs Option B FEAT-004 compliance UX-first).
- FEAT-009 post-closeout hardening slice complete: V2 scheduler wrapper now enforces token auth contract (`x-tracebud-scheduler-token`) backed by required backend env `COOLFARM_SAI_V2_SCHEDULER_TOKEN`, with deterministic deny behavior for missing/invalid token paths.
- FEAT-005 post-closeout hardening slice complete: harvest runtime now resolves active FAOSTAT benchmark rows even when commodity is stored as numeric code (`656`) via commodity alias matching (`coffee` <-> `656` / `coffee, green`), with live audit evidence showing `yield_cap_resolved` + non-null benchmark IDs.
- FEAT-005 post-closeout hardening slice complete: plot creation now persists canonical `geography` (`GEOGRAPHY`) at insert time, satisfying `plot_geography_present_check` and unblocking end-to-end live harvest enforcement validation fixtures.
- FEAT-005 operational cadence decision locked: FAOSTAT benchmark sync cadence is set to **yearly** (aligned to FAOSTAT annual publication), with continued dual-control activation after review.
- FEAT-009 post-closeout hardening slice complete: V2 now includes a scheduler-facing stale-sweeper trigger contract (`POST /v1/integrations/coolfarm-sai/v2/runs/release-stale/trigger`) and run summary rollup fields (`lastSweeperReleasedCount`, `lastSweeperTriggerSource`) for quick operator interpretation.
- FEAT-009 post-closeout hardening slice complete: V2 stale-claim sweeper now supports cron-compatible trigger source metadata (`manual|scheduled`) and emits execution rollup event (`integration_v2_stale_sweeper_executed`); run summary now surfaces `lastSweeperRun` context for operator visibility.
- FEAT-009 post-closeout hardening slice complete: V2 stale-claim recovery is now live via `POST /v1/integrations/coolfarm-sai/v2/runs/release-stale` (threshold + scan cap), and run summary now exposes `staleClaimCount` for stuck-claim visibility in `GET /v1/integrations/coolfarm-sai/v2/runs/summary`.
- FEAT-009 post-closeout hardening slice complete: V2 claim release is now live at `POST /v1/integrations/coolfarm-sai/v2/runs/{runId}/release` with actor-owned release + optional forced stale-claim release (`force=true`) and immutable release telemetry (`integration_v2_run_released`).
- FEAT-009 post-closeout hardening slice complete: V2 run claim locking is now live via `POST /v1/integrations/coolfarm-sai/v2/runs/{runId}/claim` with due/failed/unclaimed eligibility checks, queue exclusion for claimed rows, and immutable claim telemetry (`integration_v2_run_claimed`) backed by migration `tb_v16_016_coolfarm_sai_v2_run_claim_lock.sql`.
- FEAT-009 post-closeout hardening slice complete: V2 retry queue scan is now live at `GET /v1/integrations/coolfarm-sai/v2/runs/retry-queue` with tenant-safe due-run filtering (`failed` + `next_retry_at<=now`) and bounded limit guardrails for worker-ready polling.
- FEAT-009 post-closeout hardening slice complete: V2 retry orchestration now includes exponential backoff scheduling (`next_retry_at`) and max-attempt guardrails (cap `5`) with deterministic exhaustion event telemetry (`integration_v2_run_retry_exhausted`) via migration `tb_v16_015_coolfarm_sai_v2_retry_backoff.sql`.
- FEAT-009 post-closeout hardening slice complete: V2 run retry endpoint is now live at `POST /v1/integrations/coolfarm-sai/v2/runs/{runId}/retry` with failed-only retry guardrails, `attempt_count` increment semantics, and immutable retry lineage events (`integration_v2_run_retry_completed|integration_v2_run_retry_failed`).
- FEAT-009 post-closeout hardening slice complete: V2 queue metadata and summary diagnostics are now live via migration `tb_v16_014_coolfarm_sai_v2_run_queue_metadata.sql` (`queued_at`, `attempt_count`, `error_code`) and `GET /v1/integrations/coolfarm-sai/v2/runs/summary` aggregate endpoint for tenant-scoped run-health visibility.
- FEAT-009 post-closeout hardening slice complete: V2 run-execution + run-history surfaces are now live (`POST/GET /v1/integrations/coolfarm-sai/v2/questionnaire-drafts/{id}/runs`) with deterministic `completed|failed` outcomes in `integration_runs_v2` and immutable run outcome audit events (`integration_v2_run_completed|integration_v2_run_failed`).
- FEAT-009 post-closeout hardening slice complete: V2 submit transition is now live at `POST /v1/integrations/coolfarm-sai/v2/questionnaire-drafts/{id}/submit` with canonical `draft->submitted` guardrails, validation-run lifecycle persistence in `integration_runs_v2` (`started` then `completed`), and immutable submit audit evidence (`integration_v2_questionnaire_submitted`).
- FEAT-009 post-closeout hardening slice complete: V2 draft persistence is now implemented via `POST /v1/integrations/coolfarm-sai/v2/questionnaire-drafts` with tenant fail-closed + role-scoped access, idempotent upsert semantics by `(tenant_id,idempotency_key)`, and V2 audit telemetry (`integration_v2_questionnaire_draft_saved`) backed by new isolated SQL baseline `tb_v16_013_coolfarm_sai_v2_questionnaire.sql`.
- FEAT-009 post-closeout hardening slice complete: V2 Cool Farm + SAI questionnaire endpoint now returns pathway-scoped mapping registry (`farmQuestionnaireMappingV1`) and enforces required-field mapping coverage guardrails, failing fast on mapping drift.
- FEAT-009 post-closeout hardening slice complete: V2 Cool Farm + SAI schema bootstrap API is now live at `GET /v1/integrations/coolfarm-sai/v2/questionnaire-schema` with tenant fail-closed checks, allowed-role gating (`exporter|agent|admin|compliance_manager`), pathway selection (`annuals|rice`), and shadow rollout metadata (`coolfarm_sai_v2_enabled`, default `off`).
- FEAT-009 V2 parallel-planning slice complete: Cool Farm + SAI non-Jira execution pack is now published at `product-os/01-roadmap/coolfarm-sai-v2-parallel-execution-pack.md` with isolated data boundaries, feature-flag rollout policy, parallel tracks (contract/backend/adapter/UI/QA), and readiness gates that keep V1 runtime untouched.
- FEAT-005 post-closeout hardening slice complete: FAOSTAT source sync now supports direct authenticated fetch fallback (no adapter URL required) using `YIELD_BENCHMARKS_FAOSTAT_API_KEY`, while preserving adapter URL mode when configured.
- FEAT-005 post-closeout hardening slice complete: source-sync ingestion boundary is now live (`POST /v1/yield-benchmarks/import/sync-source`) with USDA/FAOSTAT adapter URL config, dry-run support, and import-run diagnostics read surface (`GET /v1/yield-benchmarks/import-runs`).
- FEAT-005 post-closeout hardening slice complete: benchmark import scaffold is now live at `POST /v1/yield-benchmarks/import` with row-level validation, deterministic inactive-draft upsert behavior (create/update), and import lifecycle telemetry (`yield_benchmark_import_started|completed`).
- FEAT-005 post-closeout hardening slice complete: harvest runtime now resolves yield caps from active `yield_benchmarks` (commodity/geography aware with deterministic `GLOBAL` + default fallback), and emits `yield_cap_resolved|yield_cap_fallback_used|yield_cap_violation_detected` audit events.
- Demo enablement slice complete: standalone marketing demo atlas route is now live at `/demo-ecosystem` with isolated shell (no main-site header/footer links), noindex metadata, and one-page ecosystem diagrams covering actors, end-to-end data flow, canonical state transitions, and exception/recovery talk tracks.
- Remaining execution scorecard checkpoint refreshed: in-repo lanes are explicitly marked closed (`CI/Test`, `OpenAPI`, inbox/requests, `TB-V16-003`), and only external blocker lane remains (`spatial_ref_sys` owner remediation + Supabase ticket follow-through).
- FEAT-009 post-closeout hardening slice complete: admin interaction coverage now asserts success-path DDS status-read completion toast text (`EUDR DDS status read completed (status 200).`) in the same flow that reveals `Download JSON`.
- FEAT-009 post-closeout hardening slice complete: admin interaction tests now lock visibility gating for main status `Download JSON` control (appears only after successful status read).
- FEAT-009 post-closeout hardening slice complete: admin DDS status tests now cover main status-payload `Download JSON` semantics (filename/payload/URL lifecycle/toast) using shared success-path helper.
- FEAT-009 post-closeout hardening slice complete: admin test download-path URL/Blob stubbing is now centralized in reusable helper (`app/admin/test-helpers.tsx`) to reduce interaction-test boilerplate.
- FEAT-009 post-closeout hardening slice complete: admin DDS status tests now validate downloaded error-JSON payload content keys (`message`, `referenceNumber`, `occurredAt`) via Blob-stub assertions.
- FEAT-009 post-closeout hardening slice complete: admin DDS status tests now verify `Download error JSON` artifact semantics (filename pattern + object URL lifecycle + success toast).
- FEAT-009 post-closeout hardening slice complete: admin DDS status tests now verify `Copy error context` success branch payload shape and success-toast feedback.
- FEAT-009 post-closeout hardening slice complete: admin DDS status tests now lock `Copy error context` clipboard failure branch and verify deterministic error toast behavior.
- FEAT-009 post-closeout hardening slice complete: admin DDS status tests now lock `Copy filename` clipboard failure branch and verify deterministic error toast behavior.
- FEAT-009 post-closeout hardening slice complete: admin DDS status interaction tests now verify `Copy filename` clipboard write and success-toast feedback.
- FEAT-009 post-closeout hardening slice complete: shared DDS status failure-path test setup now lives in reusable admin helper (`app/admin/test-helpers.tsx`) to keep interaction tests concise.
- FEAT-009 post-closeout hardening slice complete: admin DDS status interaction coverage is now split into focused tests (export control names vs timestamp helper note) for faster fault isolation.
- FEAT-005 post-closeout hardening slice complete: backend CI now enforces benchmark-admin claim preflight (`BENCHMARK_ADMIN_ROLE_CLAIMS` required and must include `ADMIN,COMPLIANCE_MANAGER`) before integration tests run.
- FEAT-009 post-closeout hardening slice complete: admin interaction test now also asserts timestamp helper-note rendering in DDS status last-error export panel.
- FEAT-005 post-closeout hardening slice complete: backend health endpoint now exposes benchmark-admin claim diagnostics (`requiredClaims`, configured flag, warning on empty config) to prevent silent claim-policy lockout.
- FEAT-005 post-closeout hardening slice complete: yield benchmark admin routes are now claim-only (`ADMIN` / `COMPLIANCE_MANAGER`) with legacy exporter-email fallback removed.
- FEAT-005 post-closeout hardening slice complete: benchmark-admin auth now supports canonical role claims (`ADMIN`, `COMPLIANCE_MANAGER`) with claim-first enforcement in yield benchmark routes and explicit policy-mode audit attribution (`claim` vs `legacy_fallback`).
- FEAT-005 post-closeout hardening slice complete: backend now has canonical yield benchmark governance write path (`/v1/yield-benchmarks` create/update/list + `/{id}/activate`) with source-reference validation, draft->active dual-control enforcement, audit events, SQL migration baseline (`tb_v16_011_yield_benchmarks.sql`), and OpenAPI publication.
- P2 spec hardening advanced: `TRACEBUD_V1_2_EUDR_SPEC.md` Section 37 now uses source-verified bootstrap benchmark guidance with explicit derivation and source-reference baseline fields (FAOSTAT primary; USDA FAS cross-check lane for soy rows).
- Supabase owner-remediation execution lane is now automation-ready: deterministic post-run verifier `tracebud-backend/sql/tb_v16_009_postgis_owner_remediation_verify.sql` is available and wired into `product-os/04-quality/remaining-lanes-final-execution-pack.md` for one-shot PASS/FAIL evidence capture during the privileged window.
- BLK-003 is now closed: initial integration targets are documented in `product-os/04-quality/p1-integration-target-decision-template.md`, and implemented EUDR integration routes are now published in `docs/openapi/tracebud-v1-draft.yaml` with governance checks green.
- FEAT-009 post-closeout hardening slice complete: Tracebud OpenAPI now includes `GET /v1/integrations/eudr/echo`, `POST /v1/integrations/eudr/dds`, and `GET /v1/integrations/eudr/dds/status` under the `Integrations` tag.
- FEAT-009 post-closeout hardening slice complete: admin interaction coverage now verifies accessible export-control names in DDS status last-error panel (`app/admin/page.test.tsx`).
- FEAT-009 post-closeout hardening slice complete: DDS status last-error export controls now include explicit accessibility labels for copy/download filename/context actions.
- FEAT-009 post-closeout hardening slice complete: DDS status filename placeholder token is now centralized in shared utility and reused by both preview filename and explanatory note text.
- FEAT-009 post-closeout hardening slice complete: DDS status error-context handling now uses shared utility helpers (`lib/eudr-dds-status-error-context.ts`) with dedicated unit coverage.
- FEAT-009 post-closeout hardening slice complete: DDS status error-export filename generation now uses one shared builder for preview + download, preventing naming drift.
- FEAT-009 post-closeout hardening slice complete: DDS status filename preview now explicitly clarifies `<timestamp>` resolves at download time to avoid escalation-template ambiguity.
- FEAT-009 post-closeout hardening slice complete: DDS status last-error panel now includes one-click `Copy filename` beside the export filename preview for faster escalation template fill-ins.
- FEAT-009 post-closeout hardening slice complete: DDS status last-error panel now shows a deterministic suggested filename preview (`eudr-dds-status-error-<reference>-<timestamp>.json`) before exporting error JSON.
- FEAT-009 post-closeout hardening slice complete: DDS status last-error panel now includes inline helper text clarifying exported error JSON files are saved to the browser default Downloads folder.
- FEAT-009 post-closeout hardening slice complete: admin DDS status panel now includes one-click `Download error JSON` export for attachment-ready escalation packets (message/timestamp/reference payload).
- FEAT-009 post-closeout hardening slice complete: admin DDS status panel now includes one-click `Copy error context` export (mapped message, timestamp, reference) with success/failure toasts for escalation handoff packets.
- FEAT-009 post-closeout hardening slice complete: admin DDS status panel now persists and displays last status-read error context (message + timestamp + reference) for faster operator handoff, clearing on successful status checks.
- FEAT-009 post-closeout hardening slice complete: admin DDS status read now shows a compact retry/escalation hint block for malformed provider payload failures, driven by shared malformed-error predicate logic with test coverage.
- FEAT-009 post-closeout hardening slice complete: admin DDS status read now maps malformed provider payload failures to explicit operator guidance via shared status-feedback helper, with focused utility tests and status-route non-2xx pass-through coverage.
- P1 managed-vendor legal checklist tracker is now prefilled with suggested owner roles and due dates (`2026-04-24` to `2026-05-06`) plus initial evidence artifact placeholders to accelerate counsel/procurement execution.
- P1 managed-vendor legal checklist now includes execution tracker columns (owner/due/status/evidence) in `product-os/04-quality/p1-integration-target-decision-template.md` for deterministic counsel/procurement closure.
- P1 integration strategy is now explicitly locked in the decision template: managed EUDR REST API as primary delivery path with open-source fallback evaluation lane, plus required managed-vendor legal checklist before production sign-off (`product-os/04-quality/p1-integration-target-decision-template.md`).
- FEAT-009 post-closeout hardening slice complete: backend DDS status read now maps malformed provider JSON payloads to deterministic failure (`EUDR DDS status response was not valid JSON`) with dedicated unit test coverage.
- FEAT-009 post-closeout hardening slice complete: replay/non-replay DDS submit success messaging is now centralized in shared helper (`lib/eudr-dds-submit-feedback.ts`) with focused regression tests for replay wording and default status behavior.
- FEAT-009 post-closeout hardening slice complete: dashboard DDS submit UX now shows replay-aware success messaging when backend returns `replayed=true`, and DDS proxy route tests now lock replay payload pass-through contract (`statusCode=409`, `replayed=true`).
- FEAT-009 post-closeout hardening slice complete: backend DDS submit now treats provider `409` duplicates as deterministic idempotency replay success, returning explicit `replayed=true` and recording replay-aware audit telemetry (`integration_eudr_dds_submitted` phase `replayed`).
- FEAT-009 post-closeout hardening slice complete: status payload masking is now centralized in shared `payload-redaction` utility with dedicated tests, and admin diagnostics consumes this shared redaction path.
- FEAT-009 post-closeout hardening slice complete: admin DDS status payload handling now applies built-in redaction for likely sensitive keys across panel display, clipboard copy, and JSON download actions.
- FEAT-009 post-closeout hardening slice complete: admin DDS status panel now supports `Copy payload` and `Download JSON` actions with operator feedback to streamline evidence handoff.
- FEAT-009 post-closeout hardening slice complete: admin DDS status trigger now renders a result detail panel (reference/status/timestamp plus formatted provider payload excerpt) for operator troubleshooting.
- FEAT-009 post-closeout hardening slice complete: dashboard now supports DDS status read flow end-to-end (new status proxy route + admin status trigger card with reference input and operator feedback toasts).
- FEAT-009 post-closeout hardening slice complete: backend DDS status read endpoint is now live (`GET /v1/integrations/eudr/dds/status?referenceNumber=...`) with tenant/exporter-agent guardrails, fail-closed validation, timeout/error mapping, immutable audit telemetry (`integration_eudr_dds_status_checked`), and unit coverage.
- FEAT-009 post-closeout hardening slice complete: DDS schema validation is now centralized in shared utility (`lib/eudr-dds-validation.ts`) and reused by both admin preflight UI and dashboard DDS proxy route, with dedicated validator tests.
- FEAT-009 post-closeout hardening slice complete: admin DDS submit flow now uses schema-driven preflight validation (nested required/type checks with path-specific errors) instead of hard-coded field checks.
- FEAT-009 post-closeout hardening slice complete: admin DDS submit flow now performs lightweight pre-submit shape validation (`declarationType`, `referenceNumber`, `operator.name`, `product.commodity`) with targeted toast feedback before proxy calls.
- FEAT-009 post-closeout hardening slice complete: admin DDS card now includes `Copy JSON` operator helper with clipboard success/failure toasts for quicker external validation/debug workflows.
- FEAT-009 post-closeout hardening slice complete: admin DDS sample helper now supports preset selection (`import`, `export`, `domestic`) before one-click JSON load, improving operator scenario testing ergonomics.
- FEAT-009 post-closeout hardening slice complete: admin DDS submit card now includes one-click `Load sample JSON` helper that seeds a baseline statement scaffold and refreshes idempotency key for faster operator testing.
- FEAT-009 post-closeout hardening slice complete: admin diagnostics now includes an operator-triggered EUDR DDS submit card (idempotency + statement JSON inputs, submit action to `/api/integrations/eudr/dds`, validation/error toasts, and filing diagnostics reload on success).
- FEAT-009 post-closeout hardening slice complete: dashboard proxy route now supports EUDR DDS submit (`POST /api/integrations/eudr/dds`) with statement/idempotency validation, fail-closed backend-url enforcement, auth/header pass-through, and route test coverage.
- FEAT-009 post-closeout hardening slice complete: first real EUDR DDS write path is now live (`POST /v1/integrations/eudr/dds`) with exporter-only + tenant-claim enforcement, required `statement`/`idempotencyKey` validation, bounded timeout and upstream failure mapping, and immutable audit telemetry event `integration_eudr_dds_submitted` backed by unit coverage.
- FEAT-009 post-closeout hardening slice complete: backend now exposes tenant-safe EUDR connectivity check (`GET /v1/integrations/eudr/echo`) with exporter/agent role gating, server-side `EUDR_API_*` env usage, timeout/failure mapping, and audit telemetry event `integration_eudr_echo_checked` plus unit coverage.
- P1 integration target lock doc now includes authoritative secret-placement guidance for EUDR keys (backend-only env, dashboard non-secret boundary, production secret-manager requirement, and required env var names) in `product-os/04-quality/p1-integration-target-decision-template.md`.
- P1 integration target lock draft now includes provisional TRACES DDS lifecycle mapping (`POST/PUT/DELETE /eudr/dds` under `/api`) with an explicit Swagger verification gate before implementation lock in `product-os/04-quality/p1-integration-target-decision-template.md`.
- P1 integration target lock draft now includes source-backed TRACES/EUDR REST contract details from docs (`x-api-key`, optional `x-api-eudr-version`, base URL, echo smoke endpoint, and quick-start response-code taxonomy) in `product-os/04-quality/p1-integration-target-decision-template.md`.
- P1 integration target lock template is now pre-filled with a concrete first-pair proposal (`TRACES NT filing adapter` + `ERP outbound adapter`) at `product-os/04-quality/p1-integration-target-decision-template.md`, including draft auth/endpoint/idempotency/retry/tenant-boundary/event/constraint baselines for fast sign-off review.
- P1 external API integration target decision is now execution-ready via one-page lock template at `product-os/04-quality/p1-integration-target-decision-template.md` (covers provider/auth, endpoint contract, idempotency/retry, permission/state/exception/event gates, and v1.6 constraints) so BLK-003 can be closed after Product/Engineering sign-off.
- Recurring execution checkpoint is now tracked at `product-os/06-status/remaining-execution-scorecard.md` (completion % + two-week plan + weekly checklist for inbox/CI/spec/data lanes).
- Remaining execution checkpoint has been refreshed with current completion percentages and updated two-week targets (`inbox 88%`, `CI 84%`, `OpenAPI 86%`, `P2 52%`, `TB-V16-003 20%`, `spatial_ref_sys remediation 65%`) in `product-os/06-status/remaining-execution-scorecard.md`.
- Remaining execution checkpoint advanced with fresh execution evidence: ownership integration lane is green (`6 suites / 39 tests`) and OpenAPI governance lane re-validated, updating snapshot to `CI 86%` and `OpenAPI 87%` in `product-os/06-status/remaining-execution-scorecard.md`.
- Remaining execution checkpoint advanced again: ownership integration lane now includes API-level authenticated coverage for `GET /v1/audit/gated-entry/actors` (tenant-claim fail-closed + ids validation + tenant-scoped actor resolution) and is green at `7 suites / 42 tests`, updating scorecard CI depth to `88%`.
- Remaining execution checkpoint advanced again: ownership integration lane now includes API-level authenticated coverage for `GET /v1/audit/gated-entry/filing-activity` and `/export` (tenant fail-closed + phase filter + CSV metadata contract) and is green at `8 suites / 45 tests`, updating scorecard CI depth to `90%`.
- Remaining execution checkpoint advanced again: ownership integration lane now includes API-level authenticated coverage for `GET /v1/audit/gated-entry/chat-threads` (tenant fail-closed + `created`/`resolved` phase filtering) and is green at `9 suites / 48 tests`, updating scorecard CI depth to `92%`.
- Remaining execution checkpoint advanced again: ownership integration lane now includes API-level authenticated coverage for `GET /v1/audit/gated-entry/workflow-activity` (tenant fail-closed + phase/slaState filters) and is green at `10 suites / 51 tests`, updating scorecard CI depth to `94%`.
- Remaining execution checkpoint advanced again: ownership integration lane now includes API-level authenticated coverage for `GET /v1/audit/gated-entry/dashboard-summary` (tenant fail-closed + tenant-scoped counters/readiness) and is green at `11 suites / 54 tests`, updating scorecard CI depth to `96%`.
- Remaining execution checkpoint in-repo closeout is complete for CI/OpenAPI lanes: ownership lane remains green at `11 suites / 54 tests` and OpenAPI examples/contract depth refresh is lint-clean, updating scorecard to `CI 100%` and `OpenAPI 100%` in `product-os/06-status/remaining-execution-scorecard.md`.
- Remaining non-code closeout lanes now have a one-pass operator packet at `product-os/04-quality/remaining-lanes-final-execution-pack.md` covering TB-V16-003 staging execution, privileged `spatial_ref_sys` remediation, cooperative inbox fail-closed verification, go/no-go gates, rollback criteria, and status-evidence update targets.
- Remaining execution packet live-run update: cooperative inbox/backend verification command is now complete in-repo (`apps/dashboard-product` tests green: `31 files / 130 tests`), while TB-V16-003 and owner-remediation runs now have explicit blocking errors captured (`plot_geometry_version` missing on target DB; `must be owner of table spatial_ref_sys`).
- Remaining execution packet live-run update (latest): TB-V16-003 staging migration is now successfully executed with verification evidence (`plot.geography` present, missing backfill `0`, `idx_plot_geography_gist` present) after existence-safe handling for optional `plot_geometry_version`; the only remaining external blocker is owner-level execution for `spatial_ref_sys` remediation (`42501 must be owner of table spatial_ref_sys`).
- Remaining execution blocker tracking is now explicit: Supabase owner-only remediation required for `spatial_ref_sys` (`42501`) and escalation is marked `pending_ticket` (project ref `uzsktajlnofosxeqwdwl`, ticket ID `TBD`) in `product-os/06-status/remaining-execution-scorecard.md`.
- FEAT-002 post-closeout hardening slice complete: queue i18n CI governance now produces a single schema-versioned combined report (`queue-i18n-report.json`) that packages current summary, previous-run delta metadata, and baseline provenance, with dedicated generator + schema-assert gates and artifact upload coverage.
- FEAT-002 post-closeout hardening slice complete: queue i18n artifact now co-packages `queue-i18n-report.json` with `docs/openapi/queue-i18n-report.schema.json`, and app CI now fail-closes if the schema file is missing before artifact publish.
- FEAT-002 post-closeout hardening slice complete: combined queue i18n report now includes schema fingerprint provenance (`schemaSha256`) derived from `docs/openapi/queue-i18n-report.schema.json`, and CI summary surfaces this digest for contract-integrity review.
- FEAT-002 post-closeout hardening slice complete: combined report assertion now enforces strict lowercase hex format for `schemaSha256` (`^[a-f0-9]{64}$`) before digest parity comparison.
- FEAT-002 post-closeout hardening slice complete: combined report contract now declares explicit digest algorithm (`schemaDigestAlgorithm=sha256`) and assertion fails closed if algorithm metadata diverges.
- FEAT-002 post-closeout hardening slice complete: combined report now includes compact provenance token (`schemaDigestRef=sha256:<digest>`) with schema pattern enforcement and assertion parity checks.
- FEAT-002 post-closeout hardening slice complete: queue i18n combined report consumer contract now treats `schemaDigestRef` as the primary pin; `schemaSha256` is documented as compatibility-only; CI summary and JSON Schema descriptions align with that ordering.
- FEAT-002 post-closeout hardening slice complete: queue i18n baseline artifact fetch now extracts previous `queue-i18n-report.json` when bundled; combined report v2 emits `previousSchemaDigestRef` + `schemaDigestRefChanged` for run-to-run schema digest drift visibility in CI summary.
- FEAT-002 post-closeout hardening slice complete: queue i18n baseline zip extraction for previous smoke summary now tries `apps/offline-product/` first then root filename, matching report extraction and GitHub artifact path layout.
- FEAT-002 post-closeout hardening slice complete: queue i18n baseline zip extraction now emits optional `unzip -l` diagnostics when any path misses and `ACTIONS_STEP_DEBUG=true` (step debug) is enabled.
- FEAT-002 post-closeout hardening slice complete: queue i18n baseline fetch step now forwards `ACTIONS_STEP_DEBUG` from repository secrets into step `env` so bash diagnostics align with GitHub step-debug semantics.
- FEAT-002 post-closeout hardening slice complete: offline app now exposes `npm run i18n:queue:verify` as a single local command for the full queue i18n smoke/summary/baseline/report assertion chain.
- FEAT-002 post-closeout hardening slice complete: offline app now includes `i18n:queue:clean` and `i18n:queue:verify:clean` to remove generated queue i18n JSON artifacts after local verify runs, and `i18n:queue:verify` is orchestrated by `i18n-queue-verify.mjs` for reliable Node-side execution (no shell redirect).
- FEAT-002 post-closeout hardening slice complete: queue i18n clean scripts now support `clean:all` / `verify:clean:all` to drop optional baseline/previous/artifact zip files when explicitly requested.
- FEAT-002 post-closeout hardening slice complete: `apps/offline-product/.gitignore` now excludes queue i18n local governance artifacts from version control noise.
- FEAT-002 post-closeout hardening slice complete: repository root `package.json` now exposes `i18n:queue:smoke` / `i18n:queue:verify` / `i18n:queue:verify:clean` / `i18n:queue:verify:clean:all` delegating to the offline app package.
- FEAT-003 post-closeout hardening slice complete: historical deforestation decision contract (`POST /v1/plots/{id}/deforestation-decision?cutoffDate=YYYY-MM-DD`) is now live with agent/exporter gating, immutable decision telemetry (`plot_deforestation_decision_recorded`), service/controller unit coverage, and OpenAPI publication.
- FEAT-003 post-closeout hardening slice complete: deforestation decision history read surface (`GET /v1/plots/{id}/deforestation-decision-history`) is now live with immutable event retrieval contract, controller test coverage, and OpenAPI publication.
- FEAT-003 post-closeout hardening slice complete: dashboard decision-evidence visibility is now live via plot-detail decision-history panel + hook + proxy route, and DB-backed integration now proves persisted decision-history retrieval (`plot_deforestation_decision_recorded`).
- FEAT-004 post-closeout hardening slice complete: compliance evidence verification now computes deterministic document reason codes (`DOC_MISSING`, `DOC_PENDING_REVIEW`, `DOC_REJECTED`, `DOC_STALE`, `DOC_SOURCE_MISSING`) and surfaces remediation guidance directly in dashboard evidence cards.
- FEAT-004 post-closeout hardening slice complete: backend DDS readiness evaluation now emits the same deterministic document reason codes for API-level preflight parity with dashboard diagnostics.
- FEAT-004 post-closeout hardening slice complete: compliance page now consumes backend readiness reason codes through dashboard proxy route + hook and renders API-aligned remediation guidance in-page.
- FEAT-004 post-closeout hardening slice complete: compliance checks/evidence sections now consume backend package detail + readiness payloads (with static fallback only when no package diagnostics are selected).
- FEAT-004 post-closeout hardening slice complete: typed package evidence-documents contract is now live (`GET /v1/harvest/packages/{id}/evidence-documents`) with dashboard proxy/hook wiring, and compliance evidence cards now consume first-class evidence-document rows.
- FEAT-004 post-closeout hardening slice complete: OpenAPI draft now publishes `GET /v1/harvest/packages/{id}/evidence-documents` and `DdsPackageEvidenceDocument` schema with concrete response example for contract-consumer parity.
- FEAT-004 post-closeout hardening slice complete: backend Swagger decorator parity is now in place for `GET /v1/harvest/packages/{id}/evidence-documents` via explicit `@ApiOkResponse` typed schema/example annotation in controller.
- FEAT-004 post-closeout hardening slice complete: evidence-documents Swagger schema now uses shared DTO source (`DdsPackageEvidenceDocumentDto`) with enum-backed field metadata instead of inline controller schema literals.
- FEAT-004 post-closeout hardening slice complete: `HarvestService` evidence-document return typing now reuses shared DTO contract + enums, removing duplicate string-literal schema definitions from service mapping logic.
- FEAT-004 post-closeout hardening slice complete: automated governance parity check now asserts OpenAPI `DdsPackageEvidenceDocument` enum values stay aligned with backend DTO enums (`openapi:governance:evidence-doc:parity:check`).
- FEAT-004 post-closeout hardening slice complete: contracts CI now enforces `openapi:governance:evidence-doc:parity:check` as a blocking gate, so OpenAPI/DTO evidence-document enum drift fails PR validation automatically.
- FEAT-004 post-closeout hardening slice complete: evidence-doc parity gate now also asserts required fields, field-level `type/format/nullable/enum` parity, and OpenAPI example-row key/enum validity (beyond enum-only drift checks).
- FEAT-004 post-closeout hardening slice complete: evidence-doc parity extraction now uses TypeScript AST + YAML parser-backed reads, reducing formatting-sensitive checker fragility while keeping fail-closed drift enforcement.
- FEAT-004 post-closeout hardening slice complete: fixture-based smoke coverage now validates evidence-doc parity checker pass/fail behavior (`openapi:governance:evidence-doc:parity:smoke`) and contracts CI executes it after the blocking parity gate.
- FEAT-004 post-closeout hardening slice complete: evidence-doc smoke matrix now explicitly covers enum/required/nullable/example-key drift classes with mismatch-specific assertions to lock multi-dimension checker diagnostics.
- FEAT-004 post-closeout hardening slice complete: evidence-doc smoke matrix now also covers `format` drift and non-nullable example-`null` drift, completing fixture coverage for all currently enforced parity dimensions.
- FEAT-004 post-closeout hardening slice complete: evidence-doc parity checker now emits stable machine-readable error codes and smoke assertions are code-based, reducing regression-test fragility against human-message wording edits.
- FEAT-004 post-closeout hardening slice complete: evidence-doc parity checker now supports `--json` structured output (PASS/FAIL + code/message/details), and smoke coverage includes JSON-mode pass/fail assertions.
- FEAT-004 post-closeout hardening slice complete: versioned JSON schema + assertion gate now validate parity `--json` output contract (`status/schemaVersion` envelope and PASS/FAIL typed fields) in both local and contracts CI flows.
- FEAT-004 post-closeout hardening slice complete: contracts CI now publishes dedicated parity JSON contract artifact (`contracts-openapi-evidence-doc-parity-contract`) with PASS/FAIL payload snapshots plus schema for reviewer evidence traceability.
- FEAT-004 post-closeout hardening slice complete: contracts CI now publishes an `Evidence-Doc Parity JSON Contract` summary block with schema version and fail-code highlights for low-friction reviewer triage.
- FEAT-004 post-closeout hardening slice complete: contracts CI now generates/uploads compact parity JSON metrics artifact (`contracts-openapi-evidence-doc-parity-metrics`) with schema version, fail code, check counters, and run metadata for trend tooling.
- FEAT-008 post-closeout hardening slice complete: requests campaign dialog now supports bulk contact import (CSV upload/paste, validation, deduplication, preview) with draft target payload wiring and page-level test coverage.
- FEAT-008 post-closeout hardening slice complete: requests campaign draft creation is now wired to real API proxy (`POST /api/requests/campaigns` -> `/v1/requests/campaigns`) with idempotency header forwarding and explicit UI success/error states.
- FEAT-008 post-closeout hardening slice complete: requests page integration tests now assert draft-creation success and failure banner states after bulk-target submission.
- FEAT-008 post-closeout hardening slice complete: requests submit tests now lock outbound bulk-target payload mapping + idempotency/header contract to guard against request-body drift.
- FEAT-008 post-closeout hardening slice complete: negative submit test now proves malformed CSV rows are excluded from outbound `targets[]` payload while surfacing row-level validation diagnostics.
- FEAT-008 post-closeout hardening slice complete: parse summary now shows `valid / invalid excluded` (plus duplicate skip count) to give operators immediate pre-submit confidence.
- FEAT-008 post-closeout hardening slice complete: bulk-import dialog now provides `Download CSV template` helper with tested download trigger behavior for first-time user onboarding.
- FEAT-008 post-closeout hardening slice complete: bulk-import dialog now includes `Paste sample CSV` quick-fill action with immediate parse/preview feedback for onboarding/demo speed.
- FEAT-008 post-closeout hardening slice complete: CSV parser now accepts `name` alias for `full_name` and surfaces alias-usage warning badges before submit.
- FEAT-008 post-closeout hardening slice complete: CSV parser alias coverage now includes `email_address`, `farmerid`, and `plotid` mappings for external CRM export compatibility.
- FEAT-008 post-closeout hardening slice complete: parser now accepts spaced/hyphenated header variants (e.g., `email address`, `full name`, `farmer id`, `plot id`) with visible alias warnings.
- FEAT-003 post-closeout hardening slice complete: plot detail now supports run-decision action (`cutoffDate` + POST) and auto-refreshes deforestation decision history after successful runs.
- FEAT-003 post-closeout hardening slice complete: deforestation decision panel now includes last-run metadata visibility (cutoff/verdict/provider mode) and contextual retry action after failed runs, with focused hook/panel regression tests.
- FEAT-005 closeout complete: v1.6 architecture-constraint applicability was confirmed for touched risk-scoring surfaces, provider/protocol open question resolved to `internal_v1` for S1 scope, and feature status is now `Done (TB-V16-005 / FEAT-005)`.
- FEAT-006 S1 matrix is now documented in feature spec (permissions/state transitions/exceptions/analytics/acceptance/v1.6 constraints) and ready for implementation kickoff.
- FEAT-006 S1 code slice 2 complete: filing pre-flight endpoint contract (`/v1/harvest/packages/{id}/filing-preflight`) now enforces tenant-claim/exporter scope, emits `dds_package_filing_preflight_*` lifecycle telemetry, and is covered by unit + DB-backed controller integration tests with OpenAPI publication.
- FEAT-006 S1 code slice 3 complete: package generation (`POST /v1/harvest/packages/{id}/generate`) and idempotent submission (`PATCH /v1/harvest/packages/{id}/submit` with `idempotencyKey`) contracts are now live with tenant/exporter policy enforcement, lifecycle telemetry, and test/OpenAPI coverage.
- FEAT-006 S1 code slice 4 complete: filing lifecycle diagnostics list/export surfaces (`/v1/audit/gated-entry/filing-activity` + `/export`) now provide tenant-scoped generation/submission event visibility with dashboard proxy/admin UI integration and OpenAPI-backed event schemas.
- FEAT-006 closeout complete: architecture-constraint applicability is documented for filing middleware touched areas, provider/protocol open question is resolved for S1 scope, and feature status is now `Done (TB-V16-006 / FEAT-006)`.
- FEAT-007 S1 code slice 1 complete: chat threads feature now has execution matrix bootstrap documented (permissions, state transitions, exception recovery, analytics baseline, acceptance mapping, v1.6 constraints), and FEAT-007 status is now `In progress`.
- FEAT-007 S1 code slice 2 complete: chat-thread backend contract endpoints are now live with tenant-claim enforcement, idempotent message replay semantics, and OpenAPI + unit-test coverage.
- FEAT-007 S1 code slice 3 complete: chat-thread lifecycle telemetry (`created`, `message_posted`, `message_replayed`) now persists to audit log, tenant-scoped diagnostics endpoint (`/v1/audit/gated-entry/chat-threads`) is live, and admin diagnostics now surfaces chat-thread activity with filter + pagination.
- FEAT-007 S1 code slice 4 complete: explicit thread lifecycle transition endpoints (`resolve`, `reopen`, `archive`) are now live with deterministic state guards, telemetry parity (`chat_thread_resolved|reopened|archived`), and diagnostics/OpenAPI phase coverage updates.
- FEAT-007 S1 code slice 5 complete: closeout diagnostics assertions now cover transition-phase filtering (`resolved`) in unit + DB-backed integration lanes, open question is resolved for S1 scope, and feature status is now `Done (TB-V16-007 / FEAT-007)`.
- FEAT-008 S1 code slice 1 complete: dashboards feature now has execution matrix bootstrap documented (permissions, state transitions, exception recovery, analytics baseline, acceptance mapping, and v1.6 architecture-constraint applicability).
- FEAT-008 S1 code slice 2 complete: tenant-scoped dashboard diagnostics summary contract (`/v1/audit/gated-entry/dashboard-summary`) is live with backend/proxy/admin summary surface, test coverage, and OpenAPI schema publication.
- FEAT-008 S1 code slice 3 complete: dashboard diagnostics summary now includes phase/status segmented drill-down counters plus readiness/export guardrail metadata (`hasAnyDiagnostics`, `canExportDetailed`, `latestEventAt`) across backend contract, admin UI, tests, and OpenAPI schema.
- FEAT-008 S1 code slice 4 complete: summary widgets now drive diagnostics drill-down filters directly and `Export All CSV` actions are readiness-gated; backend acceptance assertions now explicitly cover empty-tenant non-export-ready summary outcomes.
- FEAT-008 S1 code slice 5 complete: acceptance matrix is reconciled, open question is resolved to internal API/audit protocol for S1 scope, and FEAT-008 status is now `Done (TB-V16-008 / FEAT-008)`.
- FEAT-009 S1 code slice 1 complete: integrations feature now has execution matrix bootstrap documented (permissions, state transitions, exception recovery, analytics baseline, acceptance mapping, and v1.6 architecture-constraint applicability).
- FEAT-009 S1 code slice 2 complete: tenant-safe webhook registration/list/delivery evidence contract baseline is live with role-scoped policy gates, audit-backed delivery telemetry, backend unit + DB-backed integration tests, and OpenAPI updates.
- FEAT-009 S1 code slice 3 complete: dashboard analytics proxy now routes webhook/delivery diagnostics reads, admin diagnostics now surfaces integration webhook registration + delivery evidence tables, and route tests cover forwarding/fail-closed delivery query validation.
- FEAT-009 S1 code slice 4 complete: acceptance matrix is reconciled, open question is resolved to internal API/audit-log webhook evidence protocol for S1 scope, and FEAT-009 status is now `Done (TB-V16-009 / FEAT-009)`.
- FEAT-010 S1 code slice 1 complete: workflow templates feature now has execution matrix bootstrap documented (permissions, state transitions, exception recovery, analytics baseline, acceptance mapping, and v1.6 architecture-constraint applicability).
- FEAT-010 S1 code slice 2 complete: workflow templates backend contract baseline is now live with tenant-safe create/list/transition endpoints, deterministic stage-state guards, workflow audit telemetry (`workflow_template_created`, `workflow_stage_transitioned`), unit + DB-backed integration tests, and OpenAPI publication.
- FEAT-010 S1 code slice 3 complete: workflow diagnostics read surfaces are now live via backend audit endpoint + dashboard proxy/hook/admin panel (`eventKind=workflow_activity`) with phase/SLA filters, tenant-safe paging, and test/OpenAPI coverage.
- FEAT-010 S1 code slice 4 complete: workflow SLA write-path telemetry is now live via stage SLA transition endpoint with deterministic SLA state-machine guardrails/recovery (`on_track|warning|breached|escalated`), role policy enforcement, and telemetry parity including recovery (`workflow_stage_sla_recovered`) with unit + DB-backed integration and OpenAPI updates.
- FEAT-010 S1 code slice 5 complete: closeout reconciliation is complete, acceptance matrix is explicitly mapped to delivered scope, open question is resolved to internal API/audit-log telemetry protocol for S1, and FEAT-010 status is now `Done (TB-V16-010 / FEAT-010)`.
- Next execution target: start next roadmap feature execution slice after FEAT-010 done-state handoff.
- Execute P0 gates from `product-os/01-roadmap/v1-6-spec-execution-board.md` before engineering kickoff (`P0-01` contradiction pass, `P0-02` legal memo, `P0-03` pilot setup).
- P0-01 contradiction pass sequencing is complete: Pass 1-5 complete; CL-001..CL-020 normalized and marked resolved in contradiction log.
- P0-02 legal memo commissioning brief is prepared at `product-os/04-quality/p0-02-counsel-memo-commissioning-brief.md`; closeout remains blocked pending signed counsel memo.
- P0-02 memo intake template is now prepared at `product-os/04-quality/p0-02-counsel-memo-intake-template.md` to capture memo ID/signature/date, legal decisions, and section-level spec deltas on receipt.
- P0-02 legal spec-delta patch scaffold is now prepared at `product-os/04-quality/p0-02-legal-spec-delta-patch-scaffold.md` with pre-labeled legal-sensitive sections (29, 22, 23, 45.4) for rapid memo-to-spec application.
- P0-02 single-shot closeout checklist is now prepared at `product-os/04-quality/p0-02-closeout-checklist.md` with exact file/sequence/touchpoint validation for one-pass gate closure.
- P0-02 counsel handoff message draft is now prepared at `product-os/04-quality/p0-02-counsel-handoff-message-draft.md` with decision scope, source pack, and signed-memo output contract.
- P0-02 counsel dispatch checklist is now prepared at `product-os/04-quality/p0-02-counsel-dispatch-checklist.md` with pre-send checks, manifest, and post-send status update steps.
- P0-02 counsel response tracker is now prepared at `product-os/04-quality/p0-02-counsel-response-tracker.md` for SLA/follow-up cadence and memo receipt handoff tracking.
- P0-02 response tracker is now initialized with dispatch-ready placeholders (`not_sent` + 3-business-day follow-up cadence) to reduce send-time friction.
- P0-02 post-send status snippet is now prepared at `product-os/04-quality/p0-02-post-send-status-snippet.md` for consistent `current-focus` + `daily-log` updates after dispatch.
- P0-02 dispatch example is now prepared at `product-os/04-quality/p0-02-counsel-dispatch-example.md` as a copy-edit/send template plus tracker-ready metadata block.
- P0-02 readiness index is now prepared at `product-os/04-quality/p0-02-readiness-index.md` to provide one-page gate state, artifact chain, and closure definition.
- P0-02 final send packet is now prepared at `product-os/04-quality/p0-02-send-packet.md` with fill-once fields, email body, and post-send logging block.
- P0-02 tracker has been reset to `not_sent` because no external counsel is appointed yet; dispatch metadata remains intentionally blank until counsel onboarding.
- P0-02 assumed-dispatch overwrite checklist is now prepared at `product-os/04-quality/p0-02-assumed-dispatch-overwrite-checklist.md` for deterministic replacement of provisional metadata.
- P0-02 dispatch operability hardening is complete: final send packet now includes strict preflight gates, explicit source-pack manifest, channel/thread metadata capture, and a copy-ready post-send status snippet for one-shot execution.
- P0-02 follow-up cadence hardening is complete: dedicated D+3, D+6, and escalation message templates are now prepared at `product-os/04-quality/p0-02-counsel-follow-up-templates.md` and linked into readiness flow.
- P0-02 one-pass update hardening is complete: a fill-once dispatch metadata block now produces paste-ready tracker/current-focus/daily-log updates at `product-os/04-quality/p0-02-dispatch-one-pass-update-block.md`.
- P0-02 memo-receipt closeout hardening is complete: a fill-once memo-received block now produces paste-ready tracker/intake/scaffold/current-focus/done-log/daily-log updates at `product-os/04-quality/p0-02-memo-received-one-pass-closeout-block.md`.
- P0-02 operator runbook hardening is complete: end-to-end phase flow with if/then branches (dispatch, follow-up cadence, memo receipt, closeout) is now consolidated at `product-os/04-quality/p0-02-operator-runbook.md`.
- P0-03 pilot gate bootstrap is now prepared: kickoff checklist, evidence log, and readiness index are available at `product-os/04-quality/p0-03-pilot-kickoff-checklist.md`, `product-os/04-quality/p0-03-pilot-evidence-log.md`, and `product-os/04-quality/p0-03-readiness-index.md`.
- P0-03 execution ergonomics are now hardened: kickoff one-pass metadata block and phase-based operator runbook are available at `product-os/04-quality/p0-03-kickoff-one-pass-update-block.md` and `product-os/04-quality/p0-03-operator-runbook.md`.
- P0-03 weekly execution reporting is now standardized: week 1-12 checkpoint template pack with gate-progress scoreboard and midpoint escalation rules is available at `product-os/04-quality/p0-03-weekly-checkpoint-template-pack.md`.
- P0-03 midpoint governance is now standardized: week-6 decision packet (`continue/recover/escalate`) with required evidence/action fields is available at `product-os/04-quality/p0-03-midpoint-review-packet.md`.
- P0-03 closeout ergonomics are now standardized: week-12 closure can be run from one fill-once artifact at `product-os/04-quality/p0-03-closeout-one-pass-update-block.md` with execution-board + status-log sync guidance.
- P1 external decision gate ergonomics are now standardized: cross-gate decision capture and status propagation are available at `product-os/04-quality/p1-external-decision-one-pass-update-block.md` with readiness index at `product-os/04-quality/p1-external-decision-readiness-index.md`.
- P1 evidence governance is now standardized: decision gates now use a dedicated evidence register at `product-os/04-quality/p1-external-decision-evidence-register.md` with verification/freshness status per gate.
- P1 execution workflow is now standardized: a phase-based operator runbook is available at `product-os/04-quality/p1-external-decision-operator-runbook.md` for preflight, evidence verification, decision publication, and escalation cadence.
- P1 pending-gate operations are now standardized: unresolved gates now track owner/blocker/SLA/escalation in `product-os/04-quality/p1-pending-gate-tracker-board.md`.
- P1 leadership reporting is now standardized: each cycle can publish a concise risk/escalation rollup via `product-os/04-quality/p1-cycle-snapshot-template.md`.
- P1 cross-cycle trend tracking is now standardized: append-only cycle deltas and blocker recurrence are captured in `product-os/04-quality/p1-decision-history-ledger.md`.
- P1 weekly reporting wording is now standardized: a paste-ready weekly summary block sourced from snapshot + ledger is available at `product-os/04-quality/p1-weekly-summary-block-template.md`.
- P1 cycle close-governance is now standardized: reconciliation across one-pass/evidence/tracker/snapshot/ledger/weekly-summary is gated by `product-os/04-quality/p1-cycle-close-checklist.md`.
- Next execution target: onboard external counsel for P0-02 dispatch and begin P0-03 pilot kickoff setup in parallel (cooperative + buyer confirmation and pilot dates).
- Execute P1 external decision gates (cloud region, TRACES WSDL, GeoID access, legal sign-offs).
- Convert Section 32.8 endpoint contract catalog into implementation artifacts (OpenAPI draft at `docs/openapi/tracebud-v1-draft.yaml`; pass 2 tightened shipment/sync/portability/export/billing/webhook payloads + explicit conflict/422 responses; next: add endpoint-level examples and role-scope extensions).
- Replace provisional benchmark seed values in spec Section 37 with source-verified FAOSTAT/USDA/national statistics data.
- Start TB-V16-003 migration execution in staging (`GEOGRAPHY` add/backfill/index + parity checks).
- FEAT-002 S1 matrix is now documented in feature spec (permissions/state transitions/exceptions/analytics/acceptance/v1.6 constraints) and ready for implementation kickoff.
- FEAT-002 S1 code slice 1 complete: plot mobile sync metadata endpoints now fail closed on missing tenant claim, with DB-backed controller-scope deny/allow coverage and DTO-level HLC envelope validation tests.
- FEAT-002 S1 code slice 2 complete: DB-backed Nest HTTP integration now proves sync envelope policy end-to-end (`403` missing tenant, `400` malformed HLC, and valid envelope audit persistence with idempotency metadata).
- FEAT-002 S1 code slice 3 complete: sync metadata endpoints now enforce farmer ownership scope (`Plot scope violation` on foreign farmer) while preserving agent allow paths, validated in both controller-scope DB integration and API-level HTTP integration tests.
- FEAT-002 S1 code slice 4 complete: sync metadata now enforces assignment-aware agent scope (`assignmentId` + active assignment relation) with fail-closed behavior when assignment relation is absent.
- FEAT-002 S1 code slice 5 complete: canonical assignment-scope migration (`tb_v16_010`) and OpenAPI sync endpoint contracts are now published, including `assignmentId` envelope semantics and lint-validated docs.
- FEAT-002 post-closeout hardening slice 25 complete: offline app now exposes pending-sync retry health (retrying count, max attempts, latest queue error context) and queue processor now logs explicit local audit telemetry for succeeded/failed/dropped-invalid queue actions.
- FEAT-002 post-closeout hardening slice 26 complete: offline app now supports selective queue retry filters (`harvest` / `photos_sync` / `evidence_sync`) and queue processing accepts action-type scope while keeping deterministic HLC ordering within selected actions.
- FEAT-002 post-closeout hardening slice 27 complete: selective retry chips now expose per-action pending counts (`Harvest/Photos/Evidence`) so operators can prioritize backlog classes before running constrained-bandwidth retries.
- FEAT-002 post-closeout hardening slice 28 complete: settings now supports retry attempt-scope presets (`all`, `retrying only`, `first-attempt only`) and queue processing honors this scope while preserving deterministic HLC ordering for the selected subset.
- FEAT-002 post-closeout hardening slice 29 complete: settings now provides `Smart sweep` mode that runs retrying queue rows first and first-attempt rows second in one sync run, with explicit pass-level operator feedback.
- FEAT-002 post-closeout hardening slice 30 complete: smart sweep now has per-run cap guardrails (`25/50/100/200`) and pass-2 remaining-budget logic so large queues do not force long single-run retries.
- FEAT-002 post-closeout hardening slice 31 complete: settings now persists smart-retry preferences (attempt scope, smart-sweep enabled, cap) via local settings storage and re-hydrates them on screen focus.
- FEAT-002 post-closeout hardening slice 32 complete: settings now includes one-tap retry preference reset to restore default queue profile (`all`, smart sweep off, cap `100`) in both UI state and persisted settings.
- FEAT-002 post-closeout hardening slice 33 complete: reset action now emits immediate in-panel confirmation message so operators can verify preference reset execution without ambiguity.
- FEAT-002 post-closeout hardening slice 34 complete: reset confirmation is now localized through language keys (`sync_queue_preferences_reset`) with EN/ES parity.
- FEAT-002 post-closeout hardening slice 35 complete: remaining queue panel helper labels/summaries are now i18n-backed (EN/ES) for full retry-settings localization parity.
- FEAT-002 post-closeout hardening slice 36 complete: added queue i18n smoke guard (`npm run i18n:queue:smoke`) validating required EN/ES queue-panel translation keys.
- FEAT-002 post-closeout hardening slice 37 complete: queue i18n smoke is now wired into CI Expo app lane (`lint` -> `i18n:queue:smoke`) for automated localization parity enforcement.
- FEAT-002 post-closeout hardening slice 38 complete: Expo app CI lane now publishes queue i18n smoke step-summary visibility (`Offline App Queue i18n Smoke`) for faster reviewer confirmation.
- FEAT-002 post-closeout hardening slice 39 complete: queue i18n smoke now emits structured metadata (`smokeVersion`, `requiredKeyCount`) and CI summary now surfaces both values for scope-drift traceability.
- FEAT-002 post-closeout hardening slice 40 complete: Expo app CI lane now uploads queue i18n smoke summary JSON artifact (`offline-app-queue-i18n-smoke-summary`) for run-to-run metadata diffing.
- FEAT-002 post-closeout hardening slice 41 complete: Expo app CI lane now attempts previous-run queue i18n summary retrieval and publishes key-count/version deltas (with safe `n/a` fallback when baseline is missing).
- FEAT-002 post-closeout hardening slice 42 complete: previous queue i18n baseline selection now filters candidates to successful workflow runs before computing summary deltas.
- FEAT-002 post-closeout hardening slice 43 complete: previous queue i18n baseline selection now also enforces workflow-source matching (workflow-path with workflow-name fallback) to reduce cross-workflow delta noise.
- FEAT-002 post-closeout hardening slice 44 complete: queue i18n summary now surfaces baseline provenance fields (run id, workflow path/name) sourced from selected previous artifact metadata.
- FEAT-002 post-closeout hardening slice 45 complete: queue i18n artifact upload now packages baseline provenance metadata JSON with current summary JSON for co-located evidence.
- FEAT-002 post-closeout hardening slice 46 complete: queue i18n summary/baseline JSON shape assertion is now scripted and enforced in Expo app CI lane (`i18n:queue:summary:assert`).
- FEAT-002 post-closeout hardening slice 47 complete: baseline metadata now includes explicit `schemaVersion`, assertion enforces it, and CI summary surfaces that version for contract-evolution traceability.
- FEAT-002 post-closeout hardening slice 48 complete: dedicated baseline metadata schema file + standalone schema assertion command are now added and enforced in Expo app CI lane.
- FEAT-002 S1 code slice 6 complete: assignment lifecycle APIs (create/complete/cancel) now enforce explicit role/tenant boundaries and typed `ASN-001..004` transition errors, with DB-backed controller integration and OpenAPI contract publication.
- FEAT-002 S1 code slice 7 complete: dashboard plot detail now ships assignment lifecycle operator controls with proxy routes and ASN-guided recovery messaging backed by dashboard route/component tests.
- FEAT-002 S1 code slice 8 complete: assignment history listing is now exposed in backend/proxy and rendered in plot detail panel with click-to-fill ID selection for faster complete/cancel workflows.
- FEAT-002 S1 code slice 9 complete: assignment history now supports backend-filtered pagination (status/agent/fromDays/offset-limit) and dashboard operator panel controls for large-volume assignment triage.
- FEAT-002 S1 code slice 10 complete: assignment history now includes backend-provided agent labels (`agentName`) and filter/total consistency assertions across unit + DB-backed integration coverage.
- FEAT-002 S1 code slice 11 complete: assignment operator table now shows status badges and optional agent email enrichment for faster high-volume triage readability.
- FEAT-002 S1 code slice 12 complete: assignment history adds status legend tooltips and filtered CSV export for operator handoff workflows.
- FEAT-002 S1 code slice 13 complete: assignment export now uses backend CSV mode (`format=csv`) with pass-through proxy headers and full filtered paging on server side.
- FEAT-002 S1 code slice 14 complete: assignment CSV export now emits audit telemetry lifecycle events (`requested/succeeded/failed`) with filter context and row-count metadata.
- FEAT-002 S1 code slice 15 complete: admin diagnostics now surfaces assignment export telemetry via tenant-scoped backend read path and dashboard proxy `eventKind=assignment_exports`.
- FEAT-002 S1 code slice 16 complete: assignment export diagnostics now support phase/status filters and filtered table CSV export for operator handoff.
- FEAT-002 S1 code slice 17 complete: assignment export diagnostics now support backend full-range CSV export (`eventKind=assignment_exports&format=csv`) with filter-aware handoff output.
- FEAT-002 S1 code slice 18 complete: admin assignment export diagnostics now expose explicit pagination controls + total matching counts and OpenAPI now documents `assignment-exports` list/export endpoint contracts.
- FEAT-002 S1 code slice 19 complete: OpenAPI now ships a dedicated typed assignment-export telemetry event contract (discriminator + payload schema) for `plot_assignment_export_*` list responses.
- FEAT-002 S1 code slice 20 complete: plots service tests now lock assignment export audit payload key parity (`status/fromDays/agentUserId/rowCount/error`) and null-normalization semantics for `plot_assignment_export_*` events.
- FEAT-002 S1 code slice 21 complete: DB-backed audit integration now proves assignment-export diagnostics read surface returns tenant-scoped `plot_assignment_export_*` payload keys with expected contract mapping.
- FEAT-002 S1 code slice 22 complete: DB-backed audit integration now validates combined assignment-export `phase + status` filters for tenant-scoped diagnostics reads.
- FEAT-002 S1 code slice 23 complete: API-level integration now validates `/v1/audit/gated-entry/assignment-exports` tenant-claim fail-closed and combined phase/status filtering through HTTP pipeline.
- FEAT-002 closeout slice complete: checklist and open-question hygiene are resolved in feature spec and FEAT-002 status is now marked Done.
- FEAT-002 post-closeout hardening slice complete: assignment-export API integration now also validates CSV export endpoint headers/content for filtered HTTP pipeline requests.
- Kick off FEAT-003 (geospatial mapping) execution plan with explicit v1.6 spatial acceptance gates and evidence traceability.
- FEAT-003 S1 matrix is now documented in feature spec (permissions/state transitions/exceptions/analytics/acceptance/v1.6 constraints) and ready for implementation.
- FEAT-003 S1 first code slice complete: plot create now requires tenant claim fail-closed and emits canonical `GEO-101`/`GEO-102` geometry normalization error codes with unit coverage.
- FEAT-003 S1 second code slice complete: plot geometry revision endpoint now enforces tenant/scope gates, records immutable supersession audit events, and reuses canonical `GEO-101`/`GEO-102` validation outcomes.
- FEAT-003 S1 history/read contract slice complete: geometry revision audit history endpoint is now available and documented in OpenAPI for evidence-surface completeness.
- FEAT-003 S1 contract hardening slice complete: geometry history now ships typed response DTOs with positive/negative controller coverage for ownership and forwarding paths.
- FEAT-003 S1 mapping-regression slice complete: service-level tests now lock geometry-history mapping semantics (timestamp/eventType/payload fallbacks) against future audit-row drift.
- FEAT-003 S1 integration-proof slice complete: controller-scope DB-backed tests now assert geometry-history tenant-claim fail-closed and farmer ownership allow/deny behavior.
- FEAT-003 S1 contract-clarity slice complete: OpenAPI geometry-history response now includes concrete `plot_created` and `plot_geometry_superseded` examples.
- FEAT-003 S1 serialization-guard slice complete: controller tests now lock geometry-history camelCase envelope field names to prevent client-facing response drift.
- FEAT-003 S1 dashboard-contract slice complete: dashboard proxy route for geometry-history is now in place with fail-closed backend behavior and auth/header + response-shape pass-through coverage.
- FEAT-003 S1 frontend-usability slice complete: plot detail now includes geometry-history panel driven by `usePlotGeometryHistory` with explicit loading/error/empty/data rendering and component test coverage.
- FEAT-003 S1 panel-scalability slice complete: geometry-history panel now includes event-type filters and visible API cap context to keep larger histories operable.
- FEAT-003 S1 pagination slice complete: geometry-history now supports backend/proxy `limit`/`offset` with total-count page metadata and frontend previous/next pagination controls.
- FEAT-003 S1 chronology-control slice complete: geometry-history now supports `sort` direction (`desc`/`asc`) across backend contract, proxy forwarding, hook state, and panel controls.
- FEAT-003 S1 integration-ordering proof complete: DB-backed controller integration now verifies geometry-history asc/desc ordering on persisted audit events.
- FEAT-003 S1 pagination-reset UX slice complete: switching geometry-history sort direction now resets panel page to 1 with regression test coverage.
- FEAT-003 S1 hook-regression slice complete: `usePlotGeometryHistory` test now locks query generation for `page`/`sort` transitions and auth-header pass-through.
- FEAT-003 S1 page-flow regression slice complete: plot detail page test now verifies real control-driven request sequence for sort switch + pagination offsets.
- FEAT-003 S1 visual-regression slice complete: geometry-history panel now has snapshot checks for loading/error/empty state rendering stability.
- FEAT-003 S1 snapshot-review guidance slice complete: feature doc now includes reviewer checklist for expected vs suspicious snapshot diffs and standard verification steps.
- Snapshot-governance template slice complete: release QA evidence now includes reusable snapshot-diff review checklist and FEAT-003 applied command example for consistent reviewer workflow.
- FEAT-003 S1 checklist dedup slice complete: feature doc now references the shared snapshot governance template and keeps only FEAT-specific commands to reduce doc drift.
- Snapshot governance registry slice complete: release QA evidence now includes a snapshot-backed feature registry table with FEAT-003 baseline commands for repeatable reviewer onboarding.
- FEAT-003 S2 operational UX slice complete: geometry-history now restores user+plot filter/sort presets, surfaces anomaly flags for large jumps/frequent supersessions, and groups timeline rows by day for faster investigation workflows.
- FEAT-003 S2 anomaly-contract slice complete: backend geometry-history now emits typed `anomalies` metadata and dashboard hook consumes server-provided anomaly flags for cross-surface triage consistency.
- FEAT-003 S2 anomaly-sensitivity slice complete: geometry-history now supports `anomalyProfile` (`strict`/`balanced`/`lenient`) across backend contract, dashboard proxy forwarding, hook query state/persistence, and panel controls.
- FEAT-003 S2 investigation ergonomics slice complete: panel now supports `Signals only` anomaly-focused filtering and resets pagination on anomaly-profile changes to prevent stale-offset confusion.
- FEAT-003 S2 anomaly-only pagination slice complete: backend/proxy/hook now support `signalsOnly=true` so anomaly-focused investigations page through full flagged history directly.
- FEAT-003 S2 anomaly-summary slice complete: geometry-history responses now include severity/type summary counters and panel UI displays this compact signal mix for operator triage context.
- FEAT-003 S2 anomaly-summary-scope slice complete: geometry-history now marks summary scope (`current_page` vs `full_filtered_set`) and panel shows this label to avoid count interpretation ambiguity.
- FEAT-003 S2 signals-only pagination-safety slice complete: toggling `Signals only` now resets to page 1, aligning with sort/profile transitions and preventing stale-offset investigation jumps.
- FEAT-003 S2 mode-memory slice complete: panel now remembers last page for `mixed` and `signalsOnly` modes, restoring prior position when toggling views during long investigations.
- FEAT-003 S2 mode-memory persistence slice complete: per-mode page memory is now persisted in user+plot presets with numeric bounds sanitization, preserving investigation position across page reloads.
- FEAT-003 S2 mode-memory bounds slice complete: mode toggles now clamp restored page bookmarks to current total-page limits, avoiding invalid page restores when filtered totals contract.
- FEAT-003 S2 mode-memory clamp centralization slice complete: geometry-history hook now exposes canonical `clampPage` and panel mode toggles consume that shared helper for consistent pagination-bound behavior.
- FEAT-003 S2 bounded-pagination setter slice complete: geometry-history hook `setPage` now applies canonical bounds guardrails so all pagination transitions stay valid against current totals.
- FEAT-003 S2 filter-pagination reset slice complete: geometry-history panel now resets to page 1 when event-type filters change, avoiding stale page offsets during scoped investigation pivots.
- FEAT-003 S2 filter-memory slice complete: geometry-history now persists/restores page bookmarks per event filter so investigators can switch filter views without losing deep-page context.
- FEAT-003 S2 hybrid-view memory slice complete: geometry-history now persists/restores page bookmarks per combined filter+signals mode key for precise context continuity across filter and signals toggles.
- FEAT-003 S2 hybrid-memory cleanup slice complete: panel continuity now relies on combined view-key memory only, with hook-level migration fallback from legacy mode/filter presets.
- FEAT-003 S2 legacy-memory retirement slice complete: deprecated mode/filter page-memory fields are removed from active hook return contract, leaving hybrid view memory as the single bookmark interface.
- FEAT-003 S2 migration-observability slice complete: legacy preset fallback path now records local usage counter telemetry to inform safe removal timing of compatibility parsing.
- FEAT-003 S2 migration-visibility slice complete: geometry-history panel now displays legacy fallback migration counter (`legacyViewFallbackCount`) when present for operator-facing compatibility diagnostics.
- FEAT-003 S2 migration-counter reset slice complete: geometry-history panel now offers `Reset migration counter` action backed by hook-level storage/state reset API for repeatable diagnostics baselining.
- FEAT-003 S2 migration-reset confirmation slice complete: counter reset now uses explicit `Confirm reset` / `Cancel` affordances to prevent accidental telemetry baseline clearing.
- FEAT-003 S2 migration-reset high-signal hardening slice complete: when migration count is high, reset now additionally requires typed `RESET` confirmation before clearing telemetry.
- FEAT-003 closeout slice complete: feature checklist and open question are now resolved in feature spec, and status moved from `In progress` to `Done` for FEAT-003 scope.
- FEAT-004 S1 execution-matrix bootstrap complete: rules-engine feature doc now includes explicit permissions/state transitions/exception recovery/analytics coverage/acceptance mapping and v1.6 constraint applicability for implementation-ready kickoff.
- FEAT-004 S1 code slice 2 complete: backend now exposes deterministic DDS package readiness evaluation (`/v1/harvest/packages/{id}/readiness`) with tenant fail-closed/exporter-only policy, blocker/warning outputs, and OpenAPI contract coverage.
- FEAT-004 S1 code slice 3 complete: readiness evaluation now emits audit lifecycle analytics events (`requested/evaluated/blocked/warning/passed`) with status + blocker/warning counters for diagnostics evidence.
- FEAT-004 S1 code slice 4 complete: DB-backed controller-scope integration now verifies readiness checks persist `dds_package_readiness_*` lifecycle events with expected status/counter payloads.
- FEAT-004 closeout slice complete: feature checklist/open question are resolved and FEAT-004 status is now marked Done.
- Snapshot registry-operability slice complete: release QA evidence now includes a concise update procedure so future FEAT snapshot rows follow a consistent process.
- Snapshot registry CI gate slice complete: contracts workflow now validates snapshot-backed feature registry row shape and required command fields.
- Snapshot registry trend-telemetry slice complete: contracts workflow now emits/validates `openapi-snapshot-registry-report.json`, publishes summary counters, and uploads dedicated snapshot-registry metrics artifact.
- Governance trend unification slice complete: trend and trend-delta artifacts now aggregate snapshot-registry status/row telemetry next to presentation drift metrics, with schema/assert + smoke coverage updated accordingly.
- FEAT-001 close-out completed: checklist, quality evidence, and governance gates are now aligned for done-state handoff.
- Continue dashboard-product hardening: replace mock cooperative inbox requests with tenant-isolated backend APIs and preserve audit event mappings.
- Keep `useInboxRequests` page contract stable while swapping `request-service` local storage implementation to backend endpoints.
- Replace dashboard in-memory inbox API route backing with persisted `tracebud-backend` request entities and tenant-authenticated service calls.
- Harden backend inbox endpoints with authenticated tenant claim checks and migrate snapshot payload storage to dedicated request tables.
- Execute 1-week auth/tenant hardening plan in `product-os/01-roadmap/dashboard-auth-tenant-hardening-week-plan.md` (TB-DBX-001..004).
- TB-DBX-001 complete: signed tenant-claim enforcement and auth-semantic pass-through in dashboard inbox proxy.
- TB-DBX-002 complete: dedicated tenant-scoped inbox request/event tables and indexes now back list/respond/bootstrap operations.
- TB-DBX-003 test harness complete: inbox tenant/state and claim-enforcement tests added.
- TB-DBX-004 complete: MVP feature fences now gate deferred routes at navigation and route-entry levels.
- TB-DBX-004 assertions complete: dashboard Vitest checks now cover deferred-route inaccessibility and feature-flag enable behavior.
- TB-DBX-004 CI lane complete: dashboard `npm test` now runs in root CI workflow.
- Next: enforce `TEST_DATABASE_URL` execution in CI and extend dashboard/integration coverage for additional deferred routes and role-path scenarios.
- Report role-boundary hardening complete: backend report exports now enforce exporter-only access with dedicated controller regression tests; next is extending tenant-boundary denial tests into additional backend modules.
- Harvest/plots tenant-boundary hardening in progress: farmer-role ownership checks added for farmerId/plotId scoped endpoints and package detail/export surfaces tightened to exporter role; next is adding integration coverage with real tenant-linked fixture data.
- Harvest/plots tenant-boundary integration slice added: env-gated ownership integration tests now validate farmer/profile and plot ownership joins in isolated DB schema fixtures; next is executing these tests in CI with `TEST_DATABASE_URL` and extending to controller + API-level integration assertions.
- Harvest/plots controller-level ownership integration added: DB-backed controller integration assertions now cover deny/allow for farmer scoped voucher/plot-list/plot-update paths; next is wiring mandatory CI execution evidence for this specific slice under `TEST_DATABASE_URL`.
- Harvest/plots controller-level policy coverage expanded: DB-backed integration now also asserts tenant-claim + exporter-role enforcement for package list/detail/TRACES export endpoints in controller scope.
- Harvest/plots controller-level package policy parity complete: DB-backed integration now additionally covers submit endpoint (`PATCH /v1/harvest/packages/:id/submit`) under tenant-claim + exporter-role deny/allow paths.
- Ownership/access QA evidence refreshed locally: release QA evidence now records latest ownership lane snapshot (`5 suites`, `19 tests`) pending next CI artifact refresh.
- Ownership CI execution gate added: backend CI now runs a dedicated required ownership integration step; next is extending this explicit-gate pattern to broader tenant-claim API integration slices.
- Signed tenant-claim enforcement expanded beyond inbox to harvest/plots/reports controllers; next is extending these checks to any remaining authenticated endpoints and adding DB-backed API integration coverage for package/report export paths.
- Signed tenant-claim closure for authenticated controllers completed (including `audit`); next is DB-backed API integration coverage for package/report export paths under tenant-claim policy.
- DB-backed package/report export access integration coverage now includes package list/detail/TRACES plus both report endpoints (`plots`, `harvests`) under tenant-claim + role policy; CI now also hard-fails on skipped ownership suites, next is validating first non-skipped run evidence.
- Release QA evidence for ownership/access lane is now populated with CI run URL + artifact proof (`product-os/04-quality/release-qa-evidence.md`) and reviewer PASS signoff; CI runtime modernization (issue #22) implementation is in progress to remove Node 20 deprecation warnings.
- Founder OS foundation complete at schema/spec layer: Supabase migrations/functions/seeds and marketing lead mirror are ready; next is deployment validation and dashboard CRM/content UI scaffolding.
- Apply Founder OS SQL artifacts in Supabase and validate `generate_daily_actions` + `generate_content_tasks` outputs against real leads/content data.
- Founder OS phase 4 scaffold complete in dashboard app (`/crm/*`, `/content/*` + API routes); next step is hardening write-action permissions and adding analytics event coverage for founder action tracking.
- Founder OS Lite execution slice complete: `Today` flow + target bootstrap + exchange logging shipped; next step is analytics event instrumentation for bootstrap/action completion and cadence streak visibility.
- Founder OS analytics + cadence visibility slice complete: bootstrap/completion actions now emit audit-backed founder analytics events and `Today` now surfaces outreach/publishing streaks using recent action history; next is validating streak behavior against seeded historical data and adding UI test coverage.
- Customer-product priority: inbox routes are being hardened to backend-only mode (no local fallback) so tenant-isolated request list/respond/bootstrap flows fail closed when backend is unavailable.
- Inbox proxy hardening regression coverage complete in dashboard tests: fail-closed (`503` without backend URL) and auth-header pass-through are now asserted for list/respond/bootstrap routes.
- Inbox proxy error-propagation coverage complete: dashboard route tests now also assert backend `401/403` status and payload pass-through for list/respond/bootstrap to preserve tenant-claim and role denial semantics in UI flows.
- Dashboard RBAC role-path coverage expanded: mixed feature-gate states are now asserted for importer and country-reviewer navigation visibility to prevent regressions in deferred-route gating by role.
- Dashboard middleware route-entry coverage expanded: gated redirects now preserve query params and include explicit gate markers for deferred routes to improve diagnostics/analytics attribution.
- Dashboard gated-entry analytics slice complete: landing page now parses `feature` + `gate` markers and emits a session-deduped audit analytics event for deferred-route redirect attempts.
- Dashboard gated-entry telemetry endpoint slice complete: dedicated route (`/api/analytics/gated-entry`) now validates marker payloads and receives session-deduped landing telemetry via page-level fetch path with route/page test coverage.
- Dashboard telemetry persistence slice complete: gated-entry route now proxies validated events to backend `/v1/audit` with auth-header pass-through, using local sink fallback only when backend URL is unset.
- Dashboard telemetry read-proxy slice complete: `GET /api/analytics/gated-entry` now proxies tenant-scoped backend reads from `/v1/audit/gated-entry` with fail-closed backend-url enforcement and denial/error pass-through semantics.
- Dashboard telemetry diagnostics UI slice complete: admin panel now renders tenant-scoped gated-entry telemetry from `/api/analytics/gated-entry` with explicit loading/error/empty handling and manual refresh.
- Dashboard telemetry diagnostics UX hardening complete: admin card now includes gate/time filters, pagination, and auth-health hints for invalid-session token reads.
- Telemetry server-side query slice complete: backend and dashboard proxy now support gated-entry query params (`gate`, `fromHours`, `limit`, `offset`) and admin diagnostics consumes server-side paging/filtering directly.
- Telemetry sort-order hardening complete: backend and dashboard proxy now validate/forward `sort` (`desc`/`asc`) for gated-entry reads, and admin diagnostics now supports explicit newest/oldest ordering.
- Telemetry operator workflow hardening complete: admin diagnostics now provides saved filter presets plus page-level CSV export for gated-entry triage handoffs.
- Telemetry full-range export hardening complete: admin diagnostics can now export all matching filtered rows via paginated backend traversal for incident/release evidence handoff.
- Telemetry backend export endpoint slice complete: backend now serves tenant-scoped gated-entry CSV exports and dashboard proxy/admin consume this server-generated path for full-range filter-aware downloads.
- Telemetry export observability hardening complete: backend/proxy now include row-count/limit/truncation headers and admin export flow warns when capped results are returned.
- Telemetry export auditability hardening complete: backend now records `dashboard_gated_entry_exported` events with export metadata for compliance/evidence traceability.
- Telemetry export-activity visibility complete: backend exposes tenant-scoped export event listing and admin diagnostics now renders recent CSV export activity for operator handoff context.
- Telemetry export actor attribution complete: export audit payloads now carry `exportedBy` identity and admin activity table surfaces actor context per export.
- Telemetry actor fallback hardening complete: export events now fallback to `user:<id>` when email is absent and admin export rows fallback to `user_id` before `unknown`.
- Telemetry actor UX enrichment complete: admin diagnostics now resolves known actor IDs to `name (email)` using tenant user directory data for clearer export attribution.
- Telemetry actor enrichment wiring hardened: export-actor resolution now runs inside telemetry export hook via user-directory fetch, reducing page-coupling and stale-snapshot risks.
- Telemetry actor-directory cache slice complete: export hook now caches user-directory lookups (short TTL) and clears cache on manual reload to reduce fetch churn during rapid admin refresh loops.
- Telemetry actor-directory cache invalidation hardened: export hook now subscribes to admin-data mutations and auto-invalidates/reloads actor labels on user/admin updates.
- Telemetry mutation reload coalescing complete: export hook now debounces mutation-triggered refreshes to reduce redundant fetch churn during rapid admin changes.
- Telemetry debounce observability slice complete: export hook now exposes opt-in debug counters/logs for mutation/debounce/fetch behavior to support runtime tuning.
- Telemetry debug UX slice complete: admin diagnostics now exposes a session debug toggle to control export-hook diagnostics without console interaction.
- Telemetry debug visibility slice complete: admin diagnostics now renders live export-hook counters while debug mode is enabled.
- Telemetry actor source hardening complete: export hook now resolves actor IDs through backend tenant-scoped actor endpoint first, reducing reliance on frontend-only directory snapshots.
- Telemetry debug counter delivery hardened: admin debug statusline now receives event-driven counter updates from hook subscriptions instead of interval polling.
- Telemetry actor-resolution integration coverage expanded: audit DB integration suite now validates tenant-scoped actor lookup fallback behavior for mixed-tenant export events.
- Telemetry actor source quality improved: actor lookup now uses canonical `user_account.name` fallback when `exportedBy` is absent, while retaining tenant-scoped filtering guarantees.
- Telemetry actor lookup input hardening complete: `ids` query values are now validated before DB execution to prevent malformed UUID cast failures.
- Telemetry actor contract clarity slice complete: backend+proxy tests now cover missing/invalid `ids` actor lookup paths and feature docs now include explicit valid/invalid request examples.
- Telemetry actor lookup OpenAPI slice complete: draft OpenAPI now documents `/v1/audit/gated-entry/actors` with valid/invalid examples and explicit `400` contract for malformed/missing `ids`.
- Telemetry OpenAPI coverage expanded: draft now also documents `/v1/audit/gated-entry`, `/v1/audit/gated-entry/export`, and `/v1/audit/gated-entry/exports` with pagination/filter/csv metadata semantics.
- Telemetry OpenAPI examples/schema hardening complete: list/export activity endpoints now include concrete `200` examples and a typed telemetry event schema for client consumption.
- Telemetry OpenAPI contract hardening complete: telemetry event envelope is now discriminator-based (`attempt` vs `exported`) with typed payload schemas, and backend unit tests assert export-audit payload contract fields to reduce docs/runtime drift.
- OpenAPI CI gating slice complete: root pipeline now runs a dedicated contracts job (`npm run openapi:lint`) to catch spec-structure regressions automatically.
- OpenAPI governance baseline added: repo now includes `.redocly.yaml` rule config and explicit operation IDs for telemetry/audit export endpoints to support progressive contract-quality enforcement.
- OpenAPI progressive hardening in flight: Requests endpoints (`/v1/requests/campaigns*`) now have explicit operation IDs and baseline `4xx` responses; warning count reduced from `109` to `103`.
- OpenAPI progressive hardening expanded: Plots endpoints (`/v1/plots`, `/v1/plots/{id}/geometry-versions`) now have explicit operation IDs + baseline `4xx` responses; warning count reduced further from `103` to `99`.
- OpenAPI progressive hardening expanded again: Evidence endpoints (`/v1/evidence-documents*`) now have explicit operation IDs + baseline `4xx` responses; warning count reduced from `99` to `95`.
- OpenAPI progressive hardening expanded for both requested domains: Consent (`/v1/consent-grants*`) and Compliance Exports (`/v1/compliance-exports*`) now have explicit operation IDs + baseline `4xx` responses; warning count reduced from `95` to `85`.
- OpenAPI progressive hardening expanded to batches: `/v1/batches*` endpoints now have explicit operation IDs + baseline `4xx` responses; warning count reduced further from `85` to `79`.
- OpenAPI progressive hardening expanded to reviews: `yield-exception` and `dedup-review` endpoints now have explicit operation IDs + baseline `4xx` responses; warning count reduced further from `79` to `69`.
- OpenAPI progressive hardening expanded to shipments: `/v1/shipment-headers*` endpoints now have explicit operation IDs + baseline `4xx` responses; warning count reduced further from `69` to `64`.
- OpenAPI progressive hardening expanded to DDS: `/v1/dds-records/*` endpoints now have explicit operation IDs + baseline `4xx` responses while preserving `409`/`422` contracts; warning count reduced further from `64` to `59`.
- OpenAPI progressive hardening expanded to Sync: `/v1/sync/flush` and `/v1/sync-conflicts/{id}/resolve` now have explicit operation IDs + baseline `4xx` responses; warning count reduced further from `59` to `55`.
- OpenAPI progressive hardening expanded to Portability: `/v1/portability-requests*` endpoints now have explicit operation IDs + baseline `4xx` responses; warning count reduced further from `55` to `51`.
- OpenAPI progressive hardening expanded to annual reporting: `/v1/annual-reporting-snapshots*` endpoints now have explicit operation IDs + baseline `4xx` responses; warning count reduced further from `51` to `47`.
- OpenAPI progressive hardening expanded to compliance issues: `/v1/compliance-issues*` endpoints now have explicit operation IDs + baseline `4xx` responses; warning count reduced further from `47` to `37`.
- OpenAPI progressive hardening expanded to billing + webhooks: `/v1/billing*` and `/v1/webhooks*` endpoints now have explicit operation IDs + baseline `4xx` responses; warning count reduced further from `37` to `27`.
- OpenAPI rule enforcement tightened: `.redocly.yaml` now enforces `operation-operationId` and `operation-4xx-response` as `error`; follow-up fixes were applied on remaining legacy endpoints and lint now passes with warning count reduced from `27` to `21`.
- OpenAPI metadata hygiene closure complete: draft now includes `info.license`, non-placeholder server URLs, and descriptions for all tags; strict lint now passes with zero warnings.
- OpenAPI CI visibility enhancement complete: contracts workflow now publishes lint status + warning count into GitHub job summary for governance trend visibility.
- OpenAPI CI baseline-delta visibility complete: contracts summary now includes errors/warnings baseline deltas from `docs/openapi/lint-baseline.json` and uploads per-run metrics artifact for release evidence trails.
- OpenAPI baseline refresh guardrail complete: baseline updates now require explicit approval + reason (`OPENAPI_BASELINE_APPROVED=true`, `OPENAPI_BASELINE_REASON=...`) via `npm run openapi:baseline:refresh`, which fail-closes if strict lint is not green.
- OpenAPI manual refresh lane complete: dedicated `workflow_dispatch` workflow now gates baseline refresh behind explicit approval input + reason and uploads refreshed baseline evidence artifact.
- OpenAPI baseline PR automation complete: manual refresh workflow now opens a review PR with only baseline file changes to enforce protected-branch adoption of governance baseline updates.
- OpenAPI baseline review ownership complete: CODEOWNERS now protects baseline file and refresh workflow edits behind designated governance reviewer approval.
- OpenAPI governance ownership parity complete: CODEOWNERS now also enforces designated review on `.redocly.yaml` and `docs/openapi/tracebud-v1-draft.yaml` to align policy/spec changes with baseline governance controls.
- OpenAPI branch-protection verification checklist complete: release QA evidence now defines required settings and PR evidence steps to confirm CODEOWNERS protections are enforced on the default branch.
- OpenAPI branch-protection evidence template complete: release QA evidence now includes fill-in placeholders for PR URL, reviewer/timestamp, and mergeability outcomes to standardize governance proof capture.
- OpenAPI governance drift-check complete: CI contracts lane now runs `openapi:governance:check` to assert mandatory OpenAPI CODEOWNERS entries remain present.
- OpenAPI governance owner-pin check complete: CI governance check now also asserts expected CODEOWNERS owner handles for protected OpenAPI paths.
- OpenAPI governance policy externalization complete: CODEOWNERS path/owner requirements now live in `docs/openapi/governance-codeowners-policy.json`, consumed by CI governance checks as the single source of truth.
- OpenAPI governance policy schema gate complete: contracts CI now validates `governance-codeowners-policy.json` shape via dedicated validator + schema before CODEOWNERS checks run.
- OpenAPI governance reporting consolidation complete: contracts CI now emits unified governance check summary + run artifact (`contracts-openapi-governance-metrics`) for policy/codeowners evidence traceability.
- OpenAPI governance report sourcing hardened: CI governance summary now consumes script-emitted JSON reports rather than parsing free-form log text.
- OpenAPI governance raw-report artifacting complete: contracts CI now uploads `contracts-openapi-governance-reports` containing the underlying policy/codeowners report JSON inputs used for summary derivation.
- OpenAPI governance summary navigation improved: summary now includes direct run-link references for governance artifacts to speed reviewer access.
- OpenAPI governance artifact inventory metadata complete: governance metrics JSON now carries expected artifact names and required files for run-level completeness assertions.
- OpenAPI governance artifact inventory assertion complete: contracts CI now checks expected governance evidence files exist before artifact upload steps execute.
- OpenAPI governance artifact JSON-shape assertion complete: contracts CI now validates status/schema consistency across governance metrics and raw report JSON outputs.
- OpenAPI governance assertion maintainability complete: CI now calls centralized script `openapi:governance:artifacts:assert` instead of duplicating assertion logic inline in workflow YAML.
- OpenAPI governance script namespace cleanup complete: governance policy/check/assert scripts now live under `scripts/openapi-governance/` with npm commands updated accordingly.
- OpenAPI governance README complete: script folder now includes operator-facing command/run-order reference to reduce onboarding and handoff friction.
- OpenAPI governance README command gate complete: CI now validates README command references against `package.json` to prevent doc/automation drift.
- OpenAPI governance README path gate complete: CI README check now also validates referenced repo file paths for documentation integrity.
- OpenAPI governance markdown QA utility complete: shared checker now centralizes markdown command/path reference validation for reusable doc-governance enforcement.
- OpenAPI governance markdown QA CLI complete: generic `openapi:governance:markdown:check` now enables argument-driven checks on arbitrary markdown docs without new wrapper scripts.
- OpenAPI governance markdown QA multi-doc CI proof complete: contracts lane now validates `product-os/04-quality/release-qa-evidence.md` via generic checker (`--no-commands`) to enforce release-doc path integrity in production workflow.
- OpenAPI governance markdown-reference metrics complete: checker now supports `--report`, and contracts lane publishes `contracts-markdown-reference-metrics` plus summary counters for ongoing doc-drift trend visibility.
- OpenAPI governance markdown report schema gate complete: contracts lane now asserts `openapi-markdown-reference-report.json` shape + PASS/FAIL consistency before publishing markdown metrics artifact.
- OpenAPI governance markdown report versioning complete: report now emits `schemaVersion: 1` and assertion validates compatibility against `docs/openapi/markdown-reference-report.schema.json`.
- OpenAPI governance markdown CI observability complete: summary block now includes markdown report `Schema version` for direct reviewer visibility in run output.
- OpenAPI governance schema-bump runbook gate complete: README command check now asserts required `Markdown Schema Bump Procedure` heading and 5-step structure to prevent procedural drift.
- OpenAPI governance README-policy externalization complete: README structure anchors now source from `docs/openapi/governance-readme-policy.json` with schema validation via `docs/openapi/governance-readme-policy.schema.json`.
- OpenAPI governance README metrics artifact complete: README check now emits report JSON and contracts lane uploads `contracts-openapi-readme-governance-metrics` for governance evidence parity.
- OpenAPI governance README report schema gate complete: contracts lane now validates `openapi-governance-readme-report.json` against `docs/openapi/governance-readme-report.schema.json` before artifact upload.
- OpenAPI governance metrics unification complete: consolidated governance metrics + artifact-inventory assertions now include README report status/artifact parity alongside policy/CODEOWNERS checks.
- OpenAPI governance metrics generator extraction complete: CI now delegates summary/metrics assembly to `scripts/openapi-governance/metrics-generate.mjs` via `openapi:governance:metrics:generate`.
- OpenAPI governance metrics smoke coverage complete: CI now runs `openapi:governance:metrics:smoke` fixture checks to validate generator status folding and expected artifact inventory before artifacts assertion.
- OpenAPI governance summary-line smoke coverage complete: metrics smoke harness now asserts README/policy/CODEOWNERS/overall status lines in generated governance summary text.
- OpenAPI governance summary template deduplication complete: shared module `scripts/openapi-governance/metrics-summary-lines.mjs` now drives both generator output and smoke assertions.
- OpenAPI governance smoke strictness balancing complete: smoke harness now emphasizes strict structured metrics assertions plus ordered summary-line presence to protect signal while reducing formatting brittleness.
- OpenAPI governance presentation warning lane complete: contracts pipeline now runs non-blocking `openapi:governance:metrics:presentation:check` for summary-format drift visibility without merge blocking.
- OpenAPI governance presentation warning UX complete: CI now appends `Governance Presentation Drift Warning` summary guidance when non-blocking presentation check fails.
- OpenAPI governance presentation telemetry metrics complete: `openapi-governance-metrics.json` now captures `presentationValidationStatus`, `presentationDriftWarningCount`, and `presentationValidationRecordedAt` for per-run drift trend visibility.
- OpenAPI governance drift trend helper complete: `openapi:governance:metrics:trend` now aggregates presentation status/warning totals across one or more metrics artifacts for operator review.
- OpenAPI governance trend publishing complete: contracts lane now writes `Governance Presentation Drift Trend` summary data and uploads `contracts-openapi-governance-presentation-trend` artifact each run.
- OpenAPI governance trend delta lane complete: contracts lane now attempts previous trend artifact retrieval and publishes prior-run deltas for FAIL counts and drift-warning totals when available.
- OpenAPI governance delta-baseline hardening complete: previous trend retrieval now prefers same-branch artifacts and ignores non-success source runs before delta publish.
- OpenAPI governance workflow-source filtering complete: previous trend retrieval now requires matching workflow path (`GITHUB_WORKFLOW_REF`) with workflow-name fallback to reduce cross-workflow baseline noise.
- OpenAPI governance delta baseline traceability complete: delta report + summary now expose baseline run/workflow metadata for operator audit context.
- OpenAPI governance trend/delta schema assertions complete: contracts lane now validates both trend and trend-delta artifacts against dedicated schemas before artifact publication.
- OpenAPI governance assertion smoke coverage complete: contracts lane now runs fixture-based smoke tests for trend/delta assertion scripts to detect contract-checker drift.
- Telemetry index-definition slice complete: SQL artifact (`tb_v16_004_audit_gated_entry_index.sql`) now defines composite audit_log index for event_type + tenant/gate payload filters + timestamp ordering, with DB-backed integration assertion.
- Telemetry index rollout automation complete: backend scripts `npm run db:apply:audit-index` and `npm run db:verify:audit-index` now support `AUDIT_INDEX_DATABASE_URL`/`DATABASE_URL`/`TEST_DATABASE_URL` resolution and verify `idx_audit_log_gated_entry_tenant_gate_ts`; release QA evidence includes local execution snapshots.
- Backend telemetry queryability slice complete: `AuditController` now provides tenant-scoped `GET /v1/audit/gated-entry` for `dashboard_gated_entry_attempt` events with dedicated controller tests.
- Backend telemetry integration slice complete: DB-backed audit integration spec now validates tenant-safe `GET /v1/audit/gated-entry` behavior; ownership lane expanded to include this audit telemetry spec (`5 suites`, `13 tests` local non-skipped).
- Backend inbox controller denial semantics coverage extended: unit tests now assert missing signed tenant-claim rejection on `respond`, explicit non-exporter bootstrap denial messaging, and allow-path bootstrap execution for exporter role.
- Backend controller policy regressions resolved: `audit` and `harvest` controller tests now align with tenant-claim + farmer-scope rules, and backend unit suite is green again.
- DB-backed inbox controller integration coverage added (`inbox.controller.int.spec.ts`) for tenant-claim denial/allow + exporter bootstrap role policy; next is CI evidence capture on non-skipped execution with `TEST_DATABASE_URL`.
- Required ownership CI lane now includes inbox controller DB-backed policy integration (`src/inbox/inbox.controller.int.spec.ts`); next is capturing first CI artifact proving non-skipped 4-suite execution.
- Controller-scope integration flake hardening applied: schema is now dropped/recreated in `controller-scope.int.spec.ts` before setup to avoid stale FK metadata across interrupted runs.
- Backend integration test commands are now local-env resilient: `test:integration` and `test:integration:ownership` auto-load `TEST_DATABASE_URL` from repo-root `.env.local` when unset in shell; local ownership lane now passes non-skipped (`4 suites`, `10 tests`).
- Ownership/access CI evidence refreshed for expanded 5-suite lane (`run 24445875783`, artifact `6446796613`, `5/5 suites`, `13/13 tests`) including audit gated-entry integration.
- Backend and offline-product CI blockers are stabilized: inbox schema checks now self-heal on transient missing-table states and offline-product lint lane is warning-clean on current branch state.
- Supabase advisor-driven RLS baseline is complete for highest-risk compliance tables (`user_account`, `farmer_profile`, `plot`, `harvest_transaction`, `voucher`, `dds_package`, `dds_package_voucher`, `audit_log`); next is phase-2 coverage for remaining public tables plus mutable `search_path` function hardening.
- Supabase advisor phase-2 is complete for app-domain public tables and mutable public functions; next is owner-level remediation for extension-owned `public.spatial_ref_sys` plus policy refinement on phase-2 RLS-enabled tables where direct client access is intended.
- Supabase lead-intake policy hardening is complete (`leads_public_insert_guarded`); next is owner-level remediation for extension-owned `public.spatial_ref_sys` and explicit ownership-key design for phase-2 app tables before opening any direct client RLS policies.
- `public.spatial_ref_sys` advisor item remains extension-owner gated for direct fix execution; owner runbook `tb_v16_009_postgis_owner_remediation_runbook.sql` is hardened (RLS + DML revoke + schema-agnostic handling + `SET SCHEMA` unsupported guard) and should be executed in the next privileged infra window, then re-validated with Supabase advisors.

## Priority migration lanes (v1.6)

- Spatial lane: enforce `GEOGRAPHY` + `ST_MakeValid` + area variance guard.
- Sync lane: enforce HLC conflict ordering and idempotency behavior.
- Lineage lane: enforce O(1) traversal via materialized lineage fields.
- TRACES lane: enforce payload chunking strategy and multi-reference reconciliation.
- GDPR lane: enforce cryptographic shredding with retention-safe audit preservation.

## Open first

- `product-os/02-features/FEAT-003-geospatial-mapping.md`

## Then

- `product-os/01-roadmap/dependency-map.md`
- `product-os/04-quality/acceptance-criteria.md`
- `product-os/05-decisions/ADR-001-tenant-model.md`
